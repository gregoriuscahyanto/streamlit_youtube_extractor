"""
OCR Extractor - Streamlit App v4
Tab 1: Cloudflare R2-Verbindung, Prefix waehlen, Datei-Browser
Tab 2: Video laden, Start/Ende, ROI-Auswahl
Tab 3: Track-Minimap Analyse - 8-Punkte + Farberkennung
"""

import streamlit as st
import cv2
import numpy as np
import json
import tempfile
import io
import time
import os
import subprocess
import shutil
import sys
import threading
import traceback
import concurrent.futures as cf
import scipy.io as sio
try:
    from scipy.io.matlab._mio5_params import MatlabObject
except Exception:
    MatlabObject = None
from scipy import signal
from scipy.io import wavfile
import pandas as pd
from pathlib import Path
from datetime import datetime
from PIL import Image
try:
    from streamlit_image_coordinates import streamlit_image_coordinates
except Exception:
    streamlit_image_coordinates = None
try:
    from streamlit_cropper import st_cropper
except Exception:
    st_cropper = None
try:
    from streamlit_js_eval import streamlit_js_eval
except Exception:
    streamlit_js_eval = None

from local_storage import LocalStorageAdapter
from backend import (
    build_result_payload,
    build_mat_struct as backend_build_mat_struct,
    collect_r2_listing_debug,
    config_from_json_payload,
    config_from_mat_file,
    connect_r2_client,
    guess_fixed_points,
    list_root_prefixes,
    load_centerline_from_mat,
    load_r2_credentials,
    render_centerline_image,
    save_slim_mat,
    summarize_mat_file,
)
from storage import StorageManager
from roi_utils import (
    can_add_roi_from_drag,
    clamp_roi_to_video,
    normalize_time_range,
    roi_from_crop_box,
    seed_drag_roi,
)
from ocr_diagnostic import diagnose_roi_ocr, find_tesseract_cmd
from track_analysis import (
    compare_minimap_to_reference,
    detect_moving_point,
    draw_comparison_overlay,
    extract_minimap_crop,
    project_point_with_homography,
)

# Crash-Log
LOG_DIR = Path.cwd() / "logs"
LOG_FILE = LOG_DIR / "app_crash.log"


def _append_crash_log(header: str, tb_text: str):
    try:
        LOG_DIR.mkdir(parents=True, exist_ok=True)
        stamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        with LOG_FILE.open("a", encoding="utf-8") as f:
            f.write(f"\n\n=== {stamp} | {header} ===\n")
            f.write(tb_text)
            f.write("\n")
    except Exception:
        pass


def _is_ignorable_shutdown_exception(exc_type, exc_value, tb_text: str) -> bool:
    # Streamlit shutdown can raise this benign runtime error while the event loop
    # is already closing. We don't want to classify that as an app crash.
    if exc_type is RuntimeError and "Event loop is closed" in str(exc_value):
        if ("streamlit\\web\\bootstrap.py" in tb_text) or ("weakref.py" in tb_text):
            return True
    # Python tempfile cleanup at interpreter shutdown can fail on Windows when
    # temp dirs are still locked by background handles.
    if exc_type is PermissionError and "WinError 5" in str(exc_value):
        if ("tempfile.py" in tb_text) and ("_cleanup" in tb_text) and ("weakref.py" in tb_text):
            return True
    return False


def _handle_unhandled_exception(exc_type, exc_value, exc_tb):
    tb_text = "".join(traceback.format_exception(exc_type, exc_value, exc_tb))
    if _is_ignorable_shutdown_exception(exc_type, exc_value, tb_text):
        return
    _append_crash_log("Unhandled Exception", tb_text)


def _handle_thread_exception(args):
    tb_text = "".join(traceback.format_exception(args.exc_type, args.exc_value, args.exc_traceback))
    if _is_ignorable_shutdown_exception(args.exc_type, args.exc_value, tb_text):
        return
    _append_crash_log(f"Thread Exception ({getattr(args, 'thread', None)})", tb_text)


sys.excepthook = _handle_unhandled_exception
if hasattr(threading, "excepthook"):
    threading.excepthook = _handle_thread_exception


def _try_jump_to_tab(label: str):
    return

st.set_page_config(
    page_title="OCR Extractor",
    page_icon="OCR",
    layout="wide",
    initial_sidebar_state="collapsed",
)

# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ CSS ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬
st.markdown("""
<style>
@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&family=Syne:wght@400;600;800&display=swap');

html, body, [class*="css"] { font-family: 'Syne', sans-serif; }
.stApp { background: #0d0f14; color: #e8eaf0; overflow: auto; }
.block-container { padding-top: 1.1rem !important; max-width: 1500px; height: calc(100vh - 3.2rem); overflow: auto; }

.app-header { display:flex; align-items:baseline; gap:14px;
  margin-bottom:1rem; border-bottom:1px solid #1e2535; padding-bottom:.7rem; }
.app-header h1 { font-family:'Syne',sans-serif; font-weight:800; font-size:1.5rem;
  color:#e8eaf0; margin:0; letter-spacing:-.02em; }
.app-header .subtitle { font-family:'JetBrains Mono',monospace; font-size:.68rem;
  color:#4a90a4; letter-spacing:.1em; text-transform:uppercase; }

.status-badge { display:inline-block; padding:3px 10px; border-radius:3px;
  font-family:'JetBrains Mono',monospace; font-size:.7rem; font-weight:600;
  letter-spacing:.05em; text-transform:uppercase; }
.status-ok   { background:#0d2e1a; color:#3ddc84; border:1px solid #1a5c34; }
.status-warn { background:#2e1f0d; color:#ffa040; border:1px solid #5c3a1a; }
.status-info { background:#0d1e2e; color:#4a90a4; border:1px solid #1a3a5c; }

.section-card { background:#13161f; border:1px solid #1e2535; border-radius:6px;
  padding:.9rem 1.1rem; margin-bottom:.9rem; }
.section-card:empty { display:none !important; padding:0 !important; margin:0 !important; border:0 !important; }
.mat-selection-no-scroll {
  max-height: calc(100vh - 230px);
  overflow-y: auto;
  overflow-x: hidden;
}
.mat-selection-disabled {
  opacity: .48;
  filter: grayscale(1);
  pointer-events: none;
  user-select: none;
}
.mat-selection-disabled [data-testid="stDataFrame"] {
  background: #20232b !important;
  border-radius: 6px;
}
.st-key-cloud_access_card,
.st-key-cloud_root_card,
.st-key-cloud_access_card [data-testid="stVerticalBlockBorderWrapper"],
.st-key-cloud_root_card [data-testid="stVerticalBlockBorderWrapper"] {
  background: #0b1524 !important;
  border-color: #2b4f77 !important;
}
.st-key-local_access_card,
.st-key-local_access_card [data-testid="stVerticalBlockBorderWrapper"] {
  background: #132114 !important;
  border-color: #376a3d !important;
}
.section-title { font-family:'JetBrains Mono',monospace; font-size:.68rem;
  letter-spacing:.15em; text-transform:uppercase; color:#4a90a4;
  margin-bottom:.6rem; border-bottom:1px solid #1e2535; padding-bottom:.35rem; }

.breadcrumb { font-family:'JetBrains Mono',monospace; font-size:.72rem;
  color:#4a90a4; background:#0a0c10; border:1px solid #1e2535;
  border-radius:4px; padding:5px 10px; margin-bottom:.7rem; }

.conn-dot { display:inline-block; width:8px; height:8px; border-radius:50%; margin-right:6px; }
.conn-dot.ok  { background:#3ddc84; box-shadow:0 0 6px #3ddc8466; }
.conn-dot.off { background:#ff5c5c; box-shadow:0 0 6px #ff5c5c66; }

.roi-tag { background:#1e2535; color:#4a90a4; padding:1px 6px; border-radius:3px;
  font-size:.65rem; font-family:'JetBrains Mono',monospace; white-space:nowrap; }
.roi-tag-track { background:#1e3520; color:#3ddc84; }
.roi-tag-sel   { background:#1a3a5c; color:#90c8e0; }

.frame-info { font-family:'JetBrains Mono',monospace; font-size:.7rem; color:#8892a4;
  padding:4px 8px; background:#0a0c10; border-radius:3px; display:inline-block; }

.metric-box { background:#0d0f14; border:1px solid #1e2535; border-radius:5px;
  padding:.6rem .8rem; text-align:center; }
.metric-val { font-family:'JetBrains Mono',monospace; font-size:1.3rem;
  font-weight:700; color:#4a90a4; }
.metric-lbl { font-family:'JetBrains Mono',monospace; font-size:.62rem;
  color:#4a5060; text-transform:uppercase; letter-spacing:.1em; margin-top:2px; }

.stButton>button { font-family:'JetBrains Mono',monospace !important;
  font-size:.75rem !important; font-weight:600 !important;
  letter-spacing:.04em !important; border-radius:4px !important; }
.stButton>button[kind="primary"] { background:#4a90a4 !important;
  border-color:#4a90a4 !important; color:#0d0f14 !important; }

hr { border-color:#1e2535 !important; }

/* selected row highlight in dataframe */
.stDataFrame [aria-selected="true"] {
  background-color: rgba(74, 144, 164, 0.30) !important;
}
.ref-track-fit img { max-height: 340px; object-fit: contain; }
.track-samples-grid { display:grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap:.65rem; overflow-x:auto; }
.track-sample-card { background:#0a0c10; border:1px solid #1e2535; border-radius:6px; padding:.45rem; min-width:0; }
.track-progress-big { font-family:'JetBrains Mono',monospace; font-size:2.05rem; font-weight:800; color:#3ddc84; line-height:1.1; margin:.22rem 0 .08rem; }
.track-metrics-small { font-family:'JetBrains Mono',monospace; font-size:.62rem; color:#8892a4; line-height:1.35; }
.mat-analysis-grid { display:grid; grid-template-columns: repeat(auto-fit, minmax(210px, 1fr)); gap:.75rem; margin-top:.75rem; }
.mat-analysis-card { background:#0a0c10; border:1px solid #1e2535; border-radius:7px; padding:.75rem .85rem; }
.mat-analysis-title { font-family:'JetBrains Mono',monospace; font-size:.64rem; color:#8892a4; letter-spacing:.08em; text-transform:uppercase; margin-bottom:.4rem; }
.mat-analysis-value { font-family:'JetBrains Mono',monospace; font-size:1.28rem; font-weight:800; color:#e8eaf0; line-height:1.05; }
.mat-analysis-sub { font-family:'JetBrains Mono',monospace; font-size:.62rem; color:#4a90a4; margin-top:.25rem; }
.mat-progress-outer { height:9px; border-radius:999px; background:#1e2535; overflow:hidden; margin-top:.55rem; }
.mat-progress-inner { height:100%; border-radius:999px; background:#3ddc84; }
.mat-analysis-bars { display:grid; grid-template-columns: 1fr; gap:.42rem; margin-top:.7rem; }
.mat-analysis-bar-row { display:grid; grid-template-columns: 190px 1fr 88px; align-items:center; gap:.65rem; font-family:'JetBrains Mono',monospace; font-size:.68rem; color:#b7c3d8; }
.mat-analysis-bar-track { height:16px; border-radius:999px; background:#1e2535; overflow:hidden; border:1px solid #243049; }
.mat-analysis-bar-fill { height:100%; border-radius:999px; background:#4a90a4; }
.mat-analysis-note { font-family:'JetBrains Mono',monospace; font-size:.62rem; color:#4a5060; margin-top:.4rem; }
.roi-theme-card { background:#101722; border:1px solid #25344a; border-radius:9px; padding:.75rem .9rem; margin:.75rem 0; }
.roi-theme-card .theme-title { font-family:'JetBrains Mono',monospace; font-size:.72rem; color:#8fcbe0; letter-spacing:.12em; text-transform:uppercase; margin-bottom:.35rem; }
.roi-theme-card .theme-text { font-family:'JetBrains Mono',monospace; font-size:.68rem; color:#8892a4; line-height:1.45; }
.save-status-card { background:#0d2e1a; border:1px solid #1a5c34; color:#dfffe8; border-radius:7px; padding:.55rem .75rem; font-family:'JetBrains Mono',monospace; font-size:.68rem; margin-top:.45rem; }
@media (max-width: 1100px) { .track-samples-grid { grid-template-columns: repeat(5, minmax(170px, 1fr)); } }
</style>
""", unsafe_allow_html=True)

# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ ROI / Format Listen ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬
ROI_NAMES = [
    "_","t_s","v_Fzg_kmph","v_Fzg_mph","numgear_GET",
    "a_G","a_mps2","a_x_G","a_x_pos_G","a_x_neg_G","a_x_mps",
    "a_y_G","a_y_pos_G","a_y_neg_G","a_y_mps",
    "P_kW","M_Nm","n_mot_Upmin",
    "M_VL_Nm","M_VR_Nm","M_HL_Nm","M_HR_Nm",
    "stellung_gaspedal_proz","stellung_bremspedal_proz","track_minimap",
]
FMT_OPTIONS = [
    "any","time_m:ss","time_m:ss.S","time_m:ss.SS","time_m:ss.SSS",
    "time_m:ss.SSSS","time_m:ss.SSSSSS","time_mm:ss","time_mm:ss.S",
    "time_mm:ss.SS","time_mm:ss.SSS","time_mm:ss.SSSS","time_mm:ss.SSSSSS",
    "time_hh:mm:ss","time_hh:mm:ss.S","time_hh:mm:ss.SS","time_hh:mm:ss.SSS",
    "time_hh:mm:ss.SSSS","time_hh:mm:ss.SSSSSS",
    "integer","int_1","int_2","int_3","int_4","int_min2_max3","int_min3_max4",
    "float","alnum",
]
MAT_OVERVIEW_COLCFG = {
    "mat_datei": st.column_config.TextColumn("mat_datei", width="medium"),
    "remote_key": st.column_config.TextColumn("remote_key", width="large"),
    "audio_video_vorhanden": st.column_config.TextColumn("Audio+Video vorhanden", width="small"),
    "roi_ausgewaehlt": st.column_config.TextColumn("ROI", width="small"),
    "track_ausgewaehlt": st.column_config.TextColumn("Track", width="small"),
    "anfang_ende_ausgewaehlt": st.column_config.TextColumn("Start/Ende", width="small"),
    "ocr_durchgefuehrt": st.column_config.TextColumn("OCR", width="small"),
    "ocr_vollstaendig": st.column_config.TextColumn("OCR vollstaendig", width="small"),
    "audioanalyse_spektrogramm": st.column_config.TextColumn("Audio/Spektrogramm", width="small"),
    "validierung": st.column_config.TextColumn("Validierung", width="small"),
    "fehler": st.column_config.TextColumn("Fehler", width="large"),
}
MAT_TABLE_HEIGHT = 430
VIDEO_EXTS = (".mp4", ".mov", ".avi", ".mkv")
AUDIO_EXTS = (".wav", ".mp3", ".m4a", ".aac", ".flac")
FRAMEPACK_JPEG_QUALITY = 15
FRAMEPACK_MAX_WIDTH = 0  # keep original resolution; 0 disables resize
AUDIO_PROXY_ENABLED = True
AUDIO_LOWPASS_HZ = 1000
AUDIO_TARGET_SR = 4000
AUDIO_PROXY_NAME = "audio_proxy_1k.wav"
LAMP_GREEN = "\U0001F7E2"
LAMP_RED = "\U0001F534"
MOJIBAKE_GREEN = "\u00f0\u0178\u0178\u00a2"
MOJIBAKE_RED = "\u00f0\u0178\u201d\u00b4"

# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ Session-State ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬
def init_state():
    _acc, _key, _sec, _bkt = load_r2_credentials(streamlit_secrets=st.secrets)
    _local_default = str((st.secrets.get("local") or {}).get("default_path") or Path.cwd())

    defs = dict(
        # R2
        r2_account_id=_acc,
        r2_access_key_id=_key,
        r2_secret_access_key=_sec,
        r2_bucket=_bkt,
        r2_connected=False, r2_client=None,
        r2_prefix="",
        r2_prefix_options=[],
        r2_listing_debug=[],
        auto_connect_attempted=False,
        auto_connect_used=False,
        local_auto_connect_attempted=False,
        # Local DB
        local_base_path=_local_default,
        local_base_path_input=_local_default,
        local_connected=False,
        local_client=None,
        local_root="",
        local_root_options=[],
        sync_overview_rows=[],
        sync_queue_rows=[],
        sync_status_map={},
        sync_running=False,
        sync_stop_requested=False,
        sync_run_queue=[],
        sync_run_idx=0,
        sync_run_total=0,
        sync_run_started_ts=0.0,
        sync_run_selected_folders=[],
        sync_selected_folders=[],
        sync_editor_value=None,
        sync_refresh_running=False,
        sync_refresh_idx=0,
        sync_refresh_total=0,
        sync_refresh_folders=[],
        sync_refresh_rows=[],
        mat_files=[],
        mat_targets=[],
        mat_scan_prefix=None,
        mat_selected_key="",
        mat_pending_selected_key="",
        mat_selected_summary=None,
        mat_summary_cache={},
        mat_overview_rows=[],
        mat_auto_updated_prefix=None,
        jump_to_mat_tab=False,
        mat_update_running=False,
        mat_update_idx=0,
        mat_update_total=0,
        mat_update_keys=[],
        mat_run_state="idle",
        mat_load_requested=False,
        mat_load_running=False,
        tab_default=None,
        # Datei-Browser
        fb_path="", fb_items=[], fb_selected=None,
        # Aufnahme
        capture_folder="",
        # Video / ROI
        video_path=None, video_name="",
        media_source="none",
        framepack_remote_prefix="",
        framepack_files=[],
        framepack_cache={},
        vid_duration=0.0, vid_fps=25.0, vid_width=0, vid_height=0,
        t_start=0.0, t_end=0.0, t_current=0.0,
        rois=[], selected_roi=None,
        drag_roi={},
        roi_draw_armed=False,
        roi_force_default_once=False,
        roi_last_frame_key=None,
        roi_seed_drag={},
        roi_seen_nonseed=False,
        roi_reject_seed_once=False,
        roi_cropper_ver=0,
        roi_prev_frame_idx=None,
        roi_wait_user_move=False,
        roi_anchor_box={},
        roi_reject_anchor_events=0,
        roi_pending_select_idx=None,
        roi_click_stage=0,
        roi_click_p1=None,
        roi_click_last_sig="",
        roi_display_meta={},
        roi_editor_df=None,
        roi_ocr_probe_result=None,
        roi_global_scale=1.2,
        roi_editor_widget_key="roi_data_editor_v3",
        # Track
        ref_track_img=None, ref_track_pts=None, minimap_pts=None,
        centerline=None, centerline_px=None, ref_track_mat_name="",
        minimap_next_pt_idx=0,
        track_comparison=None, moving_pt_history=[],
        moving_pt_color_range=dict(h_lo=0,h_hi=30,s_lo=150,s_hi=255,v_lo=150,v_hi=255),
        # Status
        status_msg="Bereit", status_type="info",
    )
    for k, v in defs.items():
        if k not in st.session_state:
            st.session_state[k] = v

init_state()

# One-time migration for legacy ROI editor widget state keys that could hold
# incompatible column schema/dtypes.
for _legacy_roi_key in ("roi_data_editor", "roi_data_editor_v1", "roi_data_editor_v2"):
    if _legacy_roi_key in st.session_state:
        st.session_state.pop(_legacy_roi_key, None)

# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ Hilfsfunktionen ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬
@st.cache_data(show_spinner=False)
def get_frame(video_path: str, time_s: float):
    cap = cv2.VideoCapture(video_path)
    if not cap.isOpened(): return None
    fps = cap.get(cv2.CAP_PROP_FPS) or 25.0
    cap.set(cv2.CAP_PROP_POS_FRAMES, int(time_s * fps))
    ret, frame = cap.read()
    cap.release()
    return cv2.cvtColor(frame, cv2.COLOR_BGR2RGB) if ret else None

@st.cache_data(show_spinner=False)
def get_video_info(video_path: str):
    cap = cv2.VideoCapture(video_path)
    fps = cap.get(cv2.CAP_PROP_FPS) or 25.0
    info = dict(fps=fps,
                width=int(cap.get(cv2.CAP_PROP_FRAME_WIDTH)),
                height=int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT)),
                frames=int(cap.get(cv2.CAP_PROP_FRAME_COUNT)))
    info["duration"] = info["frames"] / fps
    cap.release()
    return info

def set_status(msg, kind="info"):
    st.session_state.status_msg  = msg
    st.session_state.status_type = kind


def _list_local_root_options(base_path: str) -> list[str]:
    try:
        base = Path(base_path).expanduser().resolve()
    except Exception:
        return [""]
    if not base.exists() or not base.is_dir():
        return [""]
    opts = [""]
    try:
        for p in sorted(base.iterdir(), key=lambda x: x.name.lower()):
            if p.is_dir():
                opts.append(p.name)
    except Exception:
        pass
    return opts


def _local_effective_root() -> str:
    base = Path(st.session_state.local_base_path).expanduser()
    root = st.session_state.local_root.strip("/\\")
    return str((base / root).resolve() if root else base.resolve())


def _pick_local_folder_dialog(initial_dir: str = "") -> tuple[bool, str]:
    try:
        import tkinter as tk
        from tkinter import filedialog

        root = tk.Tk()
        root.withdraw()
        root.attributes("-topmost", True)
        start_dir = initial_dir or str(Path.cwd())
        selected = filedialog.askdirectory(initialdir=start_dir)
        root.destroy()
        if selected:
            return True, selected
        return False, ""
    except Exception as e:
        return False, str(e)


def _get_cloud_capture_folders() -> list[str]:
    if not st.session_state.r2_connected or st.session_state.r2_client is None:
        return []
    pfx = st.session_state.r2_prefix.strip("/")
    cap_dir = f"{pfx}/captures" if pfx else "captures"
    ok, items = st.session_state.r2_client.list_files(cap_dir)
    if not ok or not isinstance(items, list):
        return []
    out = [i.rstrip("/") for i in items if i.endswith("/")]
    out.sort()
    return out


def _get_local_capture_folders() -> list[str]:
    client = st.session_state.local_client
    if not st.session_state.local_connected or client is None:
        return []
    ok, items = client.list_files("captures")
    if not ok or not isinstance(items, list):
        return []
    out = [i.rstrip("/") for i in items if i.endswith("/")]
    out.sort()
    return out


def _has_local_fullfps_video(folder: str) -> tuple[bool, int]:
    client = st.session_state.local_client
    if not st.session_state.local_connected or client is None:
        return False, 0
    ok, items = client.list_files(f"captures/{folder}")
    if not ok or not isinstance(items, list):
        return False, 0
    vids = [
        n for n in items
        if (not n.endswith("/")) and n.lower().endswith((".mp4", ".mov", ".avi", ".mkv"))
    ]
    fullfps = [n for n in vids if "_1fps" not in n.lower()]
    return len(fullfps) > 0, len(fullfps)


def _has_cloud_proxy_video(folder: str) -> tuple[bool, int]:
    if not st.session_state.r2_connected or st.session_state.r2_client is None:
        return False, 0
    pfx = st.session_state.r2_prefix.strip("/")
    remote = f"{pfx}/captures/{folder}" if pfx else f"captures/{folder}"
    ok, items = st.session_state.r2_client.list_files(remote)
    if not ok or not isinstance(items, list):
        return False, 0
    vids = [
        n for n in items
        if (not n.endswith("/")) and n.lower().endswith((".mp4", ".mov", ".avi", ".mkv"))
    ]
    proxy = [n for n in vids if "_1fps" in n.lower()]

    # Frame-pack support: captures/<folder>/frames_1fps/{000001.jpg, ..., index.json}
    frame_count = 0
    if "frames_1fps/" in items:
        ok_fp, fp_items = st.session_state.r2_client.list_files(f"{remote}/frames_1fps")
        if ok_fp and isinstance(fp_items, list):
            frame_count = len([x for x in fp_items if x.lower().endswith((".jpg", ".jpeg", ".png", ".webp"))])

    total_count = len(proxy) + frame_count
    return total_count > 0, total_count


def _expected_reduced_frame_count_for_video(src_video: Path) -> int:
    cap = cv2.VideoCapture(str(src_video))
    if not cap.isOpened():
        return 0
    try:
        fps = float(cap.get(cv2.CAP_PROP_FPS) or 0.0)
        frame_count = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0)
        if fps <= 0:
            fps = 25.0
        duration_s = (frame_count / fps) if frame_count > 0 else 0.0
        return max(1, int(np.ceil(duration_s)))
    except Exception:
        return 0
    finally:
        cap.release()


def _cloud_reduced_completeness(folder: str, src_video: Path | None) -> tuple[bool, int, int, str]:
    """
    Returns (is_complete, cloud_count, expected_count, status_text).
    """
    _exists, cloud_count = _has_cloud_proxy_video(folder)
    expected_count = 0
    if src_video is not None:
        expected_count = _expected_reduced_frame_count_for_video(src_video)
    if expected_count <= 0:
        return False, int(cloud_count), int(expected_count), "Nein (Erwartete Frames unbekannt)"

    pfx = st.session_state.r2_prefix.strip("/")
    cap_dir = f"{pfx}/captures/{folder}" if pfx else f"captures/{folder}"
    audio_ok = False
    if st.session_state.r2_connected and st.session_state.r2_client is not None:
        ok_cap, cap_items = st.session_state.r2_client.list_files(cap_dir)
        if ok_cap and isinstance(cap_items, list):
            lowered = [x.lower() for x in cap_items if not x.endswith("/")]
            audio_ok = AUDIO_PROXY_NAME.lower() in lowered

    complete_frames = int(cloud_count) >= int(expected_count)
    if complete_frames and audio_ok:
        text = "Ja"
    else:
        parts = [f"Frames {int(cloud_count)}/{int(expected_count)}"]
        if not audio_ok:
            parts.append("Audio fehlt")
        text = f"Nein ({', '.join(parts)})"
    return bool(complete_frames and audio_ok), int(cloud_count), int(expected_count), text


def _build_sync_overview_rows() -> tuple[list[dict], list[dict]]:
    local_folders = sorted(set(_get_local_capture_folders()))

    overview_rows: list[dict] = []
    queue_rows: list[dict] = []

    for folder in local_folders:
        local_ok, _local_count = _has_local_fullfps_video(folder)
        if not local_ok:
            continue
        src_video = _find_local_fullfps_video(folder)
        cloud_complete, cloud_count, expected_count, cloud_text = _cloud_reduced_completeness(folder, src_video)
        state = "OK" if cloud_complete else ""
        row = {
            "auswaehlen": False,
            "capture_folder": folder,
            "reduziert_in_cloud": cloud_text,
            "status": state,
            "cloud_framepack_anzahl": int(cloud_count),  # internal/debug
            "expected_framepack_anzahl": int(expected_count),  # internal/debug
        }
        overview_rows.append(row)
        if not cloud_complete:
            queue_rows.append(row)

    return overview_rows, queue_rows


def _local_capture_folder_path(folder: str) -> Path | None:
    if not st.session_state.local_connected:
        return None
    try:
        base = Path(st.session_state.local_base_path).expanduser().resolve()
        p = (base / "captures" / folder).resolve()
        if base != p and base not in p.parents:
            return None
        return p
    except Exception:
        return None


def _find_local_fullfps_video(folder: str) -> Path | None:
    cap_dir = _local_capture_folder_path(folder)
    if cap_dir is None or (not cap_dir.exists()) or (not cap_dir.is_dir()):
        return None
    candidates_by_ext: dict[str, list[Path]] = {".avi": [], ".mp4": [], ".mov": [], ".mkv": []}
    for p in cap_dir.iterdir():
        if not p.is_file():
            continue
        if p.suffix.lower() not in VIDEO_EXTS:
            continue
        if "_1fps" in p.name.lower():
            continue
        candidates_by_ext[p.suffix.lower()].append(p)
    for ext in [".avi", ".mp4", ".mov", ".mkv"]:
        cands = candidates_by_ext.get(ext) or []
        if cands:
            return max(cands, key=lambda x: x.stat().st_size)
    return None


def _find_local_audio_file(folder: str) -> Path | None:
    cap_dir = _local_capture_folder_path(folder)
    if cap_dir is None or (not cap_dir.exists()) or (not cap_dir.is_dir()):
        return None
    candidates_by_ext: dict[str, list[Path]] = {e: [] for e in AUDIO_EXTS}
    for p in cap_dir.iterdir():
        if not p.is_file():
            continue
        ext = p.suffix.lower()
        if ext in candidates_by_ext:
            candidates_by_ext[ext].append(p)
    # Prefer WAV because scipy wav reader is robust and fast.
    for ext in [".wav", ".flac", ".m4a", ".mp3", ".aac"]:
        cands = candidates_by_ext.get(ext) or []
        if cands:
            return max(cands, key=lambda x: x.stat().st_size)
    return None


def _build_audio_proxy_wav(src_audio: Path, out_wav: Path) -> tuple[bool, str]:
    """
    Build compact mono WAV with lowpass + downsampling for WAV/MP3 sources.
    Uses ffmpeg if available (system ffmpeg or imageio-ffmpeg bundled binary).
    """
    ffmpeg_exe = shutil.which("ffmpeg")
    if not ffmpeg_exe:
        try:
            import imageio_ffmpeg
            ffmpeg_exe = imageio_ffmpeg.get_ffmpeg_exe()
        except Exception:
            ffmpeg_exe = None

    if ffmpeg_exe:
        cmd = [
            ffmpeg_exe,
            "-y",
            "-i", str(src_audio),
            "-vn",
            "-ac", "1",
            "-ar", str(int(AUDIO_TARGET_SR)),
            "-af", f"lowpass=f={int(AUDIO_LOWPASS_HZ)}",
            str(out_wav),
        ]
        try:
            proc = subprocess.run(cmd, capture_output=True, text=True)
            if proc.returncode == 0 and out_wav.exists() and out_wav.stat().st_size > 0:
                return True, ""
            err = (proc.stderr or proc.stdout or "").strip()
            return False, f"ffmpeg fehlgeschlagen: {err[-240:]}"
        except Exception as e:
            return False, f"ffmpeg Aufruf fehlgeschlagen: {e}"

    # Minimal fallback for plain WAV without ffmpeg.
    if src_audio.suffix.lower() != ".wav":
        return False, "Kein ffmpeg verfuegbar (fuer MP3 benoetigt)."
    try:
        sr, data = wavfile.read(str(src_audio))
        x = data.astype(np.float32, copy=False)
        if x.ndim > 1:
            x = np.mean(x, axis=1)
        if np.issubdtype(data.dtype, np.integer):
            max_abs = float(np.iinfo(data.dtype).max)
            if max_abs > 0:
                x = x / max_abs
        cutoff = min(float(AUDIO_LOWPASS_HZ), 0.45 * float(sr))
        if cutoff > 10.0 and sr > 2 * cutoff:
            b, a = signal.butter(4, cutoff / (0.5 * float(sr)), btype="low")
            x = signal.filtfilt(b, a, x).astype(np.float32)
        target_sr = int(min(max(2000, int(AUDIO_TARGET_SR)), int(sr)))
        if target_sr != int(sr):
            x = signal.resample_poly(x, target_sr, int(sr)).astype(np.float32)
        x = np.clip(x, -1.0, 1.0)
        y = (x * 32767.0).astype(np.int16)
        wavfile.write(str(out_wav), int(target_sr), y)
        return True, ""
    except Exception as e:
        return False, f"Audio-Proxy Build fehlgeschlagen: {e}"


def _upload_audio_proxy_1k(folder: str) -> tuple[bool, str]:
    src_audio = _find_local_audio_file(folder)
    if src_audio is None:
        return False, "Keine lokale Audiodatei gefunden."
    tmp_out = Path(tempfile.gettempdir()) / f"{folder}_{AUDIO_PROXY_NAME}"
    ok_build, msg_build = _build_audio_proxy_wav(src_audio, tmp_out)
    if not ok_build:
        return False, msg_build
    pfx = st.session_state.r2_prefix.strip("/")
    key = f"{pfx}/captures/{folder}/{AUDIO_PROXY_NAME}" if pfx else f"captures/{folder}/{AUDIO_PROXY_NAME}"
    ok_up, msg_up = st.session_state.r2_client.upload_file(str(tmp_out), key)
    try:
        if tmp_out.exists():
            tmp_out.unlink()
    except Exception:
        pass
    if not ok_up:
        return False, f"Upload fehlgeschlagen: {msg_up}"
    return True, ""


def _cloud_framepack_prefix(folder: str) -> str:
    pfx = st.session_state.r2_prefix.strip("/")
    return f"{pfx}/captures/{folder}/frames_1fps" if pfx else f"captures/{folder}/frames_1fps"


def _upload_framepack_1fps(src_video: Path, folder: str, progress_cb=None) -> tuple[bool, str, int, str]:
    """
    Extract 1 fps frames and upload as frame-pack:
      captures/<folder>/frames_1fps/000000.jpg
      captures/<folder>/frames_1fps/index.json
    """
    if st.session_state.r2_client is None:
        return False, "Cloud Client nicht verbunden.", 0, ""

    cap = cv2.VideoCapture(str(src_video))
    if not cap.isOpened():
        return False, f"Video kann nicht geoeffnet werden: {src_video.name}", 0, ""

    try:
        fps = float(cap.get(cv2.CAP_PROP_FPS) or 0.0)
        frame_count = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0)
        if fps <= 0:
            fps = 25.0
        duration_s = (frame_count / fps) if frame_count > 0 else 0.0
        seconds = max(1, int(np.ceil(duration_s)))
        prefix = _cloud_framepack_prefix(folder)
        uploaded = 0
        entries = []

        for sec in range(seconds):
            cap.set(cv2.CAP_PROP_POS_MSEC, float(sec) * 1000.0)
            ok, frame = cap.read()
            if not ok or frame is None:
                continue

            # Optional downscale (biggest size lever) while keeping aspect ratio.
            if FRAMEPACK_MAX_WIDTH and FRAMEPACK_MAX_WIDTH > 0:
                h0, w0 = frame.shape[:2]
                if w0 > FRAMEPACK_MAX_WIDTH:
                    new_w = int(FRAMEPACK_MAX_WIDTH)
                    new_h = max(1, int(round(h0 * (new_w / float(w0)))))
                    frame = cv2.resize(frame, (new_w, new_h), interpolation=cv2.INTER_AREA)

            ok_enc, use_buf = cv2.imencode(
                ".jpg",
                frame,
                [int(cv2.IMWRITE_JPEG_QUALITY), int(FRAMEPACK_JPEG_QUALITY)],
            )
            if not ok_enc:
                continue
            use_size = len(use_buf)
            use_q = int(FRAMEPACK_JPEG_QUALITY)

            fname = f"{sec:06d}.jpg"
            key = f"{prefix}/{fname}"
            ok_up, msg_up = st.session_state.r2_client.upload_bytes(
                use_buf.tobytes(), key, content_type="image/jpeg"
            )
            if not ok_up:
                return False, f"Upload fehlgeschlagen ({fname}): {msg_up}", uploaded, ""
            uploaded += 1
            entries.append({"sec": sec, "file": fname, "bytes": int(use_size), "quality": int(use_q)})
            if progress_cb:
                progress_cb((sec + 1) / seconds, f"Frames: {sec + 1}/{seconds}")

        index_payload = {
            "type": "frame_pack",
            "sample_rate_hz": 1.0,
            "source_video": src_video.name,
            "source_fps": fps,
            "duration_s": duration_s,
            "frame_count": uploaded,
            "frames": entries,
            "created_at": datetime.utcnow().isoformat() + "Z",
        }
        ok_idx, msg_idx = st.session_state.r2_client.upload_string(
            json.dumps(index_payload, ensure_ascii=False, indent=2),
            f"{prefix}/index.json",
        )
        if not ok_idx:
            return False, f"index.json Upload fehlgeschlagen: {msg_idx}", uploaded, ""

        audio_note = ""
        if AUDIO_PROXY_ENABLED:
            ok_a, msg_a = _upload_audio_proxy_1k(folder)
            if ok_a:
                audio_note = " + AudioProxy"
            else:
                audio_note = f" | AudioProxy: {msg_a}"
        return True, "", uploaded, audio_note
    except Exception as e:
        return False, str(e), 0, ""
    finally:
        cap.release()


def _start_sync_run(selected_folders: list[str]):
    queue_rows = st.session_state.sync_queue_rows or []
    overview_rows = st.session_state.sync_overview_rows or []
    if selected_folders:
        selected_set = set(selected_folders)
        # Explicit user selection should be honored independent of current status.
        run_queue = [r for r in overview_rows if str(r.get("capture_folder", "")) in selected_set]
    else:
        run_queue = list(queue_rows)
    st.session_state.sync_run_queue = run_queue
    st.session_state.sync_run_total = len(run_queue)
    st.session_state.sync_run_idx = 0
    st.session_state.sync_run_started_ts = time.time()
    st.session_state.sync_run_selected_folders = list(selected_folders or [])
    st.session_state.sync_running = len(run_queue) > 0
    st.session_state.sync_stop_requested = False


def _refresh_sync_overview_live(table_slot=None, progress_slot=None):
    st.session_state.sync_refresh_running = True
    st.session_state.sync_editor_value = None
    st.session_state.sync_selected_folders = []
    folders = sorted(set(_get_local_capture_folders()))
    rows = [
        {
            "auswaehlen": False,
            "capture_folder": f,
            "reduziert_in_cloud": "...",
            "status": "Pruefung...",
            "cloud_framepack_anzahl": 0,
        }
        for f in folders
    ]
    st.session_state.sync_overview_rows = list(rows)
    st.session_state.sync_queue_rows = []
    total = len(folders)

    if table_slot is not None:
        table_slot.dataframe(
            pd.DataFrame(rows)[["capture_folder", "reduziert_in_cloud", "status"]],
            width="stretch",
            hide_index=True,
            height=340,
            column_config={
                "capture_folder": st.column_config.TextColumn("MAT/Folder", width="large"),
                "reduziert_in_cloud": st.column_config.TextColumn("Reduzierte Version in Cloud", width="large"),
                "status": st.column_config.TextColumn("Status", width="medium"),
            },
        )

    prog = None
    if progress_slot is not None and total > 0:
        prog = progress_slot.progress(0.0, text=f"Sync-Uebersicht: 0/{total} geprueft (0%)")

    for idx, folder in enumerate(folders):
        local_ok, _ = _has_local_fullfps_video(folder)
        if local_ok:
            src_video = _find_local_fullfps_video(folder)
            cloud_complete, cloud_count, expected_count, cloud_text = _cloud_reduced_completeness(folder, src_video)
            rows[idx]["reduziert_in_cloud"] = cloud_text
            rows[idx]["status"] = "OK" if cloud_complete else ""
            rows[idx]["cloud_framepack_anzahl"] = int(cloud_count)
            rows[idx]["expected_framepack_anzahl"] = int(expected_count)
        else:
            rows[idx]["reduziert_in_cloud"] = "-"
            rows[idx]["status"] = "Ohne Video"
            rows[idx]["cloud_framepack_anzahl"] = 0
            rows[idx]["expected_framepack_anzahl"] = 0

        st.session_state.sync_overview_rows = list(rows)
        st.session_state.sync_queue_rows = [
            r for r in rows if str(r.get("reduziert_in_cloud", "")).startswith("Nein")
        ]

        # Reduce redraw frequency to avoid table jitter.
        should_redraw = (idx == total - 1) or (idx % 3 == 0)
        if table_slot is not None and should_redraw:
            table_slot.dataframe(
                pd.DataFrame(rows)[["capture_folder", "reduziert_in_cloud", "status"]],
                width="stretch",
                hide_index=True,
                height=340,
                column_config={
                    "capture_folder": st.column_config.TextColumn("MAT/Folder", width="large"),
                    "reduziert_in_cloud": st.column_config.TextColumn("Reduzierte Version in Cloud", width="large"),
                    "status": st.column_config.TextColumn("Status", width="medium"),
                },
            )
        if prog is not None:
            done = idx + 1
            prog.progress(done / total, text=f"Sync-Uebersicht: {done}/{total} geprueft ({int((done/total)*100)}%)")

    final_rows = [r for r in rows if str(r.get("status", "")) != "Ohne Video"]
    st.session_state.sync_overview_rows = final_rows
    st.session_state.sync_queue_rows = [
        r for r in final_rows if str(r.get("reduziert_in_cloud", "")).startswith("Nein")
    ]
    st.session_state.sync_editor_value = pd.DataFrame(final_rows)[
        ["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"]
    ].copy() if final_rows else pd.DataFrame(
        columns=["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"]
    )
    st.session_state.sync_refresh_running = False


def _set_sync_row_status(folder: str, text: str):
    rows = list(st.session_state.sync_overview_rows or [])
    for r in rows:
        if str(r.get("capture_folder", "")) == str(folder):
            r["status"] = text
            break
    st.session_state.sync_overview_rows = rows

    df = st.session_state.get("sync_editor_value")
    if isinstance(df, pd.DataFrame) and (not df.empty):
        if "capture_folder" in df.columns and "status" in df.columns:
            mask = df["capture_folder"].astype(str) == str(folder)
            if mask.any():
                df.loc[mask, "status"] = text
                st.session_state.sync_editor_value = df


def _finish_sync_run(final_msg: str, kind: str):
    st.session_state.sync_running = False
    st.session_state.sync_stop_requested = False
    set_status(final_msg, kind)


def _format_eta_seconds(sec: float) -> str:
    sec = max(0, int(sec))
    mm, ss = divmod(sec, 60)
    hh, mm = divmod(mm, 60)
    if hh > 0:
        return f"{hh:02d}:{mm:02d}:{ss:02d}"
    return f"{mm:02d}:{ss:02d}"


def _has_valid_8_points(points):
    if not isinstance(points, list) or len(points) != 8:
        return False
    for p in points:
        if not isinstance(p, (list, tuple)) or len(p) != 2:
            return False
        try:
            float(p[0]); float(p[1])
        except Exception:
            return False
    return True


def _clamp_roi_to_video(x, y, w, h, vid_w, vid_h):
    return clamp_roi_to_video(x, y, w, h, vid_w, vid_h)


def _get_dynamic_roi_target_width(default_width: int = 620, anchor_id: str = "roi-left-width-probe") -> int:
    # Fallback-first for stability.
    width = int(default_width)
    if streamlit_js_eval is None:
        return width
    try:
        # streamlit_js_eval only re-evaluates when the expression string changes.
        # We install a one-time resize bridge that proactively sends updated widths
        # back to Python, which triggers reruns on browser resize.
        expr = (
            "(function(){"
            "const d = window.parent?.document || document;"
            f"const anchorId = {json.dumps(str(anchor_id))};"
            "const readW = function(){"
            "  const a = d.getElementById(anchorId);"
            "  const col = a?.closest('[data-testid=\"column\"]');"
            "  const p = a?.parentElement;"
            "  const block = d.querySelector('.block-container');"
            "  return Math.round(col?.clientWidth || p?.clientWidth || block?.clientWidth || window.parent?.innerWidth || window.innerWidth || 0);"
            "};"
            "if (!window.__roiResizeBridgeInstalled) {"
            "  window.__roiResizeBridgeInstalled = true;"
            "  let t = null;"
            "  window.addEventListener('resize', function(){"
            "    clearTimeout(t);"
            "    t = setTimeout(function(){"
            "      try { sendDataToPython({value: readW(), dataType: 'json'}); } catch (e) {}"
            "    }, 120);"
            "  });"
            "}"
            "return readW();"
            "})()"
        )
        container_w = streamlit_js_eval(
            js_expressions=expr,
            key="roi_viewport_width_probe",
            want_output=True,
        )
        if isinstance(container_w, (int, float)) and float(container_w) > 300:
            cw = int(round(float(container_w)))
            width = max(260, min(920, cw - 24))
    except Exception:
        # Never break app render because of viewport probing.
        pass
    return width


def _try_auto_connect_once():
    if st.session_state.r2_connected or st.session_state.auto_connect_attempted:
        return

    st.session_state.auto_connect_attempted = True
    acc = st.session_state.r2_account_id.strip()
    key = st.session_state.r2_access_key_id.strip()
    sec = st.session_state.r2_secret_access_key.strip()
    bkt = st.session_state.r2_bucket.strip()
    if not all([acc, key, sec, bkt]):
        return

    ok, msg, client = connect_r2_client(acc, key, sec, bkt)
    if not ok or client is None:
        set_status(f"Auto-Connect fehlgeschlagen: {msg}", "warn")
        return

    st.session_state.r2_connected = True
    st.session_state.r2_client = client
    opts = list_root_prefixes(client)
    st.session_state.r2_prefix_options = opts
    real = [o for o in opts if o]
    if len(real) == 1:
        st.session_state.r2_prefix = real[0]
        st.session_state.fb_path = real[0]
        st.session_state.fb_items = r2_list(real[0])
    else:
        st.session_state.fb_path = ""
        st.session_state.fb_items = r2_list("")
    st.session_state.auto_connect_used = True
    set_status("Auto-Connect erfolgreich.", "ok")

def _try_auto_connect_local_once():
    if st.session_state.local_connected or st.session_state.get("local_auto_connect_attempted"):
        return
    st.session_state.local_auto_connect_attempted = True
    lp_str = str((st.secrets.get("local") or {}).get("default_path") or "").strip()
    if not lp_str:
        return
    try:
        lp = Path(lp_str).expanduser().resolve()
        if not lp.exists() or not lp.is_dir():
            return
        if not (lp / "captures").exists():
            return
        local_client = LocalStorageAdapter(str(lp))
        ok_local, _ = local_client.test_connection()
        if ok_local:
            st.session_state.local_connected = True
            st.session_state.local_client = local_client
            st.session_state.local_base_path = str(lp)
            st.session_state.local_root = ""
    except Exception:
        pass


def _file_icon(name):
    ext = Path(name).suffix.lower()
    return {
        ".mp4": "[VID]", ".mov": "[VID]", ".avi": "[VID]", ".mkv": "[VID]",
        ".mat": "[MAT]", ".json": "[JSON]", ".wav": "[AUD]", ".mp3": "[AUD]",
        ".png": "[IMG]", ".jpg": "[IMG]", ".jpeg": "[IMG]",
        ".txt": "[TXT]", ".md": "[TXT]",
    }.get(ext, "[FILE]")

def draw_rois(frame, rois, sel, vid_w, vid_h, skip_idx=None):
    img = frame.copy()
    dh, dw = img.shape[:2]
    sx = dw/vid_w if vid_w else 1.0
    sy = dh/vid_h if vid_h else 1.0
    for i, r in enumerate(rois):
        if skip_idx is not None and i == skip_idx:
            continue
        x,y,w,h = int(r["x"]*sx),int(r["y"]*sy),int(r["w"]*sx),int(r["h"]*sy)
        is_track = r["name"]=="track_minimap"
        color = (40, 220, 180) if is_track else ((255, 225, 40) if i == sel else (255, 80, 80))
        thick = 3 if i == sel else 2
        cv2.rectangle(img,(x,y),(x+w,y+h),color,thick)
        cv2.putText(img,r["name"],(x+3,y+14),cv2.FONT_HERSHEY_SIMPLEX,.44,color,1,cv2.LINE_AA)
    return img


@st.cache_resource(show_spinner=False)
def _opencv_gui_available():
    try:
        info = cv2.getBuildInformation()
    except Exception:
        return False, "OpenCV Build-Info nicht verfuegbar."
    # OpenCV reports GUI backend in this section. Headless builds usually show NONE.
    gui_lines = [ln.strip() for ln in info.splitlines() if ln.strip().startswith("GUI:")]
    if not gui_lines:
        return False, "OpenCV GUI-Backend unbekannt."
    gui_line = gui_lines[0]
    if "NONE" in gui_line.upper():
        return False, (
            "OpenCV ist als headless installiert (ohne GUI). "
            "Bitte in der venv umstellen: "
            "`pip uninstall -y opencv-python-headless && pip install opencv-python`"
        )
    return True, gui_line


def _pick_roi_with_cv_window(frame_rgb):
    gui_ok, gui_msg = _opencv_gui_available()
    if not gui_ok:
        return False, gui_msg, None
    try:
        view = cv2.cvtColor(frame_rgb, cv2.COLOR_RGB2BGR)
        cv2.namedWindow("ROI Auswahl", cv2.WINDOW_NORMAL)
        x, y, w, h = cv2.selectROI("ROI Auswahl", view, showCrosshair=True, fromCenter=False)
        cv2.destroyWindow("ROI Auswahl")
    except Exception as e:
        return False, f"ROI-Fenster konnte nicht geoeffnet werden: {e}", None
    if int(w) < 1 or int(h) < 1:
        return False, "Keine ROI ausgewaehlt.", None
    return True, "", {"x": int(x), "y": int(y), "w": int(w), "h": int(h)}

def _sanitize_rois(rois):
    out = []
    for r in rois or []:
        if not isinstance(r, dict):
            continue
        nr = dict(r)
        nr["fmt"] = _normalize_roi_format(nr.get("fmt", "any"))
        nr.pop("pattern", None)
        nr["max_scale"] = float(st.session_state.get("roi_global_scale", 1.2))
        out.append(nr)
    return out

def build_result_json():
    return build_result_payload(
        t_start=st.session_state.t_start,
        t_end=st.session_state.t_end,
        rois=st.session_state.rois,
        video={
            "width": st.session_state.vid_width,
            "height": st.session_state.vid_height,
            "fps": st.session_state.vid_fps,
            "duration": st.session_state.vid_duration,
        },
        track={
            "ref_pts": st.session_state.ref_track_pts,
            "minimap_pts": st.session_state.minimap_pts,
            "moving_pt_color_range": st.session_state.moving_pt_color_range,
        },
    )

def build_mat_struct(result):
    return backend_build_mat_struct(result, video_name=st.session_state.video_name)


def _mat_struct_to_plain(obj):
    """Convert scipy/h5 decoded MATLAB structs to savemat-friendly plain dicts."""
    obj = _mat_scalar(obj)
    if obj is None:
        return {}
    if isinstance(obj, dict):
        return {str(k): _mat_struct_to_plain(v) for k, v in obj.items() if not str(k).startswith("#")}
    fields = getattr(obj, "_fieldnames", None)
    if fields:
        return {str(k): _mat_struct_to_plain(getattr(obj, k)) for k in fields}
    if isinstance(obj, np.ndarray):
        if obj.dtype == object:
            return np.array([_mat_struct_to_plain(v) for v in obj.ravel().tolist()], dtype=object).reshape(obj.shape)
        return obj
    if isinstance(obj, (np.generic,)):
        return obj.item()
    return obj


def _load_recordresult_template_fields(mat_path: str) -> dict:
    """Load only reusable recordResult.metadata from an existing MAT file.

    OCR/audio/validation are intentionally not copied: ROI Setup creates a new
    OCRExtractor-compatible OCR parameter block, while audio_rpm and validation
    are produced later by spectrogram analysis and validation.
    """
    out = {}
    if not mat_path:
        return out
    try:
        data = sio.loadmat(mat_path, squeeze_me=True, struct_as_record=False)
        rr = _mat_scalar(data.get("recordResult"))
        if rr is None:
            return out
        meta = _mat_obj_get(rr, "metadata")
        if meta is not None:
            out["metadata"] = _mat_struct_to_plain(meta)
        return out
    except NotImplementedError:
        pass
    except Exception:
        return out

    try:
        import h5py
        with h5py.File(mat_path, "r") as f:
            rr = _h5_get_path_ci(f, ["recordResult"])
            if rr is None:
                return out
            obj = _h5_get_path_ci(rr, ["metadata"])
            if obj is not None:
                val = _h5_decode_value(obj)
                if val is not None:
                    out["metadata"] = _mat_struct_to_plain(val)
    except Exception:
        pass
    return out


def _download_template_mat_for_save() -> str:
    key = str(st.session_state.get("mat_selected_key") or st.session_state.get("mat_pending_selected_key") or "").strip()
    if key and st.session_state.get("r2_connected") and st.session_state.get("r2_client") is not None:
        tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".mat")
        tmp.close()
        ok, _msg = st.session_state.r2_client.download_file(key, tmp.name)
        if ok:
            return tmp.name
        try:
            Path(tmp.name).unlink(missing_ok=True)
        except Exception:
            pass
    # Developer/project-folder fallback for local testing.
    for candidate in (Path.cwd() / "results_20251117_020622.mat", Path("/mnt/data/results_20251117_020622.mat")):
        try:
            if candidate.exists():
                return str(candidate)
        except Exception:
            pass
    return ""



def _matlab_cellstr(values):
    return np.array([str(v) for v in (values or [])], dtype=object).reshape((-1, 1))


def _matlab_datetime_object(dt=None):
    """Best-effort MATLAB datetime object for MAT export.

    scipy cannot write native MCOS datetime/table objects exactly like MATLAB v7.3,
    but writing them as MATLAB objects keeps the intended class instead of a plain struct.
    """
    dt = dt or datetime.now()
    if MatlabObject is None:
        return np.array([dt.year, dt.month, dt.day, dt.hour, dt.minute, dt.second], dtype=float)
    arr = np.empty((1, 1), dtype=[
        ("year", object), ("month", object), ("day", object),
        ("hour", object), ("minute", object), ("second", object),
    ])
    arr[0, 0] = (
        np.array([[dt.year]], dtype=float),
        np.array([[dt.month]], dtype=float),
        np.array([[dt.day]], dtype=float),
        np.array([[dt.hour]], dtype=float),
        np.array([[dt.minute]], dtype=float),
        np.array([[dt.second + dt.microsecond / 1_000_000.0]], dtype=float),
    )
    return MatlabObject(arr, classname="datetime")


def _build_roi_table_for_matlab(rois=None):
    """Build recordResult.ocr.roi_table / roi_table_raw as a MATLAB table object.

    Columns follow OCRExtractor.m naming: name_roi, roi, fmt, max_scale.
    """
    rows = []
    for r in _sanitize_rois(rois if rois is not None else st.session_state.get("rois", [])):
        rows.append({
            "name_roi": str(r.get("name", "_") or "_"),
            "roi": np.array([
                float(r.get("x", 0.0) or 0.0),
                float(r.get("y", 0.0) or 0.0),
                float(r.get("w", 0.0) or 0.0),
                float(r.get("h", 0.0) or 0.0),
            ], dtype=float).reshape((1, 4)),
            "fmt": _normalize_roi_format(r.get("fmt", "any")),
            "max_scale": float(r.get("max_scale", st.session_state.get("roi_global_scale", 1.2)) or 1.2),
        })
    n = len(rows)
    arr = np.empty((n, 1), dtype=[
        ("name_roi", object),
        ("roi", object),
        ("fmt", object),
        ("max_scale", object),
    ])
    for i, row in enumerate(rows):
        arr[i, 0] = (
            row["name_roi"],
            row["roi"],
            row["fmt"],
            np.array([[row["max_scale"]]], dtype=float),
        )
    if MatlabObject is None:
        return arr
    return MatlabObject(arr, classname="table")


def _mat_export_to_jsonable(obj):
    """Recursively convert the MAT export payload into strict JSON values."""
    if MatlabObject is not None and isinstance(obj, MatlabObject):
        return _mat_export_to_jsonable(np.asarray(obj))
    obj = _mat_scalar(obj)
    if MatlabObject is not None and isinstance(obj, MatlabObject):
        return _mat_export_to_jsonable(np.asarray(obj))
    if isinstance(obj, dict):
        return {str(k): _mat_export_to_jsonable(v) for k, v in obj.items()}
    if isinstance(obj, np.void):
        if obj.dtype.names:
            return {name: _mat_export_to_jsonable(obj[name]) for name in obj.dtype.names}
        return _mat_export_to_jsonable(obj.tolist())
    if isinstance(obj, np.ndarray):
        if obj.dtype.names:
            return [_mat_export_to_jsonable(item) for item in obj.reshape(-1)]
        return [_mat_export_to_jsonable(v) for v in obj.reshape(-1).tolist()]
    if isinstance(obj, (list, tuple, set)):
        return [_mat_export_to_jsonable(v) for v in obj]
    if isinstance(obj, np.generic):
        return _mat_export_to_jsonable(obj.item())
    if isinstance(obj, (datetime, pd.Timestamp)):
        return obj.isoformat()
    if isinstance(obj, (bytes, bytearray)):
        return obj.decode("utf-8", errors="ignore")
    return obj

def _ensure_ocr_extractor_ocr_struct(rr: dict) -> dict:
    """Normalize recordResult.ocr for the MATLAB OCRExtractor workflow."""
    ocr = rr.get("ocr", {}) if isinstance(rr.get("ocr", {}), dict) else {}
    params = ocr.get("params", {}) if isinstance(ocr.get("params", {}), dict) else {}
    params["start_s"] = float(st.session_state.get("t_start", 0.0) or 0.0)
    params["end_s"] = float(st.session_state.get("t_end", 0.0) or 0.0)
    ocr["params"] = params

    # OCRExtractor.m expects table-like ROI parameter blocks. Build both the
    # effective and raw table from the current ROI editor state; do not leave
    # scipy's default nested struct representation here.
    roi_table = _build_roi_table_for_matlab(st.session_state.get("rois", []))
    ocr["roi_table"] = roi_table
    ocr["roi_table_raw"] = roi_table
    ocr["created"] = _matlab_datetime_object(datetime.now())

    # Match OCRExtractor.m catalog names exactly.
    ocr["roi_catalog"] = {
        "roiNames": _matlab_cellstr(ROI_NAMES),
        "fmtOptions": _matlab_cellstr(FMT_OPTIONS),
    }

    # Track calibration belongs to OCR because OCRExtractor.m uses
    # recordResult.ocr.trkCalSlim for the minimap/track ROI. If backend did not
    # already create it, build a minimal OCRExtractor-compatible struct.
    if "trkCalSlim" not in ocr:
        track_roi = next((r for r in st.session_state.get("rois", []) if str(r.get("name", "")) == "track_minimap"), None)
        if track_roi is not None:
            marker = dict(st.session_state.get("moving_pt_color_range", {}) or {})
            ocr["trkCalSlim"] = {
                "roi": np.array([
                    float(track_roi.get("x", 0.0) or 0.0),
                    float(track_roi.get("y", 0.0) or 0.0),
                    float(track_roi.get("w", 0.0) or 0.0),
                    float(track_roi.get("h", 0.0) or 0.0),
                ], dtype=float),
                "ptsMini": np.array(st.session_state.get("minimap_pts") or [], dtype=float),
                "marker": marker,
            }
    rr["ocr"] = ocr
    return rr


def _merge_recordresult_template(mat_struct: dict, template_fields: dict) -> dict:
    out = dict(mat_struct or {})
    rr = _mat_struct_to_plain(out.get("recordResult", {}))
    if not isinstance(rr, dict):
        rr = {"data": rr}

    # Only metadata is inherited from the original MAT file; it may be extended
    # below with current Streamlit/video information. Do not copy OCR results,
    # audio_rpm, or validation from another processing stage.
    if isinstance(template_fields.get("metadata"), dict):
        inherited_meta = dict(template_fields["metadata"])
        current_meta = rr.get("metadata", {}) if isinstance(rr.get("metadata", {}), dict) else {}
        inherited_meta.update(current_meta)
        rr["metadata"] = inherited_meta
    else:
        rr.setdefault("metadata", {})

    rr = _ensure_ocr_extractor_ocr_struct(rr)

    # audio_rpm and validation are created by later processing steps. Do not
    # write empty placeholders during ROI setup.
    rr.pop("audio_rpm", None)
    rr.pop("validation", None)

    out["recordResult"] = rr
    return out


def _build_save_mat_struct(result):
    mat_struct = build_mat_struct(result)
    template_path = _download_template_mat_for_save()
    try:
        template_fields = _load_recordresult_template_fields(template_path) if template_path else {}
        return _merge_recordresult_template(mat_struct, template_fields)
    finally:
        try:
            if template_path and str(template_path).startswith(tempfile.gettempdir()):
                Path(template_path).unlink(missing_ok=True)
        except Exception:
            pass


def _upload_bytes_compat(client, key: str, data: bytes, content_type: str) -> tuple[bool, str]:
    try:
        if hasattr(client, "upload_bytes"):
            return client.upload_bytes(data, key, content_type=content_type)
        tmp = tempfile.NamedTemporaryFile(delete=False)
        tmp.write(data)
        tmp.close()
        try:
            return client.upload_file(tmp.name, key)
        finally:
            try:
                Path(tmp.name).unlink(missing_ok=True)
            except Exception:
                pass
    except Exception as e:
        return False, str(e)


def _save_result_json_and_mat() -> tuple[bool, str, dict]:
    cf = st.session_state.capture_folder or Path(str(st.session_state.video_name or "output")).stem or "output"
    safe_cf = "".join(ch if ch.isalnum() or ch in "._-" else "_" for ch in str(cf)).strip("._") or "output"
    result = build_result_json()
    mat_struct_for_save = _build_save_mat_struct(result)
    json_payload = _mat_export_to_jsonable(mat_struct_for_save)
    json_bytes = json.dumps(json_payload, indent=2, ensure_ascii=False, default=lambda o: _mat_export_to_jsonable(o)).encode("utf-8")
    mat_buf = io.BytesIO()
    sio.savemat(mat_buf, mat_struct_for_save, do_compression=True)
    mat_bytes = mat_buf.getvalue()
    json_name = f"results_{safe_cf}.json"
    mat_name = f"results_{safe_cf}.mat"
    saved_targets = []
    try:
        out_dir = Path.cwd() / "results"
        out_dir.mkdir(parents=True, exist_ok=True)
        (out_dir / json_name).write_bytes(json_bytes)
        (out_dir / mat_name).write_bytes(mat_bytes)
        saved_targets.append(str(out_dir))
    except Exception as e:
        saved_targets.append(f"lokal fehlgeschlagen: {e}")
    saved_mat_key = ""
    if st.session_state.get("r2_connected") and st.session_state.get("r2_client") is not None:
        # 1) zentrale MAT/JSON-Ablage fuer MAT selection
        res_dir = _results_dir_key().strip("/")
        json_key = f"{res_dir}/{json_name}" if res_dir else json_name
        mat_key = f"{res_dir}/{mat_name}" if res_dir else mat_name
        ok_json, msg_json = _upload_bytes_compat(st.session_state.r2_client, json_key, json_bytes, "application/json")
        ok_mat, msg_mat = _upload_bytes_compat(st.session_state.r2_client, mat_key, mat_bytes, "application/octet-stream")
        if ok_json and ok_mat:
            saved_mat_key = mat_key
            saved_targets.append(f"R2: {res_dir or '/'}")
            try:
                st.session_state.mat_summary_cache.pop(mat_key, None)
            except Exception:
                pass
        else:
            saved_targets.append(f"R2 results fehlgeschlagen: JSON={msg_json or ok_json}, MAT={msg_mat or ok_mat}")

        # 2) passende Capture-Cloud-Ablage neben audio_proxy/frames_1fps
        cap_dir = _capture_results_dir_key(str(cf)).strip("/")
        if cap_dir and cap_dir != res_dir:
            cap_json_key = f"{cap_dir}/{json_name}"
            cap_mat_key = f"{cap_dir}/{mat_name}"
            ok_cj, msg_cj = _upload_bytes_compat(st.session_state.r2_client, cap_json_key, json_bytes, "application/json")
            ok_cm, msg_cm = _upload_bytes_compat(st.session_state.r2_client, cap_mat_key, mat_bytes, "application/octet-stream")
            if ok_cj and ok_cm:
                saved_targets.append(f"R2: {cap_dir}")
            else:
                saved_targets.append(f"R2 capture fehlgeschlagen: JSON={msg_cj or ok_cj}, MAT={msg_cm or ok_cm}")

        if saved_mat_key:
            _invalidate_and_update_mat_selection_for_capture(str(cf), saved_mat_key)
    payload = dict(json_name=json_name, mat_name=mat_name, json_bytes=json_bytes, mat_bytes=mat_bytes, targets=saved_targets, mat_key=saved_mat_key)
    return True, f"Gespeichert: {json_name} + {mat_name}", payload


def load_json_config(data):
    cfg = config_from_json_payload(data, vid_duration=st.session_state.vid_duration)
    st.session_state.t_start = cfg.get("t_start", st.session_state.t_start)
    st.session_state.t_end = cfg.get("t_end", st.session_state.t_end)
    st.session_state.rois = _sanitize_rois(cfg.get("rois", []))
    if cfg.get("ref_track_pts"):
        st.session_state.ref_track_pts = cfg["ref_track_pts"]
    if cfg.get("minimap_pts"):
        st.session_state.minimap_pts = cfg["minimap_pts"]
    if cfg.get("moving_pt_color_range"):
        st.session_state.moving_pt_color_range = cfg["moving_pt_color_range"]

def _apply_video(local_path, display_name):
    info = get_video_info(local_path)
    st.session_state.update(
        video_path=local_path, video_name=display_name,
        media_source="video",
        framepack_remote_prefix="",
        framepack_files=[],
        framepack_cache={},
        vid_fps=info["fps"], vid_width=info["width"],
        vid_height=info["height"], vid_duration=info["duration"],
        t_start=0.0, t_end=info["duration"], t_current=0.0, rois=[],
        selected_roi=None, drag_roi={}, roi_draw_armed=False,
        roi_wait_user_move=False, roi_anchor_box={}, roi_reject_anchor_events=0,
        roi_editor_df=None)
    if not st.session_state.capture_folder:
        st.session_state.capture_folder = Path(display_name).stem
    get_frame.clear(); get_video_info.clear()
    set_status(f"Video geladen: {display_name}", "ok")


def _has_media_source() -> bool:
    src = str(st.session_state.media_source or "none")
    if src == "video":
        return bool(st.session_state.video_path)
    if src == "framepack":
        return bool(st.session_state.framepack_remote_prefix) and bool(st.session_state.framepack_files)
    return False


def _load_framepack_from_r2(capture_folder: str) -> bool:
    if not capture_folder or st.session_state.r2_client is None:
        return False
    pfx = st.session_state.r2_prefix.strip("/")
    remote_prefix = f"{pfx}/captures/{capture_folder}/frames_1fps" if pfx else f"captures/{capture_folder}/frames_1fps"
    ok, items = st.session_state.r2_client.list_files(remote_prefix)
    if not ok or not isinstance(items, list):
        return False

    frame_files = sorted([n for n in items if n.lower().endswith((".jpg", ".jpeg", ".png", ".webp"))])
    if not frame_files:
        return False

    first_key = f"{remote_prefix}/{frame_files[0]}"
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=Path(frame_files[0]).suffix or ".jpg")
    tmp.close()
    ok_dl, msg_dl = st.session_state.r2_client.download_file(first_key, tmp.name)
    if not ok_dl:
        set_status(f"Frame-Pack Download: {msg_dl}", "warn")
        return False
    try:
        img = np.array(Image.open(tmp.name).convert("RGB"))
    except Exception as e:
        set_status(f"Frame-Pack Parse: {e}", "warn")
        return False
    finally:
        try:
            Path(tmp.name).unlink(missing_ok=True)
        except Exception:
            pass

    h, w = img.shape[:2]
    fps = 1.0
    duration = max(1.0, float(len(frame_files)))
    st.session_state.update(
        video_path=None,
        video_name=f"{capture_folder} [frames_1fps]",
        media_source="framepack",
        framepack_remote_prefix=remote_prefix,
        framepack_files=frame_files,
        framepack_cache={0: img},
        vid_fps=fps,
        vid_width=w,
        vid_height=h,
        vid_duration=duration,
        t_start=0.0,
        t_end=duration,
        t_current=0.0,
        rois=[],
        selected_roi=None,
        drag_roi={},
        roi_draw_armed=False,
        roi_wait_user_move=False,
        roi_anchor_box={},
        roi_reject_anchor_events=0,
        roi_editor_df=None,
    )
    set_status(f"Frame-Pack geladen: {capture_folder} ({len(frame_files)} Frames)", "ok")
    return True


def _get_media_frame(time_s: float):
    src = str(st.session_state.media_source or "none")
    if src == "video" and st.session_state.video_path:
        return get_frame(st.session_state.video_path, time_s)
    if src != "framepack":
        return None
    files = st.session_state.framepack_files or []
    if not files:
        return None
    idx = int(max(0, min(len(files) - 1, np.floor(float(time_s)))))
    cache = st.session_state.framepack_cache or {}
    if idx in cache:
        return cache[idx]
    remote_prefix = st.session_state.framepack_remote_prefix
    key = f"{remote_prefix}/{files[idx]}"
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=Path(files[idx]).suffix or ".jpg")
    tmp.close()
    ok, msg = st.session_state.r2_client.download_file(key, tmp.name)
    if not ok:
        set_status(f"Frame-Pack Download: {msg}", "warn")
        return None
    try:
        frame = np.array(Image.open(tmp.name).convert("RGB"))
    except Exception as e:
        set_status(f"Frame-Pack Parse: {e}", "warn")
        return None
    finally:
        try:
            Path(tmp.name).unlink(missing_ok=True)
        except Exception:
            pass
    cache[idx] = frame
    if len(cache) > 20:
        oldest = sorted(cache.keys())[:-20]
        for k in oldest:
            cache.pop(k, None)
    st.session_state.framepack_cache = cache
    return frame

def r2_list(prefix):
    """Lists objects under prefix. Returns [{"name", "path", "is_dir"}]."""
    if not st.session_state.r2_connected: return []
    client = st.session_state.r2_client
    ok, items = client.list_files(prefix)
    if not ok or not isinstance(items, list): return []
    result = []
    base = prefix.rstrip("/")
    for item in items:
        is_dir = item.endswith("/")
        name   = item.rstrip("/")
        if not name: continue
        full_path = (base + "/" + name) if base else name
        result.append({"name": name, "path": full_path, "is_dir": is_dir})
    result.sort(key=lambda x: (not x["is_dir"], x["name"].lower()))
    return result

def get_root_prefixes():
    if not st.session_state.r2_connected: return [""]
    return list_root_prefixes(st.session_state.r2_client)


def _results_dir_key() -> str:
    pfx = st.session_state.r2_prefix.strip("/")
    return f"{pfx}/results" if pfx else "results"


def _refresh_mat_files():
    if not st.session_state.r2_connected or st.session_state.r2_client is None:
        st.session_state.mat_files = []
        st.session_state.mat_targets = []
        return
    client = st.session_state.r2_client
    pfx = st.session_state.r2_prefix.strip("/")
    res_key = _results_dir_key()
    cap_root = f"{pfx}/captures" if pfx else "captures"

    ok_res, res_items = client.list_files(res_key)
    ok_cap, cap_items = client.list_files(cap_root)
    if not ok_res or not isinstance(res_items, list):
        res_items = []
    if not ok_cap or not isinstance(cap_items, list):
        cap_items = []

    mats = []
    for name in res_items:
        if not name.endswith("/"):
            full_key = f"{res_key}/{name}" if res_key else name
            if full_key.lower().endswith(".mat"):
                mats.append(full_key.strip("/"))
    mats.sort(reverse=True)
    st.session_state.mat_files = mats

    folders = sorted([n.rstrip("/") for n in cap_items if n.endswith("/")], reverse=True)
    mats_set = set(mats)
    mat_by_folder = {}
    for mk in mats:
        g = _mat_capture_guess_from_key(mk)
        if g and g not in mat_by_folder:
            mat_by_folder[g] = mk

    targets = []
    used_mats = set()
    for folder in folders:
        expected = f"{res_key}/results_{folder}.mat" if res_key else f"results_{folder}.mat"
        mat_key = expected if expected in mats_set else mat_by_folder.get(folder, "")
        if mat_key:
            used_mats.add(mat_key)
        targets.append({"kind": "folder", "folder": folder, "mat_key": mat_key})

    for mk in mats:
        if mk not in used_mats:
            targets.append({"kind": "mat_only", "folder": _mat_capture_guess_from_key(mk), "mat_key": mk})
    st.session_state.mat_targets = targets

    # Invalidate summary cache when file set changes.
    cache = st.session_state.get("mat_summary_cache") or {}
    st.session_state.mat_summary_cache = {k: v for k, v in cache.items() if k in mats}
    valid_mat_keys = [t.get("mat_key", "") for t in targets if t.get("mat_key")]
    if st.session_state.mat_selected_key not in valid_mat_keys:
        st.session_state.mat_selected_key = valid_mat_keys[0] if valid_mat_keys else ""
        st.session_state.mat_selected_summary = None
    if st.session_state.mat_pending_selected_key not in valid_mat_keys:
        st.session_state.mat_pending_selected_key = st.session_state.mat_selected_key
    st.session_state.mat_scan_prefix = st.session_state.r2_prefix


def _mat_capture_guess_from_key(remote_key: str) -> str:
    filename = Path(remote_key).name
    if filename.startswith("results_") and filename.lower().endswith(".mat"):
        return filename[len("results_"):-4]
    return ""


def _download_mat_to_temp(remote_key: str):
    client = st.session_state.r2_client
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".mat")
    tmp.close()
    ok, msg = client.download_file(remote_key, tmp.name)
    if not ok:
        return False, msg, None
    return True, "", tmp.name


def _mat_obj_get(obj, field: str, default=None):
    if obj is None:
        return default
    if isinstance(obj, dict):
        return obj.get(field, default)
    if hasattr(obj, field):
        return getattr(obj, field)
    try:
        if isinstance(obj, np.ndarray) and obj.dtype.names and field in obj.dtype.names:
            return _mat_scalar(obj[field])
    except Exception:
        pass
    return default


def _mat_scalar(x):
    try:
        while isinstance(x, np.ndarray) and x.size == 1:
            x = x.item()
    except Exception:
        pass
    return x


def _mat_to_text(x, default: str = "") -> str:
    """Robust MATLAB value -> Python text, including char arrays and categorical-like values."""
    if x is None:
        return default
    x = _mat_scalar(x)
    try:
        if isinstance(x, bytes):
            return x.decode("utf-8", errors="ignore").strip()
        if isinstance(x, str):
            return x.strip()
        if isinstance(x, np.ndarray):
            if x.size == 0:
                return default
            if x.dtype.kind in ("U", "S"):
                vals = np.asarray(x).ravel().tolist()
                if all(len(str(v)) == 1 for v in vals):
                    return "".join(str(v) for v in vals).strip()
                return str(vals[0]).strip()
            if x.dtype == object:
                vals = np.asarray(x).ravel().tolist()
                if vals:
                    return _mat_to_text(vals[0], default)
        for fld in ("codes", "categoryNames", "categories", "data"):
            val = _mat_obj_get(x, fld)
            if val is not None and val is not x:
                txt = _mat_to_text(val, "")
                if txt:
                    return txt
        return str(x).strip()
    except Exception:
        return default


def _normalize_roi_format(fmt) -> str:
    txt = _mat_to_text(fmt, "any")
    if not txt or txt == "<undefined>":
        txt = "any"
    txt = str(txt).strip().strip("'\"")
    low = txt.lower().replace(" ", "_").replace("-", "_")
    aliases = {
        "double": "float", "single": "float", "numeric": "float", "number": "float",
        "decimal": "float", "float64": "float", "float32": "float",
        "int": "integer", "uint": "integer", "uint8": "integer", "uint16": "integer",
        "uint32": "integer", "int8": "integer", "int16": "integer", "int32": "integer",
        "text": "alnum", "string": "alnum", "char": "alnum",
        "time": "time_m:ss.SSS", "duration": "time_m:ss.SSS",
    }
    if low in aliases:
        txt = aliases[low]
    elif low in {o.lower(): o for o in FMT_OPTIONS}:
        txt = {o.lower(): o for o in FMT_OPTIONS}[low]
    if txt == "custom" or txt not in FMT_OPTIONS:
        return "any"
    return txt


def _mat_text_list(x) -> list[str]:
    """MATLAB char/cell/string/categorical labels -> list[str]."""
    if x is None:
        return []
    x = _mat_scalar(x)
    try:
        arr = np.asarray(x)
        if arr.size == 0:
            return []
        if arr.dtype.kind in ("U", "S"):
            if arr.ndim == 2:
                return ["".join(str(c) for c in row).strip() for row in arr]
            return [str(v).strip() for v in arr.ravel().tolist()]
        return [_mat_to_text(v, "") for v in arr.ravel().tolist()]
    except Exception:
        txt = _mat_to_text(x, "")
        return [txt] if txt else []


def _mat_categorical_column_values(col, n: int) -> list | None:
    """Decode MATLAB categorical saved in v7 MAT as codes + categoryNames/categories."""
    if col is None:
        return None
    obj = _mat_scalar(col)
    codes = _mat_obj_get(obj, "codes")
    cats = (_mat_obj_get(obj, "categoryNames") or
            _mat_obj_get(obj, "categories") or
            _mat_obj_get(obj, "category_names"))
    if codes is None or cats is None:
        return None
    labels = _mat_text_list(cats)
    if not labels:
        return None
    try:
        code_arr = np.asarray(_mat_scalar(codes)).ravel()
    except Exception:
        return None
    vals = []
    for c in code_arr[:n]:
        try:
            if not np.isfinite(float(c)):
                vals.append(""); continue
            idx = int(c) - 1
            vals.append(labels[idx] if 0 <= idx < len(labels) else "")
        except Exception:
            vals.append("")
    if len(vals) < n:
        vals += [""] * (n - len(vals))
    return vals[:n]


def _mat_column_values(col, n: int) -> list:
    if col is None:
        return [None] * n
    cat_vals = _mat_categorical_column_values(col, n)
    if cat_vals is not None:
        return cat_vals
    col = _mat_scalar(col)
    try:
        arr = np.asarray(col)
        if arr.ndim == 0:
            vals = [arr.item()]
        elif arr.dtype.kind in ("U", "S") and arr.ndim == 2:
            vals = ["".join(str(c) for c in row).strip() for row in arr]
        else:
            vals = arr.ravel().tolist()
    except Exception:
        vals = [col]
    if len(vals) < n:
        vals = vals + [None] * (n - len(vals))
    return vals[:n]


def _parse_roi_value(v):
    if v is None:
        return None
    try:
        arr = np.asarray(_mat_scalar(v), dtype=float).ravel()
        arr = arr[np.isfinite(arr)]
        if arr.size >= 4:
            return [float(arr[0]), float(arr[1]), float(arr[2]), float(arr[3])]
    except Exception:
        pass
    txt = _mat_to_text(v, "")
    if not txt:
        return None
    try:
        nums = [float(x) for x in txt.replace(",", " ").replace(";", " ").split()]
        if len(nums) >= 4:
            return nums[:4]
    except Exception:
        return None
    return None




def _h5_decode_value(v, f=None, _depth: int = 0):
    """Best-effort reader for MATLAB v7.3 values, including refs and char arrays."""
    if _depth > 6:
        return None
    try:
        import h5py
        if isinstance(v, h5py.Reference):
            if not v or f is None:
                return None
            return _h5_decode_value(f[v], f, _depth + 1)
        if isinstance(v, h5py.Dataset):
            data = v[()]
            return _h5_decode_value(data, v.file, _depth + 1)
        if isinstance(v, h5py.Group):
            out = {}
            for k in v.keys():
                out[str(k)] = _h5_decode_value(v[k], v.file, _depth + 1)
            return out
    except Exception:
        pass
    try:
        arr = np.asarray(v)
        if arr.dtype == object:
            vals = []
            for item in arr.ravel().tolist():
                vals.append(_h5_decode_value(item, f, _depth + 1))
            return vals
        if arr.dtype.kind in ("S", "U"):
            vals = arr.ravel().tolist()
            vals = [x.decode("utf-8", errors="ignore") if isinstance(x, bytes) else str(x) for x in vals]
            if all(len(x) == 1 for x in vals):
                return "".join(vals).strip()
            return [x.strip() for x in vals]
        # MATLAB char arrays in v7.3 are often uint16 numeric matrices.
        if arr.dtype.kind in ("u", "i") and arr.size and arr.size < 4096:
            flat = arr.ravel()
            if np.all((flat >= 0) & (flat < 65536)) and np.any((flat >= 32) & (flat <= 126)):
                chars = [chr(int(c)) for c in flat if int(c) != 0]
                txt = "".join(chars).strip()
                if txt and sum(ch.isprintable() for ch in txt) >= max(1, int(0.8 * len(txt))):
                    return txt
        return arr
    except Exception:
        return v


def _h5_get_path_ci(root, path_parts):
    cur = root
    try:
        for part in path_parts:
            if part in cur:
                cur = cur[part]
                continue
            low = str(part).lower()
            match = next((k for k in cur.keys() if str(k).lower() == low), None)
            if match is None:
                return None
            cur = cur[match]
        return cur
    except Exception:
        return None


def _h5_to_text_list(v) -> list[str]:
    v = _h5_decode_value(v)
    if v is None:
        return []
    if isinstance(v, str):
        return [v]
    if isinstance(v, dict):
        for key in ("data", "values", "categoryNames", "categories"):
            if key in v:
                vals = _h5_to_text_list(v[key])
                if vals:
                    return vals
        return []
    if isinstance(v, np.ndarray):
        if v.dtype.kind in ("U", "S"):
            return [str(x).strip() for x in v.ravel().tolist()]
        return [_mat_to_text(x, "") for x in v.ravel().tolist()]
    if isinstance(v, list):
        out = []
        for item in v:
            out.extend(_h5_to_text_list(item))
        return [x for x in out if x]
    txt = _mat_to_text(v, "")
    return [txt] if txt else []


def _h5_column_values(table_group, names: tuple[str, ...], n_hint: int = 0) -> list:
    if table_group is None:
        return []
    obj = None
    for name in names:
        obj = _h5_get_path_ci(table_group, [name])
        if obj is not None:
            break
    if obj is None:
        return []
    val = _h5_decode_value(obj)
    # MATLAB categorical: group with codes + categoryNames/categories.
    if isinstance(val, dict):
        codes = val.get("codes", None)
        if codes is None:
            codes = val.get("Codes", None)
        cats = val.get("categoryNames", None)
        if cats is None:
            cats = val.get("categories", None)
        if cats is None:
            cats = val.get("CategoryNames", None)
        if codes is not None and cats is not None:
            labels = _h5_to_text_list(cats)
            try:
                code_arr = np.asarray(codes, dtype=float).ravel()
                out = []
                for c in code_arr:
                    idx = int(c) - 1
                    out.append(labels[idx] if 0 <= idx < len(labels) else "")
                return out
            except Exception:
                pass
        for key in ("data", "values", "Value", "value"):
            if key in val:
                val = val[key]
                break
    if isinstance(val, str):
        return [val]
    if isinstance(val, list):
        # Cell array refs typically decode to one value per row.
        out = []
        for item in val:
            if isinstance(item, np.ndarray):
                if item.size == 4:
                    out.append(item.astype(float).ravel().tolist())
                else:
                    out.append(_mat_to_text(item, "") or item)
            else:
                txts = _h5_to_text_list(item)
                out.append(txts[0] if len(txts) == 1 else (txts or item))
        return out
    try:
        arr = np.asarray(val)
        if arr.ndim == 2 and 4 in arr.shape and arr.size >= 4 and names[0].lower() == "roi":
            if arr.shape[0] == 4:
                return [arr[:, i].astype(float).tolist() for i in range(arr.shape[1])]
            if arr.shape[1] == 4:
                return [arr[i, :].astype(float).tolist() for i in range(arr.shape[0])]
        if arr.ndim == 2 and arr.dtype.kind in ("u", "i") and arr.size < 10000:
            # char matrix: columns/rows are strings
            if max(arr.shape) > 1 and min(arr.shape) > 1:
                rows = []
                for row in arr.T if arr.shape[0] < arr.shape[1] else arr:
                    txt = "".join(chr(int(c)) for c in np.asarray(row).ravel() if int(c) != 0).strip()
                    rows.append(txt)
                return rows
        return arr.ravel().tolist()
    except Exception:
        return [val]




def _h5_decode_cell_dataset_to_list(ds, f=None) -> list:
    """Decode a MATLAB v7.3 cell dataset to a flat Python list."""
    try:
        arr = np.asarray(ds[()])
        if arr.dtype != object:
            return []
        out = []
        for item in arr.ravel().tolist():
            out.append(_h5_decode_value(item, f or getattr(ds, "file", None)))
        return out
    except Exception:
        return []


def _h5_collect_mcos_reference_values(f) -> list:
    """Return decoded MCOS reference payloads used by MATLAB v7.3 table/categorical objects."""
    vals = []
    try:
        mcos = _h5_get_path_ci(f, ["#subsystem#", "MCOS"])
        if mcos is None:
            return vals
        arr = np.asarray(mcos[()]).ravel().tolist()
        for ref in arr:
            try:
                vals.append(_h5_decode_value(ref, f))
            except Exception:
                vals.append(None)
    except Exception:
        pass
    return vals


def _looks_like_roi_name_catalog(vals: list[str]) -> bool:
    low = {str(v).strip().lower() for v in vals}
    return "track_minimap" in low and ("t_s" in low or "v_fzg_kmph" in low)


def _looks_like_fmt_catalog(vals: list[str]) -> bool:
    low = {str(v).strip().lower() for v in vals}
    return "any" in low and ("integer" in low or "float" in low or "time_m:ss" in low)


def _h5_mcos_table_categorical_columns(f) -> dict:
    """
    Best-effort decoder for MATLAB v7.3 table variables saved as MCOS objects.
    This is intentionally used as supplemental metadata: it recovers categorical
    name_roi/fmt columns when recordResult.ocr.roi_table itself is a MATLAB table
    dataset instead of a navigable HDF5 group.
    """
    out = {"names": [], "fmts": []}
    vals = _h5_collect_mcos_reference_values(f)
    if not vals:
        return out

    catalogs = []
    code_arrays = []
    for idx, val in enumerate(vals):
        if isinstance(val, list) and val and all(isinstance(x, str) for x in val):
            cleaned = [str(x).strip() for x in val if str(x).strip()]
            if _looks_like_roi_name_catalog(cleaned) or _looks_like_fmt_catalog(cleaned):
                catalogs.append((idx, cleaned))
        else:
            try:
                arr = np.asarray(val)
                if arr.size > 0 and arr.size <= 200 and arr.dtype.kind in ("u", "i"):
                    flat = arr.astype(int).ravel()
                    if np.all(flat >= 0):
                        code_arrays.append((idx, flat.tolist()))
            except Exception:
                pass

    roi_cats = next((c for _, c in catalogs if _looks_like_roi_name_catalog(c)), [])
    fmt_cats = next((c for _, c in catalogs if _looks_like_fmt_catalog(c)), [])

    def decode_codes(codes, cats, default=""):
        decoded = []
        for c in codes:
            try:
                ci = int(c)
                decoded.append(cats[ci - 1] if 1 <= ci <= len(cats) else default)
            except Exception:
                decoded.append(default)
        return decoded

    selected_roi_codes = None
    if roi_cats:
        roi_candidates = []
        for _, codes in code_arrays:
            if 1 <= len(codes) <= 100 and max(codes or [0]) <= len(roi_cats):
                dec = decode_codes(codes, roi_cats, "")
                if any(str(x).strip().lower() in {"track_minimap", "t_s", "v_fzg_kmph"} for x in dec):
                    roi_candidates.append((dec, list(codes)))
        if roi_candidates:
            # Prefer the column that includes track_minimap and has no blanks.
            roi_candidates.sort(key=lambda item: ("track_minimap" not in [str(x).lower() for x in item[0]], item[0].count(""), len(item[0])))
            out["names"] = roi_candidates[0][0]
            selected_roi_codes = roi_candidates[0][1]

    if fmt_cats:
        target_len = len(out.get("names") or [])
        fmt_candidates = []
        for _, codes in code_arrays:
            if 1 <= len(codes) <= 100 and max(codes or [0]) <= len(fmt_cats):
                if target_len and len(codes) != target_len:
                    continue
                # Important: MATLAB v7.3 table/categorical data can expose several
                # MCOS code arrays. The name_roi codes also decode to valid-looking
                # format strings when applied to the fmt category catalog
                # (e.g. t_s -> time_m:ss). Do not reuse the code vector that was
                # already identified as the ROI-name categorical column.
                if selected_roi_codes is not None and list(codes) == selected_roi_codes:
                    continue
                dec = decode_codes(codes, fmt_cats, "any")
                if any(str(x).strip().lower() in {"any", "integer", "float"} or str(x).startswith("time_") or str(x).startswith("int_") for x in dec):
                    fmt_candidates.append((dec, list(codes)))
        if fmt_candidates:
            # Prefer mixed/specific OCR formats over accidental low-index category
            # vectors; the real fmt vector often contains int_* / time_mm / any.
            def score(item):
                xs = item[0]
                specific = sum(1 for x in xs if str(x).startswith("int_") or str(x).startswith("time_mm") or str(x).startswith("time_hh"))
                useful = sum(1 for x in xs if str(x).startswith("time_") or str(x).startswith("int_") or str(x) in ("any", "integer", "float", "alnum"))
                return (-specific, -useful, xs.count("any"), len(xs))
            fmt_candidates.sort(key=score)
            out["fmts"] = fmt_candidates[0][0]

    return out


def _extract_roi_format_map_from_recordresult_hdf5(mat_path: str) -> dict[str, str]:
    """Recover ROI format values from a v7.3 MATLAB table/categorical roi_table."""
    try:
        import h5py
        with h5py.File(mat_path, "r") as f:
            tbl = _h5_get_path_ci(f, ["recordResult", "ocr", "roi_table"])
            if tbl is None:
                return {}
            names = _h5_column_values(tbl, ("name_roi", "name", "Name"))
            fmts = _h5_column_values(tbl, ("fmt", "format", "Format"))
            if not names or not fmts:
                recovered = _h5_mcos_table_categorical_columns(f)
                names = names or recovered.get("names") or []
                fmts = fmts or recovered.get("fmts") or []
            out = {}
            for i, nm in enumerate(names):
                name = _mat_to_text(nm, "").strip()
                if not name:
                    continue
                fmt = _normalize_roi_format(fmts[i] if i < len(fmts) else "any")
                out[name] = fmt or "any"
            return out
    except Exception:
        return {}


def _apply_roi_format_map(rois: list[dict], fmt_map: dict[str, str]) -> list[dict]:
    if not rois or not fmt_map:
        return rois
    out = []
    for r in rois:
        nr = dict(r)
        nm = str(nr.get("name", "")).strip()
        if nm in fmt_map:
            nr["fmt"] = _normalize_roi_format(fmt_map.get(nm) or "any")
        elif not nr.get("fmt"):
            nr["fmt"] = "any"
        out.append(nr)
    return out

def _extract_rois_from_recordresult_hdf5(mat_path: str) -> list[dict]:
    try:
        import h5py
        with h5py.File(mat_path, "r") as f:
            tbl = _h5_get_path_ci(f, ["recordResult", "ocr", "roi_table"])
            if tbl is None:
                return []
            names = _h5_column_values(tbl, ("name_roi", "name", "Name"))
            rois = _h5_column_values(tbl, ("roi", "ROI"))
            fmts = _h5_column_values(tbl, ("fmt", "format", "Format"))
            if (not names) or (not fmts):
                recovered = _h5_mcos_table_categorical_columns(f)
                names = names or recovered.get("names") or []
                fmts = fmts or recovered.get("fmts") or []
            n = max(len(names), len(rois), len(fmts))
            out = []
            for i in range(n):
                xywh = _parse_roi_value(rois[i] if i < len(rois) else None)
                if not xywh:
                    continue
                out.append(dict(
                    name=_mat_to_text(names[i] if i < len(names) else "_", "_") or "_",
                    x=float(xywh[0]), y=float(xywh[1]),
                    w=float(xywh[2]), h=float(xywh[3]),
                    fmt=_normalize_roi_format(fmts[i] if i < len(fmts) else "any"),
                    max_scale=float(st.session_state.get("roi_global_scale", 1.2)),
                ))
            return out
    except Exception:
        return []

def _extract_rois_from_recordresult_mat(mat_path: str) -> list[dict]:
    """Read recordResult.ocr.roi_table directly; fixes MATLAB categorical fmt values."""
    try:
        data = sio.loadmat(mat_path, squeeze_me=True, struct_as_record=False)
        rr = _mat_scalar(data.get("recordResult"))
        ocr = _mat_obj_get(rr, "ocr")
        roi_table = _mat_obj_get(ocr, "roi_table")
        n = _mat_table_height(roi_table)
        if n <= 0:
            return []
        names = _mat_column_values(_mat_table_column(roi_table, "name_roi", "name", "Name"), n)
        rois = _mat_column_values(_mat_table_column(roi_table, "roi", "ROI"), n)
        fmts = _mat_column_values(_mat_table_column(roi_table, "fmt", "format", "Format"), n)
        out = []
        for i in range(n):
            xywh = _parse_roi_value(rois[i])
            if not xywh:
                continue
            out.append(dict(
                name=_mat_to_text(names[i], "_") or "_",
                x=float(xywh[0]), y=float(xywh[1]),
                w=float(xywh[2]), h=float(xywh[3]),
                fmt=_normalize_roi_format(fmts[i]),
                max_scale=float(st.session_state.get("roi_global_scale", 1.2)),
            ))
        return out
    except NotImplementedError:
        return _extract_rois_from_recordresult_hdf5(mat_path)
    except Exception:
        h5_rois = _extract_rois_from_recordresult_hdf5(mat_path)
        return h5_rois or []



def _pts_from_mat_value(v) -> list[list[float]]:
    try:
        arr = np.asarray(_mat_scalar(v), dtype=float)
        if arr.size < 4:
            return []
        arr = arr.reshape((-1, 2)) if arr.ndim == 1 else arr
        if arr.shape[1] != 2 and arr.shape[0] == 2:
            arr = arr.T
        pts = []
        for x, y in arr[:8, :2]:
            if np.isfinite(x) and np.isfinite(y):
                pts.append([float(x), float(y)])
        return pts
    except Exception:
        return []


def _marker_to_color_range(marker) -> dict | None:
    if marker is None:
        return None
    try:
        def _first_numeric(*names):
            for name in names:
                val = _mat_obj_get(marker, name)
                if val is not None:
                    try:
                        arr = np.asarray(_mat_scalar(val), dtype=float).ravel()
                        if arr.size:
                            return arr
                    except Exception:
                        pass
            return np.array([], dtype=float)

        direct = _first_numeric("hsv_range", "range", "HSVRange")
        if direct.size >= 6:
            vals = direct[:6].astype(float)
            # Accept either normalized HSV [0..1] or OpenCV HSV ranges.
            if np.nanmax(vals) <= 1.5:
                vals = np.array([vals[0]*179, vals[1]*179, vals[2]*255, vals[3]*255, vals[4]*255, vals[5]*255])
            return dict(
                h_lo=int(max(0, round(vals[0]))), h_hi=int(min(179, round(vals[1]))),
                s_lo=int(max(0, round(vals[2]))), s_hi=int(min(255, round(vals[3]))),
                v_lo=int(max(0, round(vals[4]))), v_hi=int(min(255, round(vals[5]))),
            )

        mu = _first_numeric("hsv_mu", "hsvMean", "hsv_mean", "mu", "HSV_mu", "hsv")
        sig = _first_numeric("hsv_sig", "hsvStd", "hsv_std", "sigma", "sig", "HSV_sig")
        if mu.size < 3 or not np.all(np.isfinite(mu[:3])):
            rgb = _first_numeric("rgb", "rgb_mu", "color_rgb", "marker_rgb")
            if rgb.size >= 3:
                rgb = np.clip(rgb[:3], 0, 255).astype(np.uint8)
                mu = cv2.cvtColor(np.array([[rgb]], dtype=np.uint8), cv2.COLOR_RGB2HSV)[0, 0].astype(float)
                sig = np.array([15.0, 60.0, 60.0], dtype=float)
            else:
                return None
        if sig.size < 3 or not np.all(np.isfinite(sig[:3])):
            sig = np.array([0.08, 0.20, 0.20], dtype=float) if np.nanmax(mu[:3]) <= 1.5 else np.array([15.0, 60.0, 60.0], dtype=float)

        if np.nanmax(mu[:3]) <= 1.5:
            h = float(mu[0]) * 179.0
            s = float(mu[1]) * 255.0
            v = float(mu[2]) * 255.0
            dh = max(15.0, float(sig[0]) * 179.0 * 3.0)
            ds = max(35.0, float(sig[1]) * 255.0 * 3.0)
            dv = max(35.0, float(sig[2]) * 255.0 * 3.0)
        else:
            h, s, v = float(mu[0]), float(mu[1]), float(mu[2])
            dh = max(15.0, float(sig[0]) * 3.0)
            ds = max(35.0, float(sig[1]) * 3.0)
            dv = max(35.0, float(sig[2]) * 3.0)
        return dict(
            h_lo=int(max(0, round(h - dh))), h_hi=int(min(179, round(h + dh))),
            s_lo=int(max(0, round(s - ds))), s_hi=int(min(255, round(s + ds))),
            v_lo=int(max(0, round(v - dv))), v_hi=int(min(255, round(v + dv))),
        )
    except Exception:
        return None


def _extract_track_cal_from_recordresult_mat(mat_path: str) -> dict:
    """Load recordResult.ocr.trkCalSlim: ROI, 8 minimap points and marker color."""
    out = {}
    try:
        data = sio.loadmat(mat_path, squeeze_me=True, struct_as_record=False)
        rr = _mat_scalar(data.get("recordResult"))
        ocr = _mat_obj_get(rr, "ocr")
        trk = _mat_obj_get(ocr, "trkCalSlim")
        if trk is None:
            return out
        roi = _parse_roi_value(_mat_obj_get(trk, "roi"))
        if roi and len(roi) >= 4 and roi[2] > 0 and roi[3] > 0:
            out["track_roi"] = [float(roi[0]), float(roi[1]), float(roi[2]), float(roi[3])]
        pts = _pts_from_mat_value(_mat_obj_get(trk, "ptsMini"))
        if len(pts) >= 4:
            out["minimap_pts"] = pts[:8]
        cr = _marker_to_color_range(_mat_obj_get(trk, "marker"))
        if cr:
            out["moving_pt_color_range"] = cr
    except NotImplementedError:
        return _extract_track_cal_from_recordresult_hdf5(mat_path)
    except Exception:
        h5_out = _extract_track_cal_from_recordresult_hdf5(mat_path)
        if h5_out:
            return h5_out
    return out




def _extract_track_cal_from_recordresult_hdf5(mat_path: str) -> dict:
    out = {}
    try:
        import h5py
        with h5py.File(mat_path, "r") as f:
            trk = _h5_get_path_ci(f, ["recordResult", "ocr", "trkCalSlim"])
            if trk is None:
                return out
            roi_obj = _h5_get_path_ci(trk, ["roi"])
            roi = _parse_roi_value(_h5_decode_value(roi_obj) if roi_obj is not None else None)
            if roi and len(roi) >= 4 and roi[2] > 0 and roi[3] > 0:
                out["track_roi"] = [float(roi[0]), float(roi[1]), float(roi[2]), float(roi[3])]
            pts_obj = _h5_get_path_ci(trk, ["ptsMini"])
            pts = _pts_from_mat_value(_h5_decode_value(pts_obj) if pts_obj is not None else None)
            if len(pts) >= 4:
                out["minimap_pts"] = pts[:8]
            marker_obj = _h5_get_path_ci(trk, ["marker"])
            marker_val = _h5_decode_value(marker_obj) if marker_obj is not None else None
            # _marker_to_color_range expects object-like fields; for decoded dict use a tiny adapter.
            if isinstance(marker_val, dict):
                class _Obj: pass
                obj = _Obj()
                for k, v in marker_val.items():
                    setattr(obj, k, v)
                marker_val = obj
            cr = _marker_to_color_range(marker_val)
            if cr:
                out["moving_pt_color_range"] = cr
    except Exception:
        pass
    return out


def _extract_track_name_from_recordresult_mat(mat_path: str) -> str:
    try:
        data = sio.loadmat(mat_path, squeeze_me=True, struct_as_record=False)
        rr = _mat_scalar(data.get("recordResult"))
        ocr = _mat_obj_get(rr, "ocr")
        trk = _mat_obj_get(ocr, "track")
        for field in ("name", "track", "trackName", "track_name", "id", "title"):
            txt = _mat_to_text(_mat_obj_get(trk, field), "")
            if txt:
                return txt
    except NotImplementedError:
        try:
            import h5py
            with h5py.File(mat_path, "r") as f:
                trk = _h5_get_path_ci(f, ["recordResult", "ocr", "track"])
                if trk is not None:
                    for field in ("name", "track", "trackName", "track_name", "id", "title"):
                        obj = _h5_get_path_ci(trk, [field])
                        txts = _h5_to_text_list(obj) if obj is not None else []
                        if txts:
                            return str(txts[0])
        except Exception:
            pass
    except Exception:
        pass
    return ""


def _try_auto_load_reference_track_for_name(track_name: str) -> None:
    """Auto-select Nordschleife reference when recordResult.ocr.track says so."""
    if st.session_state.get("ref_track_img") is not None:
        return
    name = str(track_name or "")
    prefer_nord = "nordschleife" in name.lower()
    if not prefer_nord:
        return
    candidates = []
    if st.session_state.get("r2_connected") and st.session_state.get("r2_client") is not None:
        pfx = st.session_state.r2_prefix.strip("/")
        ref_dir = (pfx + "/reference_track_siesmann").strip("/") if pfx else "reference_track_siesmann"
        ok_ls, ref_items = st.session_state.r2_client.list_files(ref_dir)
        if ok_ls and isinstance(ref_items, list):
            mats = [f for f in ref_items if str(f).lower().endswith(".mat")]
            mats = sorted(
                mats,
                key=lambda f: (
                    0 if "nordschleife" in Path(str(f)).stem.lower() and Path(str(f)).stem.lower().endswith("_slim") else
                    1 if "nordschleife" in Path(str(f)).stem.lower() else 2,
                    Path(str(f)).name.lower(),
                ),
            )
            if mats and "nordschleife" in Path(str(mats[0])).stem.lower():
                _load_centerline_from_r2(f"{ref_dir}/{mats[0]}", str(mats[0]))
                return
    # Local fallback for project folder / reference files next to the app.
    try:
        search_roots = [Path.cwd(), Path(__file__).resolve().parent, Path('/mnt/data')]
        seen = set()
        for root in search_roots:
            if not root.exists() or root in seen:
                continue
            seen.add(root)
            for fp in root.rglob("*.mat"):
                nm = fp.name.lower()
                if "nordschleife" in nm:
                    candidates.append(fp)
        candidates = sorted(candidates, key=lambda fp: (0 if fp.stem.lower().endswith("_slim") else 1, fp.name.lower()))
        if candidates:
            _apply_centerline_to_session(str(candidates[0]), candidates[0].name)
    except Exception:
        pass

def _upsert_track_minimap_roi_from_mat(track_roi: list[float]) -> None:
    if not track_roi or len(track_roi) < 4:
        return
    x, y, w, h = [float(v) for v in track_roi[:4]]
    if w <= 0 or h <= 0:
        return
    new_roi = dict(name="track_minimap", x=x, y=y, w=w, h=h, fmt="any", max_scale=float(st.session_state.get("roi_global_scale", 1.2)))
    rois = list(st.session_state.get("rois") or [])
    replaced = False
    for i, r in enumerate(rois):
        if str(r.get("name", "")).strip() == "track_minimap":
            nr = dict(r)
            nr.update(new_roi)
            rois[i] = nr
            replaced = True
            break
    if not replaced:
        rois.append(new_roi)
    st.session_state.rois = _sanitize_rois(rois)


def _mat_has_nonempty_roi_field(x) -> bool:
    roi = _mat_obj_get(x, "roi")
    if roi is None:
        return False
    parsed = _parse_roi_value(roi)
    return bool(parsed and len(parsed) >= 4 and parsed[2] > 0 and parsed[3] > 0)


def _mat_is_nonempty(x) -> bool:
    if x is None:
        return False
    x = _mat_scalar(x)
    try:
        if isinstance(x, np.ndarray):
            return x.size > 0
        if isinstance(x, (str, bytes)):
            return len(x) > 0
        if isinstance(x, (list, tuple, dict)):
            return len(x) > 0
        return True
    except Exception:
        return True


def _mat_to_float(x, default=np.nan) -> float:
    try:
        x = _mat_scalar(x)
        if isinstance(x, np.ndarray):
            x = np.asarray(x, dtype=float).ravel()
            return float(x[0]) if x.size else default
        return float(x)
    except Exception:
        return default


def _mat_table_height(tbl) -> int:
    tbl = _mat_scalar(tbl)
    if tbl is None:
        return 0
    try:
        if isinstance(tbl, np.ndarray):
            return int(tbl.shape[0]) if tbl.ndim > 0 else int(tbl.size)
        for name in getattr(tbl, "_fieldnames", []) or []:
            val = _mat_obj_get(tbl, name)
            if isinstance(val, np.ndarray):
                return int(val.size if val.ndim == 1 else val.shape[0])
        return 1 if _mat_is_nonempty(tbl) else 0
    except Exception:
        return 0


def _mat_table_column(tbl, *names):
    tbl = _mat_scalar(tbl)
    for name in names:
        val = _mat_obj_get(tbl, name)
        if val is not None:
            return val
    return None


def _mat_numeric_vector(x):
    try:
        arr = np.asarray(_mat_scalar(x), dtype=float).ravel()
        return arr[np.isfinite(arr)]
    except Exception:
        return np.array([], dtype=float)


def _mat_roi_table_has_track(roi_table) -> bool:
    names = _mat_table_column(roi_table, "name_roi", "name", "Name")
    if names is None:
        return False
    try:
        vals = np.asarray(names).ravel().tolist()
        joined = " ".join(str(_mat_scalar(v)).lower() for v in vals)
        return "track_minimap" in joined
    except Exception:
        return False


def _summarize_record_result_hdf5(mat_path: str) -> dict:
    out = {}
    try:
        import h5py
        paths = []
        shapes = {}
        with h5py.File(mat_path, "r") as f:
            def visitor(name, obj):
                lname = str(name).lower()
                paths.append(lname)
                if hasattr(obj, "shape"):
                    shapes[lname] = tuple(int(v) for v in obj.shape)
            f.visititems(visitor)
        joined = "\n".join(paths)
        def has(*needles):
            return any(n.lower() in joined for n in needles)
        def nonempty_path(*needles):
            for p, sh in shapes.items():
                if any(n.lower() in p for n in needles):
                    if not sh or int(np.prod(sh)) > 0:
                        return True
            return False
        out["roi_selected"] = has("recordresult/ocr/roi_table", "roi_table")
        out["track_selected"] = nonempty_path("recordresult/ocr/trkcalslim/roi")
        out["start_end_selected"] = has("recordresult/ocr/params/start_s") and has("recordresult/ocr/params/end_s")
        out["ocr_done"] = nonempty_path("recordresult/ocr/table", "recordresult/ocr/cleaned")
        out["ocr_complete"] = bool(out.get("ocr_done") and out.get("start_end_selected"))
        out["audio_spectrogram_done"] = nonempty_path("recordresult/audio_rpm/processed", "recordresult/audio_rpm/params")
        out["validation_done"] = nonempty_path("recordresult/validation/results")
    except Exception:
        pass
    return out


def _summarize_record_result_mat(mat_path: str) -> dict:
    out = {}
    try:
        data = sio.loadmat(mat_path, squeeze_me=True, struct_as_record=False)
        rr = _mat_scalar(data.get("recordResult"))
        if rr is None:
            return out
        meta = _mat_obj_get(rr, "metadata")
        ocr = _mat_obj_get(rr, "ocr")
        audio_rpm = _mat_obj_get(rr, "audio_rpm")
        validation = _mat_obj_get(rr, "validation")

        video_path = _mat_obj_get(meta, "video") or _mat_obj_get(rr, "video")
        audio_path = _mat_obj_get(meta, "audio") or _mat_obj_get(rr, "audio")
        out["recordresult_video_path"] = str(_mat_scalar(video_path) or "")
        out["recordresult_audio_path"] = str(_mat_scalar(audio_path) or "")

        params = _mat_obj_get(ocr, "params")
        start_s = _mat_to_float(_mat_obj_get(params, "start_s"))
        end_s = _mat_to_float(_mat_obj_get(params, "end_s"))
        out["start_end_selected"] = bool(np.isfinite(start_s) and np.isfinite(end_s) and end_s > start_s)
        out["t_start"] = start_s if np.isfinite(start_s) else None
        out["t_end"] = end_s if np.isfinite(end_s) else None

        roi_table = _mat_obj_get(ocr, "roi_table")
        roi_rows = _mat_table_height(roi_table)
        out["roi_selected"] = bool(roi_rows > 0)
        out["roi_count"] = int(roi_rows)

        # OCRExtractor.m stores reusable track calibration in recordResult.ocr.trkCalSlim.
        # Count Track as available only when trkCalSlim exists AND has a non-empty roi field.
        trk_slim = _mat_obj_get(ocr, "trkCalSlim")
        out["track_selected"] = bool(_mat_has_nonempty_roi_field(trk_slim))

        raw_table = _mat_obj_get(ocr, "table")
        cleaned = _mat_obj_get(ocr, "cleaned")
        raw_rows = _mat_table_height(raw_table)
        clean_rows = _mat_table_height(cleaned)
        out["ocr_done"] = bool(raw_rows > 0 or clean_rows > 0)
        out["ocr_row_count"] = int(max(raw_rows, clean_rows))

        time_col = _mat_table_column(raw_table, "time_s", "t_s")
        if time_col is None:
            time_col = _mat_table_column(cleaned, "time_s", "t_s")
        times = _mat_numeric_vector(time_col)
        if out["ocr_done"] and np.isfinite(end_s) and times.size:
            out["ocr_complete"] = bool(np.nanmax(times) >= (end_s - 1.0))
        else:
            out["ocr_complete"] = bool(out["ocr_done"] and not np.isfinite(end_s))

        ar_processed = _mat_obj_get(audio_rpm, "processed")
        ar_params = _mat_obj_get(audio_rpm, "params")
        out["audio_spectrogram_done"] = bool(_mat_table_height(ar_processed) > 0 or _mat_is_nonempty(ar_params))

        v_results = _mat_obj_get(validation, "results")
        out["validation_done"] = bool(_mat_is_nonempty(v_results))
    except NotImplementedError:
        out.update(_summarize_record_result_hdf5(mat_path))
    except Exception:
        pass
    return out


def _compute_mat_summary_remote(remote_key: str, client, prefix: str) -> dict:
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".mat")
    tmp.close()
    ok, msg = client.download_file(remote_key, tmp.name)
    if not ok:
        return {"mat_file": Path(remote_key).name, "remote_key": remote_key, "error": f"Download: {msg}"}

    _mat_keys = set()
    record_summary = {}
    try:
        summary = summarize_mat_file(tmp.name)
        record_summary = _summarize_record_result_mat(tmp.name)
        try:
            _mat_keys = set(sio.loadmat(tmp.name, squeeze_me=True, struct_as_record=False).keys())
        except Exception:
            _mat_keys = set()
    except Exception as e:
        summary = {"error": f"{e.__class__.__name__}: {e}"}
        record_summary = _summarize_record_result_mat(tmp.name)
    finally:
        try:
            Path(tmp.name).unlink(missing_ok=True)
        except Exception:
            pass

    def _mat_has_any(keys):
        lk = {str(k).lower() for k in _mat_keys}
        return any(str(k).lower() in lk for k in keys)

    for k, v in record_summary.items():
        if v is not None:
            summary[k] = v
    summary["roi_selected"] = bool(summary.get("roi_selected") or _mat_has_any(["rois", "roi", "roi_table", "roitable"]))
    # Track status follows OCRExtractor.m: only recordResult.ocr.trkCalSlim.roi counts.
    summary["track_selected"] = bool(summary.get("track_selected"))
    summary["start_end_selected"] = bool(summary.get("start_end_selected") or _mat_has_any(["t_start", "t_end", "start_time", "end_time", "startend", "time_range"]))
    summary["ocr_done"] = bool(summary.get("ocr_done") or _mat_has_any(["ocr_results", "ocr_values", "results_table"]))
    summary["ocr_complete"] = bool(summary.get("ocr_complete") or (summary.get("ocr_done") and not _mat_has_any(["ocr_missing", "ocr_errors", "missing_values"])))
    summary["audio_spectrogram_done"] = bool(summary.get("audio_spectrogram_done") or _mat_has_any(["spectrogram", "audio_spectrogram", "audioanalysis", "audio_analysis", "pxx", "audio_rpm"]))
    summary["validation_done"] = bool(summary.get("validation_done") or _mat_has_any(["validation", "validated", "validierung", "validation_results"]))

    summary["mat_file"] = Path(remote_key).name
    summary["remote_key"] = remote_key
    if not summary.get("capture_folder"):
        summary["capture_folder"] = _mat_capture_guess_from_key(remote_key)
    capture_folder = summary.get("capture_folder", "")
    summary["video_file_exists"] = None
    summary["audio_file_exists"] = None

    if capture_folder:
        cap_dir = f"{prefix}/captures/{capture_folder}" if prefix else f"captures/{capture_folder}"
        ok_list, items = client.list_files(cap_dir)
        if ok_list and isinstance(items, list):
            files = [n for n in items if not n.endswith("/")]
            lower_files = [n.lower() for n in files]
            has_full_or_proxy_video = any(
                n.endswith((".mp4", ".mov", ".avi", ".mkv")) for n in lower_files
            )
            has_audio_file = any(
                n.endswith((".wav", ".mp3", ".m4a", ".aac", ".flac")) for n in lower_files
            )
            has_framepack = "frames_1fps/" in items
            framepack_count = 0
            framepack_expected = 0
            framepack_complete = False
            if has_framepack:
                ok_fp, fp_items = client.list_files(f"{cap_dir}/frames_1fps")
                if ok_fp and isinstance(fp_items, list):
                    framepack_count = len([
                        x for x in fp_items if str(x).lower().endswith((".jpg", ".jpeg", ".png", ".webp"))
                    ])
                    if "index.json" in fp_items:
                        tmp_idx = tempfile.NamedTemporaryFile(delete=False, suffix=".json")
                        tmp_idx.close()
                        ok_idx, _ = client.download_file(f"{cap_dir}/frames_1fps/index.json", tmp_idx.name)
                        if ok_idx:
                            try:
                                payload = json.loads(Path(tmp_idx.name).read_text(encoding="utf-8", errors="ignore"))
                                framepack_expected = int(payload.get("frame_count", 0) or 0)
                            except Exception:
                                framepack_expected = 0
                        try:
                            Path(tmp_idx.name).unlink(missing_ok=True)
                        except Exception:
                            pass
                    framepack_complete = framepack_count > 0 and (
                        framepack_expected <= 0 or framepack_count >= framepack_expected
                    )
            has_audio_proxy = any(n == AUDIO_PROXY_NAME.lower() for n in lower_files)
            summary["framepack_count"] = int(framepack_count)
            summary["framepack_expected"] = int(framepack_expected)
            summary["framepack_complete"] = bool(framepack_complete)
            summary["audio_proxy_present"] = bool(has_audio_proxy)
            summary["video_file_exists"] = bool(has_full_or_proxy_video or framepack_complete)
            summary["audio_file_exists"] = bool(has_audio_file or has_audio_proxy)
            if not framepack_complete and has_framepack:
                summary["media_detail"] = f"Frames unvollstaendig ({framepack_count}/{framepack_expected or '?'})."
            elif not has_framepack:
                summary["media_detail"] = "Frames fehlen."
            elif not summary["audio_file_exists"]:
                summary["media_detail"] = "Audio fehlt."
            else:
                summary["media_detail"] = ""
        else:
            summary["media_detail"] = "Capture-Ordner nicht lesbar."
    else:
        summary["media_detail"] = ""

    return summary


def _compute_folder_only_summary(folder: str, client, prefix: str) -> dict:
    summary = {
        "mat_file": "",
        "remote_key": "",
        "capture_folder": folder,
        "video_file_exists": False,
        "audio_file_exists": False,
        "roi_selected": False,
        "track_selected": False,
        "start_end_selected": False,
        "ocr_done": False,
        "ocr_complete": False,
        "audio_spectrogram_done": False,
        "validation_done": False,
        "error": "",
        "media_detail": "",
    }
    cap_dir = f"{prefix}/captures/{folder}" if prefix else f"captures/{folder}"
    ok_list, items = client.list_files(cap_dir)
    if not ok_list or not isinstance(items, list):
        summary["error"] = "Capture-Ordner nicht lesbar."
        return summary

    files = [n for n in items if not n.endswith("/")]
    lower_files = [n.lower() for n in files]
    has_audio_file = any(n.endswith((".wav", ".mp3", ".m4a", ".aac", ".flac")) for n in lower_files)
    has_audio_proxy = any(n == AUDIO_PROXY_NAME.lower() for n in lower_files)

    has_framepack = "frames_1fps/" in items
    framepack_count = 0
    framepack_expected = 0
    if has_framepack:
        ok_fp, fp_items = client.list_files(f"{cap_dir}/frames_1fps")
        if ok_fp and isinstance(fp_items, list):
            framepack_count = len([
                x for x in fp_items if str(x).lower().endswith((".jpg", ".jpeg", ".png", ".webp"))
            ])
            if "index.json" in fp_items:
                tmp_idx = tempfile.NamedTemporaryFile(delete=False, suffix=".json")
                tmp_idx.close()
                ok_idx, _ = client.download_file(f"{cap_dir}/frames_1fps/index.json", tmp_idx.name)
                if ok_idx:
                    try:
                        payload = json.loads(Path(tmp_idx.name).read_text(encoding="utf-8", errors="ignore"))
                        framepack_expected = int(payload.get("frame_count", 0) or 0)
                    except Exception:
                        framepack_expected = 0
                try:
                    Path(tmp_idx.name).unlink(missing_ok=True)
                except Exception:
                    pass

    framepack_complete = framepack_count > 0 and (framepack_expected <= 0 or framepack_count >= framepack_expected)
    summary["video_file_exists"] = bool(framepack_complete)
    summary["audio_file_exists"] = bool(has_audio_file or has_audio_proxy)
    if not framepack_complete and has_framepack:
        summary["media_detail"] = f"Frames unvollstaendig ({framepack_count}/{framepack_expected or '?'})."
    elif not has_framepack:
        summary["media_detail"] = "Frames fehlen."
    elif not summary["audio_file_exists"]:
        summary["media_detail"] = "Audio fehlt."
    else:
        summary["media_detail"] = ""
    return summary


def _get_mat_summary_from_r2(remote_key: str):
    cache = st.session_state.get("mat_summary_cache")
    if isinstance(cache, dict) and remote_key in cache:
        cval = cache.get(remote_key)
        if isinstance(cval, dict):
            return dict(cval)
    summary = _compute_mat_summary_remote(
        remote_key=remote_key,
        client=st.session_state.r2_client,
        prefix=st.session_state.r2_prefix.strip("/"),
    )
    if isinstance(cache, dict):
        cache[remote_key] = dict(summary)
        st.session_state.mat_summary_cache = cache
    return summary


def _analyze_mat_from_r2(remote_key: str):
    summary = _get_mat_summary_from_r2(remote_key)
    if summary.get("error"):
        set_status(f"MAT-Analysefehler: {summary['error']}", "warn")
    st.session_state.mat_selected_summary = summary


def _jn(value) -> str:
    return LAMP_GREEN if bool(value) else LAMP_RED


def _summary_to_overview_row(summary: dict, display_folder: str = "") -> dict:
    folder_label = display_folder or summary.get("capture_folder") or summary.get("mat_file", "")
    return {
        "mat_datei": folder_label,
        "remote_key": summary.get("remote_key", ""),
        "audio_video_vorhanden": _jn(
            bool(summary.get("video_file_exists")) and bool(summary.get("audio_file_exists"))
        ),
        "roi_ausgewaehlt": _jn(summary.get("roi_selected")),
        "track_ausgewaehlt": _jn(summary.get("track_selected")),
        "anfang_ende_ausgewaehlt": _jn(summary.get("start_end_selected")),
        "ocr_durchgefuehrt": _jn(summary.get("ocr_done")),
        "ocr_vollstaendig": _jn(summary.get("ocr_complete")),
        "audioanalyse_spektrogramm": _jn(summary.get("audio_spectrogram_done")),
        "validierung": _jn(summary.get("validation_done")),
        "fehler": summary.get("error", "") or summary.get("media_detail", ""),
    }


def _placeholder_overview_row(target) -> dict:
    if isinstance(target, dict):
        folder = str(target.get("folder", "") or "")
        mat_key = str(target.get("mat_key", "") or "")
    else:
        folder = ""
        mat_key = str(target)
    title = folder or Path(mat_key).name
    return {
        "mat_datei": title,
        "remote_key": mat_key,
        "audio_video_vorhanden": "...",
        "roi_ausgewaehlt": "...",
        "track_ausgewaehlt": "...",
        "anfang_ende_ausgewaehlt": "...",
        "ocr_durchgefuehrt": "...",
        "ocr_vollstaendig": "...",
        "audioanalyse_spektrogramm": "...",
        "validierung": "...",
        "fehler": "",
    }


def _build_mat_overview_rows(remote_keys: list[str]) -> list[dict]:
    rows = []
    for key in remote_keys:
        summary = _get_mat_summary_from_r2(key)
        rows.append(_summary_to_overview_row(summary))
    return rows


def _start_mat_update(remote_keys: list[str]):
    targets = []
    for it in remote_keys:
        if isinstance(it, dict):
            targets.append(it)
        else:
            targets.append({"kind": "mat_only", "folder": _mat_capture_guess_from_key(str(it)), "mat_key": str(it)})
    st.session_state.mat_update_keys = list(targets)
    st.session_state.mat_update_total = len(targets)
    st.session_state.mat_update_idx = 0
    st.session_state.mat_update_running = len(targets) > 0
    st.session_state.mat_run_state = "running" if len(targets) > 0 else "idle"
    st.session_state.mat_overview_rows = [_placeholder_overview_row(t) for t in targets]


def _step_mat_update_once():
    if not st.session_state.mat_update_running:
        return
    idx = st.session_state.mat_update_idx
    total = st.session_state.mat_update_total
    keys = st.session_state.mat_update_keys
    if idx >= total:
        st.session_state.mat_update_running = False
        st.session_state.mat_run_state = "idle"
        return

    target = keys[idx]
    folder = str(target.get("folder", "") or "") if isinstance(target, dict) else ""
    key = str(target.get("mat_key", "") or "") if isinstance(target, dict) else str(target)
    if key:
        summary = _get_mat_summary_from_r2(key)
    else:
        summary = _compute_folder_only_summary(
            folder=folder,
            client=st.session_state.r2_client,
            prefix=st.session_state.r2_prefix.strip("/"),
        )
    st.session_state.mat_overview_rows[idx] = _summary_to_overview_row(summary, display_folder=folder)
    st.session_state.mat_update_idx = idx + 1

    if st.session_state.mat_update_idx >= total:
        st.session_state.mat_update_running = False
        st.session_state.mat_run_state = "idle"
        set_status(f"Analyse fuer {total} MAT-Dateien abgeschlossen.", "ok")



def _render_disabled_mat_overview_table(slot, rows_or_df):
    df = rows_or_df if isinstance(rows_or_df, pd.DataFrame) else pd.DataFrame(rows_or_df)
    with slot.container():
        st.markdown('<div class="mat-selection-disabled">MAT-Auswahl wird aktualisiert ...</div>', unsafe_allow_html=True)
        try:
            view = df.style.set_properties(**{
                "background-color": "#20232b",
                "color": "#7d8491",
            })
        except Exception:
            view = df
        st.dataframe(
            view,
            width="stretch",
            hide_index=True,
            height=MAT_TABLE_HEIGHT,
            column_config=MAT_OVERVIEW_COLCFG,
        )

def _status_cell_style(value):
    txt = str(value)
    if (LAMP_GREEN in txt) or txt.endswith("Ja"):
        return "background-color: #0f3d1f; color: #e8ffe8;"
    if (LAMP_RED in txt) or txt.endswith("Nein"):
        return "background-color: #4a1d1d; color: #ffe8e8;"
    return ""


def _style_overview_dataframe(df: pd.DataFrame):
    status_cols = [
        "audio_video_vorhanden",
        "roi_ausgewaehlt",
        "track_ausgewaehlt",
        "anfang_ende_ausgewaehlt",
        "ocr_durchgefuehrt",
        "ocr_vollstaendig",
        "audioanalyse_spektrogramm",
        "validierung",
    ]
    styler = df.style
    if hasattr(styler, "map"):
        return styler.map(_status_cell_style, subset=status_cols)
    if hasattr(styler, "applymap"):
        return styler.applymap(_status_cell_style, subset=status_cols)
    return df


def _normalize_overview_lamps(df: pd.DataFrame) -> pd.DataFrame:
    if df is None or df.empty:
        return df

    def _decode_mojibake_once(s: str) -> str:
        try:
            return s.encode("latin-1", errors="strict").decode("utf-8", errors="strict")
        except Exception:
            return s

    def _norm_cell(v):
        s = str(v)
        cur = s
        for _ in range(3):
            nxt = _decode_mojibake_once(cur)
            if nxt == cur:
                break
            cur = nxt
        if cur == LAMP_GREEN or cur == MOJIBAKE_GREEN:
            return LAMP_GREEN
        if cur == LAMP_RED or cur == MOJIBAKE_RED:
            return LAMP_RED
        return v

    out = df.copy()
    status_cols = [
        "audio_video_vorhanden",
        "roi_ausgewaehlt",
        "track_ausgewaehlt",
        "anfang_ende_ausgewaehlt",
        "ocr_durchgefuehrt",
        "ocr_vollstaendig",
        "audioanalyse_spektrogramm",
        "validierung",
    ]
    for col in status_cols:
        if col in out.columns:
            out[col] = out[col].map(_norm_cell)
    return out

def _overview_status_true(value) -> bool:
    txt = str(value or "").strip()
    return txt in (LAMP_GREEN, MOJIBAKE_GREEN, "Ja", "True", "true", "1") or LAMP_GREEN in txt


def _render_mat_selection_analysis(df: pd.DataFrame, title_suffix: str = "") -> None:
    """Render a non-interactive MAT status analysis below the selection table."""
    if df is None or df.empty:
        return
    status_items = [
        ("Audio+Video", "audio_video_vorhanden"),
        ("ROI", "roi_ausgewaehlt"),
        ("Track", "track_ausgewaehlt"),
        ("Start/Ende", "anfang_ende_ausgewaehlt"),
        ("OCR", "ocr_durchgefuehrt"),
        ("OCR vollst.", "ocr_vollstaendig"),
        ("Audioanalyse", "audioanalyse_spektrogramm"),
        ("Validierung", "validierung"),
    ]
    total = int(len(df))
    rows = []
    for label, col in status_items:
        ok = int(df[col].map(_overview_status_true).sum()) if col in df.columns else 0
        pct = (100.0 * ok / total) if total else 0.0
        rows.append((label, ok, total, pct))

    st.markdown('<div class="section-card">', unsafe_allow_html=True)
    st.markdown(f'<div class="section-title">MAT-Analyse{title_suffix}</div>', unsafe_allow_html=True)

    kpi_html = ['<div class="mat-analysis-grid">']
    for label, ok, total_n, pct in rows[:4]:
        kpi_html.append(
            f'''<div class="mat-analysis-card">
                <div class="mat-analysis-title">{label}</div>
                <div class="mat-analysis-value">{ok}/{total_n}</div>
                <div class="mat-progress-outer"><div class="mat-progress-inner" style="width:{pct:.1f}%"></div></div>
                <div class="mat-analysis-sub">{pct:.0f}% vorhanden</div>
            </div>'''
        )
    kpi_html.append('</div>')
    st.markdown("".join(kpi_html), unsafe_allow_html=True)

    bar_html = ['<div class="mat-analysis-bars">']
    for label, ok, total_n, pct in rows:
        bar_html.append(
            f'''<div class="mat-analysis-bar-row">
                <div>{label}</div>
                <div class="mat-analysis-bar-track"><div class="mat-analysis-bar-fill" style="width:{pct:.1f}%"></div></div>
                <div>{ok}/{total_n} · {pct:.0f}%</div>
            </div>'''
        )
    bar_html.append('</div>')
    st.markdown("".join(bar_html), unsafe_allow_html=True)
    st.markdown('</div>', unsafe_allow_html=True)

def _update_all_mat_overview_rows(remote_keys: list[str], live_table=None, progress_slot=None):
    """
    Backward-compatible synchronous updater for MAT overview rows.
    """
    _start_mat_update(remote_keys)
    total = len(st.session_state.mat_update_keys or [])
    progress = progress_slot.progress(0, text=f"0/{total} MAT-Dateien analysiert") if (total > 0 and progress_slot is not None) else None
    try:
        cache = st.session_state.get("mat_summary_cache")
        if not isinstance(cache, dict):
            cache = {}

        targets = list(st.session_state.mat_update_keys or [])
        pending_idx = []
        done = 0
        for i, t in enumerate(targets):
            mk = str(t.get("mat_key", "") or "")
            folder = str(t.get("folder", "") or "")
            if mk and mk in cache:
                st.session_state.mat_overview_rows[i] = _summary_to_overview_row(cache[mk], display_folder=folder)
                done += 1
            else:
                pending_idx.append(i)

        st.session_state.mat_update_idx = done
        if live_table is not None:
            _render_disabled_mat_overview_table(live_table, st.session_state.mat_overview_rows)
        if progress is not None and total > 0:
            progress.progress(min(1.0, done / total), text=f"{done}/{total} MAT-Dateien analysiert")

        if pending_idx:
            max_workers = max(2, min(8, (os.cpu_count() or 4)))
            pfx = st.session_state.r2_prefix.strip("/")
            client = st.session_state.r2_client
            with cf.ThreadPoolExecutor(max_workers=max_workers) as ex:
                fut_to_idx = {}
                for i in pending_idx:
                    t = targets[i]
                    mk = str(t.get("mat_key", "") or "")
                    folder = str(t.get("folder", "") or "")
                    if mk:
                        fut = ex.submit(_compute_mat_summary_remote, mk, client, pfx)
                    else:
                        fut = ex.submit(_compute_folder_only_summary, folder, client, pfx)
                    fut_to_idx[fut] = i

                for fut in cf.as_completed(fut_to_idx):
                    i = fut_to_idx[fut]
                    t = targets[i]
                    mk = str(t.get("mat_key", "") or "")
                    folder = str(t.get("folder", "") or "")
                    try:
                        summary = fut.result()
                    except Exception as e:
                        summary = {"mat_file": Path(mk).name if mk else "", "remote_key": mk, "error": f"{e.__class__.__name__}: {e}"}
                    if mk:
                        cache[mk] = dict(summary)
                    st.session_state.mat_overview_rows[i] = _summary_to_overview_row(summary, display_folder=folder)
                    done += 1
                    st.session_state.mat_update_idx = done
                    if live_table is not None and (done == total or done % 3 == 0 or done <= 2):
                        _render_disabled_mat_overview_table(live_table, st.session_state.mat_overview_rows)
                    if progress is not None and total > 0:
                        progress.progress(min(1.0, done / total), text=f"{done}/{total} MAT-Dateien analysiert")

        st.session_state.mat_summary_cache = cache
        st.session_state.mat_update_running = False
        st.session_state.mat_run_state = "idle"
    except Exception:
        while st.session_state.mat_update_running:
            _step_mat_update_once()
            done = int(st.session_state.mat_update_idx or 0)
            if live_table is not None and (done == total or done % 3 == 0 or done <= 2):
                live_table.dataframe(
                    pd.DataFrame(st.session_state.mat_overview_rows),
                    width="stretch",
                    hide_index=True,
                    height=MAT_TABLE_HEIGHT,
                    column_config=MAT_OVERVIEW_COLCFG,
                )
            if progress is not None and total > 0:
                progress.progress(min(1.0, done / total), text=f"{done}/{total} MAT-Dateien analysiert")

    if progress is not None:
        progress.empty()


def _try_load_video_for_capture_folder(capture_folder: str) -> bool:
    if not capture_folder:
        return False
    if st.session_state.r2_client is None:
        return False
    # Cloud workflow: always use reduced frame-pack.
    return _load_framepack_from_r2(capture_folder)


def _overview_status_is_green(value) -> bool:
    txt = str(value or "").strip()
    return txt in {LAMP_GREEN, MOJIBAKE_GREEN, "Ja", "OK", "True", "true", "1"}


def _current_capture_folder() -> str:
    cf = str(st.session_state.get("capture_folder") or "").strip().strip("/\\")
    if cf:
        return cf
    video_name = str(st.session_state.get("video_name") or "").strip()
    if video_name:
        stem = Path(video_name.replace(" [frames_1fps]", "")).stem
        if stem:
            return stem
    selected = str(st.session_state.get("mat_selected_key") or "").strip()
    if selected:
        return _mat_capture_guess_from_key(selected) or Path(selected).stem.replace("results_", "", 1)
    return ""


def _capture_results_dir_key(capture_folder: str) -> str:
    pfx = st.session_state.r2_prefix.strip("/")
    folder = str(capture_folder or "").strip("/\\")
    if not folder:
        return _results_dir_key().strip("/")
    return f"{pfx}/captures/{folder}" if pfx else f"captures/{folder}"


def _invalidate_and_update_mat_selection_for_capture(capture_folder: str, mat_key: str = "") -> None:
    """Refresh cached MAT-selection status for the saved capture without rescanning everything."""
    folder = str(capture_folder or "").strip("/\\")
    key = str(mat_key or "").strip("/")
    try:
        if key and isinstance(st.session_state.get("mat_summary_cache"), dict):
            st.session_state.mat_summary_cache.pop(key, None)
        if key and st.session_state.get("r2_client") is not None:
            summary = _get_mat_summary_from_r2(key)
            row = _summary_to_overview_row(summary, display_folder=folder)
            rows = list(st.session_state.get("mat_overview_rows") or [])
            replaced = False
            for i, old in enumerate(rows):
                if str(old.get("mat_datei", "")) == folder or str(old.get("remote_key", "")) == key:
                    rows[i] = row
                    replaced = True
                    break
            if not replaced and rows:
                rows.insert(0, row)
            st.session_state.mat_overview_rows = rows
            st.session_state.mat_selected_key = key
            st.session_state.mat_pending_selected_key = key
            st.session_state.mat_selected_summary = summary

            targets = list(st.session_state.get("mat_targets") or [])
            found = False
            for t in targets:
                if str(t.get("folder", "")) == folder:
                    t["mat_key"] = key
                    found = True
                    break
            if not found and folder:
                targets.insert(0, {"kind": "folder", "folder": folder, "mat_key": key})
            st.session_state.mat_targets = targets
    except Exception as e:
        set_status(f"MAT Selection konnte nicht aktualisiert werden: {e}", "warn")


def _find_next_roi_setup_target() -> dict | None:
    """Return next capture with reduced audio+video in cloud but without ROI parameters."""
    rows = list(st.session_state.get("mat_overview_rows") or [])
    if not rows and st.session_state.get("mat_targets"):
        try:
            _update_all_mat_overview_rows(st.session_state.mat_targets)
            rows = list(st.session_state.get("mat_overview_rows") or [])
        except Exception:
            rows = []
    current = _current_capture_folder()
    candidates = []
    for idx, row in enumerate(rows):
        folder = str(row.get("mat_datei", "") or "").strip()
        if not folder:
            folder = _mat_capture_guess_from_key(str(row.get("remote_key", "") or ""))
        if not folder or folder == current:
            continue
        media_ok = _overview_status_is_green(row.get("audio_video_vorhanden"))
        roi_missing = not _overview_status_is_green(row.get("roi_ausgewaehlt"))
        if media_ok and roi_missing:
            candidates.append({"idx": idx, "folder": folder, "remote_key": str(row.get("remote_key", "") or "")})
    if candidates:
        return candidates[0]

    for t in list(st.session_state.get("mat_targets") or []):
        folder = str(t.get("folder", "") or "").strip()
        if not folder or folder == current:
            continue
        key = str(t.get("mat_key", "") or "")
        try:
            summary = _get_mat_summary_from_r2(key) if key else _compute_folder_only_summary(folder, st.session_state.r2_client, st.session_state.r2_prefix.strip("/"))
            if bool(summary.get("video_file_exists")) and bool(summary.get("audio_file_exists")) and not bool(summary.get("roi_selected")):
                return {"idx": -1, "folder": folder, "remote_key": key}
        except Exception:
            continue
    return None


def _load_next_roi_setup_file() -> tuple[bool, str]:
    if not st.session_state.get("r2_connected") or st.session_state.get("r2_client") is None:
        return False, "Cloud ist nicht verbunden."
    nxt = _find_next_roi_setup_target()
    if not nxt:
        return False, "Keine nächste Datei gefunden (Audio+Video vorhanden und ROI fehlt)."
    folder = str(nxt.get("folder") or "")
    if not _try_load_video_for_capture_folder(folder):
        return False, f"Reduzierte Datei konnte nicht geladen werden: {folder}"
    st.session_state.capture_folder = folder
    key = str(nxt.get("remote_key") or "")
    if key:
        st.session_state.mat_selected_key = key
        st.session_state.mat_pending_selected_key = key
        try:
            _analyze_mat_from_r2(key)
        except Exception:
            pass
    set_status(f"Nächste Datei geladen: {folder}", "ok")
    return True, folder

def _load_video_from_r2(remote_key):
    client = st.session_state.r2_client
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=Path(remote_key).suffix)
    tmp.close()
    with st.spinner(f"Lade {Path(remote_key).name} ..."):
        ok, msg = client.download_file(remote_key, tmp.name)
    if ok: _apply_video(tmp.name, Path(remote_key).name)
    else:  set_status(f"Download: {msg}", "warn")

def _load_json_from_r2(remote_key):
    client = st.session_state.r2_client
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".json"); tmp.close()
    ok, msg = client.download_file(remote_key, tmp.name)
    if ok:
        try:
            with open(tmp.name) as f: load_json_config(json.load(f))
            set_status("JSON geladen OK", "ok")
        except Exception as e: set_status(f"JSON-Parse: {e}", "warn")
    else: set_status(f"JSON-Download: {msg}", "warn")

def _load_mat_from_r2(remote_key):
    client = st.session_state.r2_client
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".mat"); tmp.close()
    ok, msg = client.download_file(remote_key, tmp.name)
    if not ok: set_status(f"MAT-Download: {msg}", "warn"); return None
    try:
        cfg = config_from_mat_file(tmp.name, vid_duration=st.session_state.vid_duration)
        st.session_state.t_start = cfg.get("t_start", st.session_state.t_start)
        st.session_state.t_end = cfg.get("t_end", st.session_state.t_end)
        st.session_state.t_current = float(st.session_state.t_start)
        direct_rois = _extract_rois_from_recordresult_mat(tmp.name)
        use_rois = direct_rois or cfg.get("rois", st.session_state.rois)
        if not direct_rois:
            use_rois = _apply_roi_format_map(use_rois, _extract_roi_format_map_from_recordresult_hdf5(tmp.name))
        st.session_state.rois = _sanitize_rois(use_rois)
        st.session_state.selected_roi = None
        st.session_state.roi_draw_armed = False
        st.session_state.drag_roi = {}
        st.session_state.roi_wait_user_move = False
        st.session_state.roi_anchor_box = {}
        st.session_state.roi_reject_anchor_events = 0
        st.session_state.roi_editor_df = None
        if cfg.get("ref_track_pts"):
            st.session_state.ref_track_pts = cfg["ref_track_pts"]
        if cfg.get("minimap_pts"):
            st.session_state.minimap_pts = cfg["minimap_pts"]
        track_cfg = _extract_track_cal_from_recordresult_mat(tmp.name)
        if track_cfg.get("track_roi"):
            _upsert_track_minimap_roi_from_mat(track_cfg["track_roi"])
        if track_cfg.get("minimap_pts"):
            st.session_state.minimap_pts = track_cfg["minimap_pts"]
            st.session_state.minimap_next_pt_idx = len(track_cfg["minimap_pts"])
        if track_cfg.get("moving_pt_color_range"):
            st.session_state.moving_pt_color_range = track_cfg["moving_pt_color_range"]
        _track_name = _extract_track_name_from_recordresult_mat(tmp.name)
        if _track_name:
            st.session_state["loaded_track_name"] = _track_name
            _try_auto_load_reference_track_for_name(_track_name)
        set_status("MAT geladen OK","ok")
        return tmp.name
    except Exception as e: set_status(f"MAT-Parse: {e}","warn")
    return None

def _apply_centerline_to_session(mat_path: str, mat_name: str) -> None:
    """Parse .mat → centerline → render image + auto-set fixed ref points."""
    try:
        cl = load_centerline_from_mat(mat_path)
    except Exception as e:
        set_status(f"Centerline-Fehler: {e}", "warn")
        return
    fixed = guess_fixed_points(mat_name)
    img, fp_px, cl_px = render_centerline_image(cl, fixed_pts=fixed, size_px=800)
    st.session_state.centerline = cl.tolist()
    st.session_state.centerline_px = cl_px
    st.session_state.ref_track_mat_name = mat_name
    st.session_state.ref_track_img = img
    if fp_px is not None:
        st.session_state.ref_track_pts = fp_px
    set_status(f"Centerline geladen: {mat_name} ({len(cl)} Punkte)", "ok")


def _autosave_slim_centerline_to_r2() -> None:
    if not (st.session_state.centerline is not None and st.session_state.r2_connected and st.session_state.r2_client is not None):
        return
    _base = Path(st.session_state.ref_track_mat_name or "track").stem
    _slim_name = (_base if _base.lower().endswith("_slim") else _base + "_slim") + ".mat"
    _pfx = st.session_state.r2_prefix.strip("/")
    _ref_dir = (_pfx + "/reference_track_siesmann").strip("/") if _pfx else "reference_track_siesmann"
    try:
        ok_ls, items = st.session_state.r2_client.list_files(_ref_dir)
        if ok_ls and isinstance(items, list):
            existing = [Path(str(x)).name for x in items if not str(x).endswith("/")]
            if _slim_name in existing:
                return
        _tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".mat")
        _tmp.close()
        save_slim_mat(np.array(st.session_state.centerline, dtype=np.float64), _tmp.name)
        _ok, _msg = st.session_state.r2_client.upload_file(_tmp.name, f"{_ref_dir}/{_slim_name}")
        try:
            Path(_tmp.name).unlink(missing_ok=True)
        except Exception:
            pass
        if _ok:
            set_status(f"Slim automatisch gespeichert: {_slim_name}", "ok")
        else:
            set_status(f"Slim-Auto-Upload fehlgeschlagen: {_msg}", "warn")
    except Exception as e:
        set_status(f"Slim-Auto-Upload Fehler: {e}", "warn")

def _load_centerline_from_r2(remote_key: str, mat_name: str) -> None:
    client = st.session_state.r2_client
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".mat")
    tmp.close()
    ok, msg = client.download_file(remote_key, tmp.name)
    if ok:
        _apply_centerline_to_session(tmp.name, mat_name)
        _autosave_slim_centerline_to_r2()
    else:
        set_status(f"Download fehlgeschlagen: {msg}", "warn")


_try_auto_connect_once()
_try_auto_connect_local_once()

# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ Header + Status ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬
st.markdown("""
<div class="app-header">
  <h1>OCR Extractor</h1>
  <span class="subtitle">R2 | ROI | TRACK</span>
</div>""", unsafe_allow_html=True)

stype = st.session_state.status_type
_pfx_display = st.session_state.r2_prefix or ""
_pfx_badge = (f'<span class="status-badge status-ok" style="margin-left:8px">'
              f'PREFIX: {_pfx_display.upper()}</span>'
              if st.session_state.r2_connected and _pfx_display
              else "")
st.markdown(
    f'<span class="status-badge status-{stype}">{st.session_state.status_msg}</span>' + _pfx_badge,
    unsafe_allow_html=True)

# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ TABS ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬
_tab_labels = ["Cloud Connection & Root", "Sync", "MAT Selection", "ROI Setup"]
_tab_default = st.session_state.get("tab_default")
_tab_kwargs = {}
if isinstance(_tab_default, str) and _tab_default in _tab_labels:
    _tab_kwargs["default"] = _tab_default
try:
    tab_setup, tab_sync, tab_mat, tab_roi = st.tabs(_tab_labels, **_tab_kwargs)
    tab_track = tab_roi
except TypeError:
    tab_setup, tab_sync, tab_mat, tab_roi = st.tabs(_tab_labels)
    tab_track = tab_roi
finally:
    pass

if isinstance(_tab_default, str) and _tab_default in _tab_labels:
    _try_jump_to_tab(_tab_default)
    # One-shot tab jump target.
    st.session_state.tab_default = None

# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â
# TAB ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Â¹Ãƒâ€¦Ã¢â‚¬Å“ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¸ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â  ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ CLOUD & DATEIEN
# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â
with tab_setup:
    with st.expander("Debug-Logs", expanded=False):
        st.caption(f"Crash-Log Datei: {LOG_FILE}")
        if LOG_FILE.exists():
            try:
                log_text = LOG_FILE.read_text(encoding="utf-8", errors="ignore")
            except Exception as e:
                log_text = f"Log konnte nicht gelesen werden: {e}"
            st.code(log_text[-12000:] if log_text else "(leer)", language="text")
            c_log1, c_log2 = st.columns(2)
            c_log1.download_button(
                "Log herunterladen",
                data=log_text.encode("utf-8", errors="ignore"),
                file_name="app_crash.log",
                mime="text/plain",
                width="stretch",
            )
            if c_log2.button("Log leeren", width="stretch"):
                try:
                    LOG_FILE.write_text("", encoding="utf-8")
                    set_status("Crash-Log geleert.", "info")
                    st.rerun()
                except Exception as e:
                    set_status(f"Log konnte nicht geleert werden: {e}", "warn")
        else:
            st.info("Noch kein Crash-Log vorhanden.")

    cloud_ok = bool(st.session_state.r2_connected)
    local_ok = bool(st.session_state.local_connected)

    col_cloud, col_local = st.columns(2, gap="large")

    with col_cloud:
        st.markdown('<div class="section-card" style="background:#0b1524;border-color:#234465;">', unsafe_allow_html=True)
        st.markdown('<div class="section-title">Cloud DB | Cloudflare R2</div>', unsafe_allow_html=True)
        # Card 1: Status
        st.markdown(
            f"""
            <div style="background:#0b1524;border:1px solid #2b4f77;border-radius:10px;padding:.8rem 1rem;margin-bottom:.7rem;">
              <div style="font-family:'JetBrains Mono',monospace;font-size:.66rem;color:#8aa8c7;text-transform:uppercase;letter-spacing:.08em;">Cloud DB Status</div>
              <div style="display:flex;align-items:center;gap:10px;margin-top:6px;">
                <span class="conn-dot {'ok' if cloud_ok else 'off'}" style="width:13px;height:13px;"></span>
                <span style="font-family:'Syne',sans-serif;font-size:1.03rem;font-weight:700;color:{'#3ddc84' if cloud_ok else '#ff5c5c'};">
                  {'Verbunden' if cloud_ok else 'Nicht verbunden'}
                </span>
              </div>
            </div>
            """,
            unsafe_allow_html=True,
        )

        # Card 2: Credentials + connect
        with st.container(border=True, key="cloud_access_card"):
            st.markdown(
                "<div style=\"font-family:JetBrains Mono,monospace;font-size:.66rem;color:#8aa8c7;text-transform:uppercase;letter-spacing:.08em;margin-bottom:.45rem;\">Cloud Zugang</div>",
                unsafe_allow_html=True,
            )
            r2_account = st.text_input(
                "Account ID",
                key="r2_account_id",
                help="Cloudflare Dashboard -> R2 -> Account ID",
            )
            r2_key = st.text_input(
                "Access Key ID",
                key="r2_access_key_id",
                help="R2 -> Manage API Tokens -> Create API Token",
            )
            r2_secret = st.text_input(
                "Secret Access Key",
                key="r2_secret_access_key",
                type="password",
            )
            r2_bucket = st.text_input(
                "Bucket Name",
                key="r2_bucket",
                placeholder="mein-bucket",
            )

            if st.button("Cloud DB verbinden", type="primary", width="stretch", key="r2_connect_btn"):
                if r2_account and r2_key and r2_secret and r2_bucket:
                    with st.spinner("Verbinde Cloud DB ..."):
                        _ok, _msg, _client = connect_r2_client(r2_account, r2_key, r2_secret, r2_bucket)
                    if _ok:
                        st.session_state.r2_connected = True
                        st.session_state.r2_client = _client
                        st.session_state.r2_prefix_options = list_root_prefixes(_client)
                        st.session_state.r2_prefix = ""
                        st.session_state.mat_scan_prefix = None
                        set_status("Cloud DB verbunden.", "ok")
                    else:
                        st.session_state.r2_connected = False
                        set_status(f"Cloud DB Verbindung fehlgeschlagen: {_msg}", "warn")
                    st.rerun()
                else:
                    set_status("Bitte alle Cloud-DB Felder ausfuellen.", "warn")
                    st.rerun()

        # Card 3: Root + refresh
        with st.container(border=True, key="cloud_root_card"):
            st.markdown(
                "<div style=\"font-family:JetBrains Mono,monospace;font-size:.66rem;color:#8aa8c7;text-transform:uppercase;letter-spacing:.08em;margin-bottom:.45rem;\">Cloud Root</div>",
                unsafe_allow_html=True,
            )
            if st.session_state.r2_connected:
                opts = st.session_state.r2_prefix_options or [""]
                cur = st.session_state.r2_prefix
                idx = opts.index(cur) if cur in opts else 0
                chosen = st.selectbox(
                    "Cloud Prefix",
                    opts,
                    index=idx,
                    format_func=lambda x: x or "(Bucket-Root)",
                    label_visibility="collapsed",
                    key="root_dd",
                )
                if chosen != st.session_state.r2_prefix:
                    st.session_state.r2_prefix = chosen
                    st.session_state.mat_scan_prefix = None
                    set_status(f"Cloud Root: {chosen or '(root)'}", "ok")
                if st.button("Cloud Liste aktualisieren", width="stretch", key="refresh_root"):
                    st.session_state.r2_prefix_options = get_root_prefixes()
                    st.rerun()
            else:
                st.caption("Erst Cloud DB verbinden.")
        st.markdown('</div>', unsafe_allow_html=True)

    with col_local:
        st.markdown('<div class="section-card" style="background:#132114;border-color:#305b34;">', unsafe_allow_html=True)
        st.markdown('<div class="section-title">Lokale DB</div>', unsafe_allow_html=True)
        # Card 1: Status
        st.markdown(
            f"""
            <div style="background:#132114;border:1px solid #376a3d;border-radius:10px;padding:.8rem 1rem;margin-bottom:.7rem;">
              <div style="font-family:'JetBrains Mono',monospace;font-size:.66rem;color:#9fbe9f;text-transform:uppercase;letter-spacing:.08em;">Lokale DB Status</div>
              <div style="display:flex;align-items:center;gap:10px;margin-top:6px;">
                <span class="conn-dot {'ok' if local_ok else 'off'}" style="width:13px;height:13px;"></span>
                <span style="font-family:'Syne',sans-serif;font-size:1.03rem;font-weight:700;color:{'#3ddc84' if local_ok else '#ff5c5c'};">
                  {'Verbunden' if local_ok else 'Nicht verbunden'}
                </span>
              </div>
            </div>
            """,
            unsafe_allow_html=True,
        )
        # Card 2: Notice + picker + path
        with st.container(border=True, key="local_access_card"):
            st.markdown(
                "<div style=\"font-family:JetBrains Mono,monospace;font-size:.66rem;color:#9fbe9f;text-transform:uppercase;letter-spacing:.08em;margin-bottom:.45rem;\">Lokaler Zugriff</div>",
                unsafe_allow_html=True,
            )
            st.markdown(
                """
                <div style="background:#17301a;border:1px solid #2b5a31;border-radius:8px;padding:.55rem .7rem;
                     font-family:'JetBrains Mono',monospace;font-size:.68rem;color:#b8ddb9;line-height:1.5;margin-bottom:.6rem;">
                Hinweis: Nur auf localhost nutzbar. Der gewaehlte Ordner muss einen Unterordner <b>captures</b> enthalten.
                </div>
                """,
                unsafe_allow_html=True,
            )

            if st.button("Ordner waehlen (lokal)", width="stretch", key="local_pick_btn"):
                try:
                    ok_pick, picked = _pick_local_folder_dialog(st.session_state.local_base_path_input)
                    if ok_pick and picked:
                        st.session_state.local_base_path_input = picked
                        lp = Path(picked).expanduser().resolve()
                        captures_dir = lp / "captures"
                        if captures_dir.exists() and captures_dir.is_dir():
                            local_client = LocalStorageAdapter(str(lp))
                            ok_local, msg_local = local_client.test_connection()
                            if ok_local:
                                st.session_state.local_connected = True
                                st.session_state.local_client = local_client
                                st.session_state.local_base_path = str(lp)
                                st.session_state.local_root = ""
                                set_status(f"Lokale DB verbunden: {lp}", "ok")
                            else:
                                st.session_state.local_connected = False
                                st.session_state.local_client = None
                                set_status(f"Lokale DB Verbindung fehlgeschlagen: {msg_local}", "warn")
                        else:
                            st.session_state.local_connected = False
                            st.session_state.local_client = None
                            set_status("Lokale DB nicht verbunden: Unterordner 'captures' fehlt.", "warn")
                        st.rerun()
                    elif picked:
                        set_status(f"Ordnerdialog nicht verfuegbar: {picked}", "warn")
                except Exception as e:
                    set_status(f"Lokale DB Verbindung fehlgeschlagen: {e}", "warn")
            st.markdown(
                f'<div class="breadcrumb">Lokaler Basispfad: {st.session_state.local_base_path if st.session_state.local_connected else "(noch nicht gesetzt)"}</div>',
                unsafe_allow_html=True,
            )


        st.markdown('</div>', unsafe_allow_html=True)


# Sync Tab
with tab_sync:
    st.markdown('<div class="section-title">Sync Uebersicht (Lokal vs. Cloud)</div>', unsafe_allow_html=True)

    can_sync_compare = bool(st.session_state.r2_connected and st.session_state.local_connected)
    overall_progress_slot = st.empty()
    stage_slot = st.empty()
    table_slot = st.empty()
    sync_refresh_clicked = False
    sync_start_clicked = False
    sync_stop_clicked = False
    selected_queue_folders = list(st.session_state.sync_selected_folders or [])
    edited = None

    def _render_sync_table(df_table: pd.DataFrame):
        table_slot.dataframe(
            df_table[["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"]],
            width="stretch",
            hide_index=True,
            height=340,
            column_config={
                "auswaehlen": st.column_config.CheckboxColumn("Auswaehlen", default=False),
                "capture_folder": st.column_config.TextColumn("MAT/Folder", width="large"),
                "reduziert_in_cloud": st.column_config.TextColumn("Reduzierte Version in Cloud", width="large"),
                "status": st.column_config.TextColumn("Status", width="medium"),
            },
        )

    if not can_sync_compare:
        st.caption("Cloud DB und lokale DB muessen beide verbunden sein.")
    else:
        st.caption("Vergleich: lokale Full-FPS Videos vs. Cloud Frame-Packs (1 fps). Sync extrahiert lokal und laedt Frames hoch.")

    if st.session_state.sync_running:
        c_sync_refresh, c_sync_start, c_sync_stop = st.columns([1, 1, 1])
        sync_refresh_clicked = c_sync_refresh.button(
            "Sync Uebersicht aktualisieren",
            width="stretch",
            disabled=True,
            key="sync_refresh_btn_running",
        )
        c_sync_start.button(
            "Auswahl uebernehmen + Sync starten",
            type="primary",
            width="stretch",
            disabled=True,
            key="sync_start_btn_running",
        )
        sync_stop_clicked = c_sync_stop.button(
            "Stop",
            type="secondary",
            width="stretch",
            key="sync_stop_btn_running",
        )

        df_live = st.session_state.get("sync_editor_value")
        if not isinstance(df_live, pd.DataFrame) or df_live.empty:
            df_live = pd.DataFrame(st.session_state.sync_overview_rows or [])
            if not df_live.empty:
                cols = ["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"]
                for c in cols:
                    if c not in df_live.columns:
                        df_live[c] = False if c == "auswaehlen" else ""
                df_live = df_live[cols]
        if isinstance(df_live, pd.DataFrame) and not df_live.empty:
            _render_sync_table(df_live)
        else:
            _render_sync_table(
                pd.DataFrame(columns=["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"]),
            )
    else:
        cached_editor_df = st.session_state.get("sync_single_table")
        if not isinstance(cached_editor_df, pd.DataFrame):
            cached_editor_df = st.session_state.get("sync_editor_value")
        if not isinstance(cached_editor_df, pd.DataFrame) or cached_editor_df.empty:
            if st.session_state.sync_overview_rows:
                df_sync_editor = pd.DataFrame(st.session_state.sync_overview_rows)[
                    ["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"]
                ].copy()
                if "auswaehlen" not in df_sync_editor.columns:
                    df_sync_editor["auswaehlen"] = False
            else:
                df_sync_editor = pd.DataFrame(columns=["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"])
            st.session_state.sync_editor_value = df_sync_editor.copy()
        else:
            df_sync_editor = cached_editor_df.copy()

        with st.form("sync_form_controls", clear_on_submit=False):
            c_sync_refresh, c_sync_start, c_sync_stop = st.columns([1, 1, 1])
            sync_refresh_clicked = c_sync_refresh.form_submit_button(
                "Sync Uebersicht aktualisieren",
                width="stretch",
                disabled=not can_sync_compare,
            )
            sync_start_clicked = c_sync_start.form_submit_button(
                "Auswahl uebernehmen + Sync starten",
                width="stretch",
                type="primary",
                disabled=not can_sync_compare,
            )
            c_sync_stop.form_submit_button(
                "Stop",
                width="stretch",
                disabled=True,
            )

            edited = st.data_editor(
                df_sync_editor[["auswaehlen", "capture_folder", "reduziert_in_cloud", "status"]],
                width="stretch",
                hide_index=True,
                height=340,
                disabled=False,
                column_config={
                    "auswaehlen": st.column_config.CheckboxColumn("Auswaehlen", default=False),
                    "capture_folder": st.column_config.TextColumn("MAT/Folder", width="large"),
                    "reduziert_in_cloud": st.column_config.TextColumn("Reduzierte Version in Cloud", width="large"),
                    "status": st.column_config.TextColumn("Status", width="medium"),
                },
                key="sync_single_table",
            )
            if isinstance(edited, pd.DataFrame):
                st.session_state.sync_editor_value = edited.copy()

    if sync_refresh_clicked:
        try:
            _refresh_sync_overview_live(table_slot=table_slot, progress_slot=overall_progress_slot)
            st.rerun()
        except Exception as e:
            set_status(f"Sync-Uebersicht Fehler: {e}", "warn")

    if sync_stop_clicked:
        st.session_state.sync_stop_requested = True
        set_status("Stop angefordert: Sync stoppt nach aktueller Datei.", "warn")

    if sync_start_clicked:
        selected_from_editor = []
        edited_df = edited if isinstance(edited, pd.DataFrame) else st.session_state.get("sync_single_table")
        if not isinstance(edited_df, pd.DataFrame):
            edited_df = st.session_state.get("sync_editor_value")
        if isinstance(edited_df, pd.DataFrame) and (not edited_df.empty):
            selected_from_editor = [
                str(row["capture_folder"])
                for _, row in edited_df.iterrows()
                if bool(row.get("auswaehlen"))
            ]
        st.session_state.sync_selected_folders = selected_from_editor

        if (not selected_from_editor) and (not st.session_state.sync_queue_rows):
            set_status("Sync-Queue ist leer.", "warn")
        else:
            target_folders = selected_from_editor if selected_from_editor else [
                str(r.get("capture_folder", ""))
                for r in (st.session_state.sync_queue_rows or [])
            ]
            for f in target_folders:
                _set_sync_row_status(f, "Queue")

            _start_sync_run(target_folders)
            if st.session_state.sync_running:
                set_status(f"Sync gestartet ({st.session_state.sync_run_total} Datei(en)).", "info")
                st.rerun()
            else:
                set_status("Keine gueltigen Queue-Dateien fuer Sync ausgewaehlt.", "warn")

    if st.session_state.sync_running:
        idx = int(st.session_state.sync_run_idx)
        total = int(st.session_state.sync_run_total)
        run_queue = st.session_state.sync_run_queue or []
        status_map = dict(st.session_state.sync_status_map or {})

        completed = max(0, idx)
        elapsed = max(0.0, time.time() - float(st.session_state.sync_run_started_ts or time.time()))
        eta = ((elapsed / completed) * (total - completed)) if completed > 0 else 0.0
        overall_progress_slot.progress(
            min(1.0, completed / max(1, total)),
            text=f"Gesamt: {completed}/{total} ({int((completed/max(1,total))*100)}%) | ETA { _format_eta_seconds(eta) }",
        )

        if st.session_state.sync_stop_requested:
            _finish_sync_run("Sync gestoppt.", "warn")
            st.rerun()

        if idx >= total:
            ok_count = sum(1 for v in status_map.values() if str(v).startswith("OK"))
            err_count = sum(1 for v in status_map.values() if str(v).startswith("Fehler"))
            st.session_state.sync_status_map = status_map
            overview_rows, queue_rows = _build_sync_overview_rows()
            st.session_state.sync_overview_rows = overview_rows
            st.session_state.sync_queue_rows = queue_rows
            st.session_state.sync_editor_value = None
            stage_slot.success(f"Sync abgeschlossen: {ok_count} erfolgreich, {err_count} Fehler.")
            _finish_sync_run(
                f"Sync abgeschlossen ({ok_count} OK / {err_count} Fehler).",
                "warn" if err_count > 0 else "ok",
            )
            st.rerun()

        qrow = run_queue[idx]
        folder = str(qrow.get("capture_folder", "")).strip()
        if not folder:
            st.session_state.sync_run_idx = idx + 1
            st.rerun()

        status_map[folder] = "Konvertierung gestartet"
        st.session_state.sync_status_map = status_map
        _set_sync_row_status(folder, "0%")
        src_video = _find_local_fullfps_video(folder)
        if src_video is None:
            status_map[folder] = "Fehler: keine lokale Full-FPS Datei"
            st.session_state.sync_status_map = status_map
            _set_sync_row_status(folder, "Fehler")
            st.session_state.sync_run_idx = idx + 1
            st.rerun()

        stage_slot.empty()

        def _cb(pct: float, txt: str):
            pct = max(0.0, min(1.0, float(pct)))
            _set_sync_row_status(folder, f"{int(pct * 100)}%")
            df_live_cb = st.session_state.get("sync_editor_value")
            if isinstance(df_live_cb, pd.DataFrame) and not df_live_cb.empty:
                _render_sync_table(df_live_cb)
            overall_pct = (idx + (pct * 0.8)) / max(1, total)
            done = int(overall_pct * 100)
            elapsed_l = max(0.0, time.time() - float(st.session_state.sync_run_started_ts or time.time()))
            completed_l = idx + max(0.01, pct)
            eta_l = (elapsed_l / completed_l) * max(0.0, total - completed_l)
            overall_progress_slot.progress(overall_pct, text=f"Gesamt: {idx}/{total} ({done}%) | ETA { _format_eta_seconds(eta_l) }")

        ok_pack, msg_pack, n_frames, audio_note = _upload_framepack_1fps(src_video, folder, progress_cb=_cb)
        if not ok_pack:
            status_map[folder] = f"Fehler Frame-Pack: {msg_pack}"
            st.session_state.sync_status_map = status_map
            _set_sync_row_status(folder, "Fehler")
            st.session_state.sync_run_idx = idx + 1
            st.rerun()
        status_map[folder] = f"OK ({n_frames} Frames{audio_note})"
        _set_sync_row_status(folder, "OK")

        st.session_state.sync_status_map = status_map
        st.session_state.sync_run_idx = idx + 1
        st.rerun()

    st.markdown("---")
    st.caption("Hinweis: Stop wirkt zwischen Dateien (nicht mitten in einer laufenden Konvertierung).")


# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â
# TAB ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â°ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¸ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â§ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â® ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ MAT-AUSWAHL
# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â
with tab_mat:
    st.markdown('<div class="section-card mat-selection-no-scroll">', unsafe_allow_html=True)
    st.markdown('<div class="section-title">MAT-Auswahl und Analyse</div>', unsafe_allow_html=True)

    connected = st.session_state.r2_connected and st.session_state.r2_client is not None
    if connected and st.session_state.mat_scan_prefix != st.session_state.r2_prefix:
        _refresh_mat_files()
        st.session_state.mat_auto_updated_prefix = None

    mat_targets = st.session_state.mat_targets if connected else []
    running = bool(st.session_state.mat_update_running)
    load_running = bool(st.session_state.mat_load_running)

    c1, c2 = st.columns(2)
    update_clicked = c1.button(
        "Update",
        width="stretch",
        key="mat_update_tab",
        disabled=(not connected) or running or load_running,
    )
    can_load = (
        connected
        and bool(st.session_state.mat_overview_rows)
        and bool(st.session_state.get("mat_pending_selected_key", ""))
        and not running
        and not load_running
    )
    load_clicked = c2.button(
        "MAT + Video laden",
        type="primary",
        width="stretch",
        key="mat_load_all_tab",
        disabled=not can_load,
    )

    progress_slot = st.empty()
    table_slot = st.empty()

    if update_clicked:
        if running:
            st.session_state.mat_update_running = False
            st.session_state.mat_run_state = "idle"
            set_status("Analyse abgebrochen.", "warn")
        else:
            _refresh_mat_files()
            mat_targets = st.session_state.mat_targets
            st.session_state.mat_auto_updated_prefix = st.session_state.r2_prefix
            if mat_targets:
                st.session_state.mat_update_running = True
                st.session_state.mat_run_state = "running"
                set_status(f"Analyse gestartet ({len(mat_targets)} Eintraege).", "info")
                _update_all_mat_overview_rows(mat_targets, live_table=table_slot, progress_slot=progress_slot)
                st.session_state.mat_update_running = False
                st.session_state.mat_run_state = "idle"
                set_status(f"Analyse fuer {len(mat_targets)} Eintraege abgeschlossen.", "ok")
            else:
                st.session_state.mat_run_state = "idle"
                set_status("Keine MAT-Dateien gefunden.", "warn")

    if connected and mat_targets and st.session_state.mat_auto_updated_prefix != st.session_state.r2_prefix and not running:
        st.session_state.mat_auto_updated_prefix = st.session_state.r2_prefix
        st.session_state.mat_update_running = True
        st.session_state.mat_run_state = "running"
        _update_all_mat_overview_rows(mat_targets, live_table=table_slot, progress_slot=progress_slot)
        st.session_state.mat_update_running = False
        st.session_state.mat_run_state = "idle"
        set_status(f"Analyse fuer {len(mat_targets)} Eintraege abgeschlossen.", "ok")

    if not connected:
        st.caption("Erst in Tab 'Verbindung & Root' verbinden und Projektroot waehlen.")

    current_selected_key = str(st.session_state.get("mat_pending_selected_key", "") or "")

    if st.session_state.mat_overview_rows:
        df_overview = pd.DataFrame(st.session_state.mat_overview_rows)
        df_overview = _normalize_overview_lamps(df_overview)
        filter_missing_roi = st.checkbox(
            "Nur Fälle anzeigen: ROI fehlt und Audio+Video vorhanden",
            value=False,
            key="mat_filter_missing_roi_with_media",
        )
        display_df = df_overview
        if filter_missing_roi:
            display_df = df_overview[
                (~df_overview["roi_ausgewaehlt"].map(_overview_status_true))
                & (df_overview["audio_video_vorhanden"].map(_overview_status_true))
            ].copy()
            st.caption(f"Filter aktiv: {len(display_df)} von {len(df_overview)} Fällen angezeigt.")
        is_running_now = bool(st.session_state.mat_update_running)
        styled_df = display_df
        allow_select = not is_running_now and not load_running
        if display_df.empty:
            table_slot.empty()
            st.info("Keine Fälle passend zum aktuellen Filter.")
        elif allow_select:
            sel_event = table_slot.dataframe(
                styled_df,
                width="stretch",
                hide_index=True,
                height=MAT_TABLE_HEIGHT,
                column_config=MAT_OVERVIEW_COLCFG,
                key="mat_single_table",
                on_select="rerun",
                selection_mode="single-row",
            )
            selected_rows = []
            if hasattr(sel_event, "selection"):
                selected_rows = list(getattr(sel_event.selection, "rows", []) or [])
            elif isinstance(sel_event, dict):
                selected_rows = list((((sel_event.get("selection") or {}).get("rows")) or []))
            if selected_rows:
                idx0 = int(selected_rows[0])
                if 0 <= idx0 < len(styled_df):
                    current_selected_key = str(styled_df.iloc[idx0].get("remote_key", "") or "")
                    st.session_state.mat_pending_selected_key = current_selected_key
        else:
            with table_slot.container():
                st.markdown('<div class="mat-selection-disabled">MAT-Auswahl wird aktualisiert ...</div>', unsafe_allow_html=True)
                try:
                    disabled_view = styled_df.style.set_properties(**{
                        "background-color": "#20232b",
                        "color": "#7d8491",
                    })
                except Exception:
                    disabled_view = styled_df
                st.dataframe(
                    disabled_view,
                    width="stretch",
                    hide_index=True,
                    height=MAT_TABLE_HEIGHT,
                    column_config=MAT_OVERVIEW_COLCFG,
                )
        _render_mat_selection_analysis(
            display_df,
            title_suffix=" (gefilterte Ansicht)" if filter_missing_roi else "",
        )
    else:
        table_slot.empty()
        st.caption("Noch keine MAT analysiert.")

    if load_clicked and can_load:
        selected_key = str(st.session_state.get("mat_pending_selected_key", "") or "")
        if selected_key:
            st.session_state.mat_selected_key = selected_key
            st.session_state.mat_pending_selected_key = selected_key
            st.session_state.mat_selected_summary = None
            st.session_state.mat_load_requested = True
        else:
            st.session_state.mat_selected_key = ""
            st.session_state.mat_selected_summary = None
            set_status("Bitte zuerst genau eine MAT-Zeile anwaehlen.", "warn")

    if st.session_state.mat_load_requested and (not running) and connected:
        selected = st.session_state.mat_selected_key
        st.session_state.mat_load_requested = False
        if not selected:
            set_status("Bitte zuerst eine Zeile mit MAT-Datei waehlen.", "warn")
            st.rerun()
        st.session_state.mat_load_running = True
        st.rerun()

    if st.session_state.mat_load_running and (not running) and connected:
        selected = st.session_state.mat_selected_key
        st.markdown(
            """
            <style>
            .mat-load-overlay {
              position: fixed;
              inset: 0;
              background: rgba(15, 22, 36, 0.42);
              z-index: 9998;
              display: flex;
              align-items: center;
              justify-content: center;
            }
            .mat-load-overlay-box {
              background: rgba(7, 14, 26, 0.90);
              color: #d7e8ff;
              border: 1px solid rgba(74, 144, 164, 0.45);
              border-radius: 12px;
              padding: 14px 18px;
              font-weight: 700;
            }
            </style>
            <div class="mat-load-overlay"><div class="mat-load-overlay-box">MAT + Video wird geladen ...</div></div>
            """,
            unsafe_allow_html=True,
        )
        with st.spinner("Lade MAT + Video ..."):
            _analyze_mat_from_r2(selected)
            summary = st.session_state.mat_selected_summary or {}
            capture_folder = summary.get("capture_folder") or _mat_capture_guess_from_key(selected)
            video_ok = _try_load_video_for_capture_folder(capture_folder)
            if video_ok:
                st.session_state.capture_folder = capture_folder
            mat_loaded = _load_mat_from_r2(selected)
            if mat_loaded is None:
                set_status("MAT konnte nicht geladen werden.", "warn")
            elif not video_ok:
                set_status("MAT geladen, aber kein passendes Video gefunden.", "warn")
        st.session_state.mat_load_running = False
        st.session_state.tab_default = "ROI Setup"
        st.rerun()

    st.markdown('</div>', unsafe_allow_html=True)


# ==============================
# TAB ROI SETUP
# ==============================
with tab_roi:
    if not _has_media_source():
        st.markdown("""
        <div style="text-align:center;padding:3rem 2rem;color:#4a5060;">
          <div style="font-size:2.5rem;margin-bottom:.8rem">VIDEO</div>
          <div style="font-family:'Syne',sans-serif;font-size:1rem;font-weight:600">
            Kein Video geladen</div>
          <div style="font-family:'JetBrains Mono',monospace;font-size:.72rem;
               margin-top:.4rem;color:#2e3545">
            -> Tab CLOUD oeffnen -> Video laden oder von R2 laden</div>
        </div>""", unsafe_allow_html=True)
    else:
        st.markdown(
            """
            <style>
            .roi-compact .section-card { margin-bottom: .4rem !important; padding: .5rem .7rem !important; }
            [data-testid="stTabContent"] { overflow: visible !important; }
            </style>
            """,
            unsafe_allow_html=True,
        )
        st.markdown('<div class="roi-compact">', unsafe_allow_html=True)

        dur = st.session_state.vid_duration
        fps = st.session_state.vid_fps
        fw = st.session_state.vid_width
        fh = st.session_state.vid_height
        step_s = round(1 / max(fps, 1.0), 4)

        prev_start = float(st.session_state.get("_roi_prev_start", st.session_state.t_start))
        prev_end = float(st.session_state.get("_roi_prev_end", st.session_state.t_end))
        prev_tcur = float(st.session_state.get("_roi_prev_tcur", st.session_state.t_current))

        col_v, col_r = st.columns([1, 1], gap="medium")
        with col_v:
            st.markdown('<div class="section-card">', unsafe_allow_html=True)
            st.markdown('<div id="roi-left-width-probe"></div>', unsafe_allow_html=True)
            _sc1, _sc2 = st.columns(2)
            _min_gap = 1.0 / max(fps, 1.0)
            _start_max = max(0.0, float(st.session_state.t_end) - _min_gap)
            t_start = _sc1.slider(
                "Start [s]",
                0.0,
                float(_start_max),
                float(min(max(st.session_state.t_start, 0.0), _start_max)),
                step=step_s,
                format="%d s",
                key="sl_start",
            )
            _end_min = min(float(dur), float(t_start) + _min_gap)
            t_end_raw = _sc2.slider(
                "Ende [s]",
                float(_end_min),
                float(dur),
                float(min(max(st.session_state.t_end, _end_min), float(dur))),
                step=step_s,
                format="%d s",
                key="sl_end",
            )
            t_start, t_end = normalize_time_range(
                start_s=float(t_start),
                end_s=float(t_end_raw),
                duration_s=float(dur),
                fps=float(fps),
            )
            st.session_state.t_start = float(t_start)
            st.session_state.t_end = float(min(t_end, dur))

            start_changed = abs(t_start - prev_start) > 1e-9
            end_changed = abs(st.session_state.t_end - prev_end) > 1e-9
            if start_changed and not end_changed:
                st.session_state.t_current = float(st.session_state.t_start)
            elif end_changed and not start_changed:
                st.session_state.t_current = float(st.session_state.t_end)
            elif start_changed and end_changed:
                st.session_state.t_current = float(st.session_state.t_start)
            st.session_state._roi_prev_start = float(st.session_state.t_start)
            st.session_state._roi_prev_end = float(st.session_state.t_end)

            t_cur = st.slider(
                "Position [s]",
                float(st.session_state.t_start),
                float(st.session_state.t_end),
                float(min(max(st.session_state.t_current, st.session_state.t_start), st.session_state.t_end)),
                step=step_s,
                format="%d s",
                key="sl_cur",
            )
            st.session_state.t_current = float(t_cur)
            frame_idx = int(round(float(t_cur) * max(float(fps), 1.0)))
            prev_frame_idx = st.session_state.get("roi_prev_frame_idx")
            t_changed = (prev_frame_idx is None) or (int(prev_frame_idx) != frame_idx)
            st.session_state._roi_prev_tcur = float(t_cur)
            st.session_state.roi_prev_frame_idx = frame_idx
            if t_changed and isinstance(st.session_state.selected_roi, int) and 0 <= st.session_state.selected_roi < len(st.session_state.rois):
                _ar = st.session_state.rois[st.session_state.selected_roi]
                st.session_state.roi_anchor_box = {
                    "x": int(round(float(_ar.get("x", 0)))),
                    "y": int(round(float(_ar.get("y", 0)))),
                    "w": int(round(float(_ar.get("w", 0)))),
                    "h": int(round(float(_ar.get("h", 0)))),
                }
                st.session_state.roi_wait_user_move = True
                st.session_state.roi_reject_anchor_events = 0

            frame = _get_media_frame(t_cur)
            drag_roi = None
            _editing_idx = st.session_state.selected_roi
            show_draw_box = bool(st.session_state.get("roi_draw_armed", False))
            # Keep all existing ROIs visible; active ROI is still shown by cropper box.
            _skip_bg = None
            if frame is not None:
                vis_rgb = draw_rois(frame, st.session_state.rois, _editing_idx, fw, fh, skip_idx=_skip_bg)
            if frame is not None:
                st.caption(f"t={t_cur:.3f}s  |  {fw}x{fh}  |  {fps:.1f}fps")
                # Hard no-clipping mode: full frame is resized to a fixed fit size (constant per video).
                src_w = int(fw) if int(fw) > 0 else int(vis_rgb.shape[1])
                src_h = int(fh) if int(fh) > 0 else int(vis_rgb.shape[0])
                disp_meta = st.session_state.get("roi_display_meta", {})
                dims_key = (int(src_w), int(src_h))
                if not isinstance(disp_meta, dict) or tuple(disp_meta.get("dims", ())) != dims_key:
                    # Stable width in ROI column, height follows source aspect ratio.
                    target_w = _get_dynamic_roi_target_width(620, "roi-left-width-probe")
                    target_w = min(target_w, max(1, src_w))
                    fit_w = int(target_w)
                    fit_h = max(1, int(round(fit_w * (src_h / float(max(1, src_w))))))
                    disp_meta = {"dims": dims_key, "w": int(fit_w), "h": int(fit_h)}
                    st.session_state.roi_display_meta = disp_meta
                else:
                    target_w_now = _get_dynamic_roi_target_width(int(disp_meta.get("w", 620)), "roi-left-width-probe")
                    target_w_now = min(target_w_now, max(1, src_w))
                    fit_w = int(target_w_now)
                    fit_h = max(1, int(round(fit_w * (src_h / float(max(1, src_w))))))
                    disp_meta = {"dims": dims_key, "w": int(fit_w), "h": int(fit_h)}
                    st.session_state.roi_display_meta = disp_meta
                off_x = 0
                off_y = 0
                if fit_w != src_w or fit_h != src_h:
                    disp_rgb = cv2.resize(vis_rgb, (fit_w, fit_h), interpolation=cv2.INTER_AREA)
                else:
                    disp_rgb = vis_rgb
                scale_x = src_w / float(max(1, fit_w))
                scale_y = src_h / float(max(1, fit_h))

                sel_idx_now = st.session_state.selected_roi
                has_active_roi = (
                    isinstance(sel_idx_now, int)
                    and 0 <= sel_idx_now < len(st.session_state.rois)
                )
                if st_cropper is not None and has_active_roi:
                    src = st.session_state.rois[sel_idx_now]
                    sx = int(round(float(src.get("x", 0))))
                    sy = int(round(float(src.get("y", 0))))
                    sw = int(round(float(src.get("w", 0))))
                    sh = int(round(float(src.get("h", 0))))
                    sx, sy, sw, sh = _clamp_roi_to_video(sx, sy, sw, sh, fw, fh)
                    sx_d = int(round(float(sx) / max(scale_x, 1e-9))) + off_x
                    sy_d = int(round(float(sy) / max(scale_y, 1e-9))) + off_y
                    sw_d = int(round(float(sw) / max(scale_x, 1e-9)))
                    sh_d = int(round(float(sh) / max(scale_y, 1e-9)))
                    sx_d = max(off_x, min(off_x + fit_w - 1, sx_d))
                    sy_d = max(off_y, min(off_y + fit_h - 1, sy_d))
                    sw_d = max(1, min(off_x + fit_w - sx_d, sw_d))
                    sh_d = max(1, min(off_y + fit_h - sy_d, sh_d))

                    crop_box = None
                    pil_vis = Image.fromarray(disp_rgb)
                    cropper_key = (
                        f"roi_cropper_main_{frame_idx}_"
                        f"{int(sel_idx_now) if isinstance(sel_idx_now, int) else -1}_"
                        f"{int(fit_w)}x{int(fit_h)}"
                    )
                    try:
                        crop_box = st_cropper(
                            pil_vis,
                            realtime_update=True,
                            box_color="#4a90a4",
                            aspect_ratio=None,
                            return_type="box",
                            should_resize_image=False,
                            default_coords=(int(sx_d), int(sx_d + sw_d), int(sy_d), int(sy_d + sh_d)),
                            key=cropper_key,
                        )
                    except TypeError:
                        crop_box = st_cropper(
                            pil_vis,
                            realtime_update=True,
                            box_color="#4a90a4",
                            aspect_ratio=None,
                            return_type="box",
                            key=cropper_key,
                        )

                    if isinstance(crop_box, dict):
                        dx_d = int(round(float(crop_box.get("left", sx_d))))
                        dy_d = int(round(float(crop_box.get("top", sy_d))))
                        dw_d = int(round(float(crop_box.get("width", sw_d))))
                        dh_d = int(round(float(crop_box.get("height", sh_d))))
                        x1_d = dx_d
                        y1_d = dy_d
                        x2_d = dx_d + dw_d
                        y2_d = dy_d + dh_d
                        # Intersect with actual image area (exclude black letterbox bars).
                        x1_fit = max(0, min(fit_w, x1_d - off_x))
                        y1_fit = max(0, min(fit_h, y1_d - off_y))
                        x2_fit = max(0, min(fit_w, x2_d - off_x))
                        y2_fit = max(0, min(fit_h, y2_d - off_y))
                        if x2_fit < x1_fit:
                            x1_fit, x2_fit = x2_fit, x1_fit
                        if y2_fit < y1_fit:
                            y1_fit, y2_fit = y2_fit, y1_fit
                        dx = int(round(x1_fit * scale_x))
                        dy = int(round(y1_fit * scale_y))
                        dw = int(round((x2_fit - x1_fit) * scale_x))
                        dh = int(round((y2_fit - y1_fit) * scale_y))
                        if dw > 0 and dh > 0:
                            cx, cy, cw_roi, ch_roi = _clamp_roi_to_video(dx, dy, dw, dh, fw, fh)
                            drag_roi = {"x": int(cx), "y": int(cy), "w": int(cw_roi), "h": int(ch_roi)}
                            st.session_state.drag_roi = drag_roi
                    st.caption("ROI direkt mit der Maus ziehen/skalieren.")
                elif st_cropper is not None:
                    # No active ROI selected: show frame only (no default blue box).
                    st.image(disp_rgb, width="stretch")
                elif streamlit_image_coordinates is not None:
                    st.warning("Drag fehlt: installiere 'streamlit-cropper-fix' fuer Ziehen mit der Maus.")
                    click = streamlit_image_coordinates(disp_rgb, key=f"roi_img_click_{frame_idx}")
                    if click and isinstance(click, dict):
                        cx_d = int(round(float(click.get("x", 0))))
                        cy_d = int(round(float(click.get("y", 0))))
                        cx = int(round(max(0, min(fit_w - 1, cx_d - off_x)) * scale_x))
                        cy = int(round(max(0, min(fit_h - 1, cy_d - off_y)) * scale_y))
                        st.session_state.drag_roi = {"x": cx, "y": cy, "w": 4, "h": 4}
                else:
                    st.image(disp_rgb, width="stretch")
                    st.warning(
                        "Fuer ROI-Drag bitte installieren: pip install streamlit-cropper-fix"
                    )
            else:
                st.warning("Frame nicht verfuegbar.")

            drag_state = st.session_state.get("drag_roi", {})
            if not isinstance(drag_state, dict):
                drag_state = {}
            sel_idx = st.session_state.selected_roi
            _is_active = isinstance(sel_idx, int) and 0 <= sel_idx < len(st.session_state.rois)
            if _is_active:
                cur_sel = st.session_state.rois[sel_idx]
                # Prefer live cropper data; fall back to stored ROI position
                if drag_roi is not None:
                    dx = int(drag_state.get("x", 0))
                    dy = int(drag_state.get("y", 0))
                    dw = int(drag_state.get("w", 0))
                    dh = int(drag_state.get("h", 0))
                else:
                    dx = int(round(float(cur_sel.get("x", 0))))
                    dy = int(round(float(cur_sel.get("y", 0))))
                    dw = int(round(float(cur_sel.get("w", 0))))
                    dh = int(round(float(cur_sel.get("h", 0))))
            else:
                dx = int(drag_state.get("x", 0))
                dy = int(drag_state.get("y", 0))
                dw = int(drag_state.get("w", 0))
                dh = int(drag_state.get("h", 0))

            # Live-sync position: cropper drag ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ update active ROI x/y/w/h
            sel_idx = st.session_state.selected_roi
            if (
                drag_roi is not None
                and isinstance(sel_idx, int)
                and 0 <= sel_idx < len(st.session_state.rois)
            ):
                ok_drag, _ = can_add_roi_from_drag({"x": dx, "y": dy, "w": dw, "h": dh})
                if ok_drag:
                    cx, cy, cw_roi, ch_roi = _clamp_roi_to_video(dx, dy, dw, dh, fw, fh)
                    cur = st.session_state.rois[sel_idx]
                    if (
                        int(round(float(cur.get("x", 0)))) != int(cx)
                        or int(round(float(cur.get("y", 0)))) != int(cy)
                        or int(round(float(cur.get("w", 0)))) != int(cw_roi)
                        or int(round(float(cur.get("h", 0)))) != int(ch_roi)
                    ):
                        st.session_state.rois[sel_idx] = {
                            **st.session_state.rois[sel_idx],
                            "x": float(cx), "y": float(cy), "w": float(cw_roi), "h": float(ch_roi),
                        }

            st.markdown('</div>', unsafe_allow_html=True)

        with col_r:
            st.markdown('<div class="section-card">', unsafe_allow_html=True)
            st.markdown('<div class="section-title">ROI-Liste</div>', unsafe_allow_html=True)

            if st.button("ROI hinzuf\u00fcgen", type="primary", width="stretch", key="roi_add_btn"):
                # New ROI starts from a fresh default rectangle (independent from existing ROI).
                _d = seed_drag_roi(fw, fh)
                _cx, _cy, _cw, _ch = _clamp_roi_to_video(
                    int(_d["x"]), int(_d["y"]), int(_d["w"]), int(_d["h"]), fw, fh
                )
                st.session_state.rois.append(dict(
                    name="_", x=float(_cx), y=float(_cy),
                    w=float(_cw), h=float(_ch), fmt="any", max_scale=float(st.session_state.get("roi_global_scale", 1.2)),
                ))
                _ni = len(st.session_state.rois) - 1
                st.session_state.selected_roi = _ni
                st.session_state.roi_draw_armed = True
                st.session_state.drag_roi = {"x": int(_cx), "y": int(_cy), "w": int(_cw), "h": int(_ch)}
                st.session_state.roi_anchor_box = {"x": int(_cx), "y": int(_cy), "w": int(_cw), "h": int(_ch)}
                st.session_state.roi_wait_user_move = True
                st.session_state.roi_reject_anchor_events = 0
                st.session_state.roi_editor_df = None
                set_status("ROI hinzugef\u00fcgt. Name in Tabelle setzen und Position mit K\u00e4stchen anpassen.", "ok")
                st.rerun()

            _rois = st.session_state.rois
            _sel = st.session_state.selected_roi
            _tbl_h = min(220, 38 * (len(_rois) + 1) + 4) if _rois else 80
            _sel_col = "__sel__"
            _base_rows = [
                {
                    _sel_col: bool(i == _sel),
                    "Name": str(r.get("name", "_")),
                    "Format": str(r.get("fmt", "any")) if str(r.get("fmt", "any")) != "custom" else "any",
                    "OCR OK": bool(r.get("ocr_test_ok", False)),
                    "OCR Wert": str(r.get("ocr_test_value", "")),
                    "OCR Details": str(r.get("ocr_test_details", "")),
                }
                for i, r in enumerate(_rois)
            ]
            _roi_cols = [_sel_col, "Name", "Format", "OCR OK", "OCR Wert", "OCR Details"]
            _base_df = pd.DataFrame(_base_rows, columns=_roi_cols)
            if _base_df.empty:
                _base_df = pd.DataFrame(
                    {
                        _sel_col: pd.Series(dtype="bool"),
                        "Name": pd.Series(dtype="object"),
                        "Format": pd.Series(dtype="object"),
                        "OCR OK": pd.Series(dtype="bool"),
                        "OCR Wert": pd.Series(dtype="object"),
                        "OCR Details": pd.Series(dtype="object"),
                    }
                )
            else:
                _base_df[_sel_col] = _base_df[_sel_col].fillna(False).astype(bool)
            _cached_df = st.session_state.get("roi_editor_df")
            if (
                isinstance(_cached_df, pd.DataFrame)
                and list(_cached_df.columns) == list(_base_df.columns)
                and len(_cached_df) == len(_base_df)
            ):
                df_edit = _cached_df.copy().reindex(columns=_roi_cols)
                df_edit[_sel_col] = df_edit[_sel_col].fillna(False).astype(bool)
                df_edit[_sel_col] = [(i == _sel) for i in range(len(df_edit))]
            else:
                df_edit = _base_df.copy()

            if _sel_col not in df_edit.columns:
                df_edit[_sel_col] = False
            df_edit = pd.DataFrame(
                {
                    _sel_col: pd.Series([bool(v) for v in df_edit[_sel_col].tolist()], dtype="bool"),
                    "Name": pd.Series([str(v) for v in df_edit["Name"].tolist()], dtype="object"),
                    "Format": pd.Series([str(v) if str(v) != "custom" else "any" for v in df_edit["Format"].tolist()], dtype="object"),
                    "OCR OK": pd.Series([bool(v) for v in df_edit.get("OCR OK", pd.Series([False] * len(df_edit))).tolist()], dtype="bool"),
                    "OCR Wert": pd.Series([str(v) for v in df_edit.get("OCR Wert", pd.Series([""] * len(df_edit))).tolist()], dtype="object"),
                    "OCR Details": pd.Series([str(v) for v in df_edit.get("OCR Details", pd.Series([""] * len(df_edit))).tolist()], dtype="object"),
                },
                columns=_roi_cols,
            )

            edited_df = st.data_editor(
                df_edit,
                column_config={
                    _sel_col: st.column_config.CheckboxColumn("", width=42),
                    "Name": st.column_config.SelectboxColumn("Name", options=ROI_NAMES, width=150),
                    "Format": st.column_config.SelectboxColumn("Format", options=FMT_OPTIONS, width=170),
                    "OCR OK": st.column_config.CheckboxColumn("OCR OK", width=70, disabled=True),
                    "OCR Wert": st.column_config.TextColumn("OCR Wert", width=110, disabled=True, help="OCR-Testwert; Details stehen in der Spalte OCR Details."),
                    "OCR Details": st.column_config.TextColumn("OCR Details", width=210, disabled=True, help="raw, conf, scale und frUp aus dem letzten OCR-Test."),
                },
                num_rows="fixed",
                width="stretch",
                hide_index=True,
                height=_tbl_h,
                key=str(st.session_state.get("roi_editor_widget_key", "roi_data_editor_v3")),
            )

            if edited_df is not None and len(edited_df) == len(_rois):
                if _sel_col in edited_df.columns:
                    edited_df[_sel_col] = edited_df[_sel_col].fillna(False).astype(bool)
                st.session_state.roi_editor_df = edited_df.copy()
                _checked_rows = [
                    _i for _i, _row in edited_df.iterrows()
                    if bool(_row.get(_sel_col, False))
                ]
                _newly_sel = int(_checked_rows[0]) if _checked_rows else _sel
                _meta_changed = False
                for _i, _row in edited_df.iterrows():
                    _r = _rois[_i]
                    _nn = str(_row["Name"]) if pd.notna(_row["Name"]) else _r.get("name", "_")
                    _nf = str(_row["Format"]) if pd.notna(_row["Format"]) else _r.get("fmt", "any")
                    if _nf == "custom":
                        _nf = "any"
                    if (_r.get("name") != _nn or _r.get("fmt") != _nf):
                        st.session_state.rois[_i] = {**_r, "name": _nn, "fmt": _nf}
                        st.session_state.rois[_i].pop("pattern", None)
                        _meta_changed = True
                if isinstance(_newly_sel, int) and _newly_sel != _sel and 0 <= _newly_sel < len(st.session_state.rois):
                    st.session_state.selected_roi = _newly_sel
                    st.session_state.roi_draw_armed = True
                    _sr = st.session_state.rois[_newly_sel]
                    st.session_state.roi_anchor_box = {
                        "x": int(round(float(_sr.get("x", 0)))),
                        "y": int(round(float(_sr.get("y", 0)))),
                        "w": int(round(float(_sr.get("w", 0)))),
                        "h": int(round(float(_sr.get("h", 0)))),
                    }
                    st.session_state.roi_wait_user_move = True
                    st.session_state.roi_reject_anchor_events = 0
                    st.rerun()
                if _meta_changed:
                    st.rerun()
            act_sel = st.session_state.selected_roi

            _scale_val = st.number_input(
                "OCR Scale fuer alle ROIs",
                min_value=0.1,
                max_value=5.0,
                value=float(st.session_state.get("roi_global_scale", 1.2)),
                step=0.1,
                key="roi_global_scale_input",
                help="Gemeinsamer Scale-Wert fuer OCR; gilt fuer alle ROIs.",
            )
            if abs(float(st.session_state.get("roi_global_scale", 1.2)) - float(_scale_val)) > 1e-9:
                st.session_state.roi_global_scale = float(_scale_val)
                for _r in st.session_state.rois:
                    if isinstance(_r, dict):
                        _r["max_scale"] = float(_scale_val)

            if isinstance(act_sel, int) and 0 <= act_sel < len(st.session_state.rois):
                sr = st.session_state.rois[act_sel]
                st.caption(
                    f"#{act_sel} {sr.get('name','')} "
                    f"[{int(sr.get('x',0))},{int(sr.get('y',0))},{int(sr.get('w',0))},{int(sr.get('h',0))}]"
                )

            _ocr_probe_indices = [
                _i for _i, _r in enumerate(st.session_state.rois)
                if str(_r.get("name", "")).strip().lower() != "track_minimap"
            ]
            _can_ocr_probe = frame is not None and bool(_ocr_probe_indices)
            if st.button(
                "OCR-Test ROI",
                width="stretch",
                key="roi_ocr_probe_btn",
                disabled=not _can_ocr_probe,
                help="Testet alle ROIs außer track_minimap.",
            ):
                tess_cmd = find_tesseract_cmd()
                if not tess_cmd:
                    st.error("Tesseract wurde nicht gefunden. Installiere Tesseract oder setze TESSERACT_CMD.")
                else:
                    all_probe_results = []
                    for _idx in _ocr_probe_indices:
                        probe_roi = {
                            **st.session_state.rois[_idx],
                            "max_scale": float(st.session_state.get("roi_global_scale", 1.2)),
                        }
                        probe = diagnose_roi_ocr(
                            frame,
                            probe_roi,
                            (int(fw), int(fh)),
                            tmp_root=LOG_DIR / "ocr_tmp",
                        )
                        _conf = float(probe.get("confidence", 0.0) or 0.0)
                        _scale = probe.get("scale", "")
                        _fr_up = probe.get("frUp", probe.get("fr_up", probe.get("variant", "")))
                        _details = (
                            f"raw={probe.get('raw', '')}; "
                            f"conf={_conf:.2f}; "
                            f"scale={_scale}; "
                            f"frUp={_fr_up}"
                        )
                        st.session_state.rois[_idx] = {
                            **st.session_state.rois[_idx],
                            "ocr_test_ok": bool(probe.get("ok")),
                            "ocr_test_value": probe.get("value", ""),
                            "ocr_test_raw": probe.get("raw", ""),
                            "ocr_test_confidence": _conf,
                            "ocr_test_scale": _scale,
                            "ocr_test_frUp": _fr_up,
                            "ocr_test_error": probe.get("error", ""),
                            "ocr_test_details": _details,
                        }
                        all_probe_results.append({
                            "idx": _idx,
                            "name": st.session_state.rois[_idx].get("name", ""),
                            **probe,
                        })
                    st.session_state.roi_ocr_probe_result = all_probe_results
                    st.session_state.roi_editor_df = None
                    st.rerun()

            if st.button("Ausgew\u00e4hlte ROI l\u00f6schen", width="stretch",
                         key="roi_del_btn", disabled=act_sel is None):
                if isinstance(act_sel, int) and 0 <= act_sel < len(st.session_state.rois):
                    st.session_state.rois.pop(act_sel)
                    st.session_state.selected_roi = None
                    st.session_state.roi_draw_armed = False
                    st.session_state.roi_wait_user_move = False
                    st.session_state.roi_anchor_box = {}
                    st.session_state.roi_reject_anchor_events = 0
                    st.session_state.roi_editor_df = None
                    if st.session_state.media_source == "video":
                        get_frame.clear()
                    set_status("ROI gel\u00f6scht.", "info")
                    st.rerun()





# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â
# TAB ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â°ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¸ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã‚ÂÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Âº ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ TRACK-ANALYSE
# ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â
with tab_track:
    st.markdown('<div class="roi-theme-card"><div class="theme-title">Track Analysis</div><div class="theme-text">Referenz-Track, Minimap-Kalibrierung, Farberkennung und Vergleich sind jetzt direkt im ROI Setup integriert.</div></div>', unsafe_allow_html=True)
    has_ref   = st.session_state.ref_track_img is not None
    track_roi = next((r for r in st.session_state.rois if r["name"]=="track_minimap"),None)
    has_vid   = _has_media_source()
    fw=st.session_state.vid_width; fh=st.session_state.vid_height
    can_cmp = (has_ref and track_roi and has_vid and
               _has_valid_8_points(st.session_state.ref_track_pts) and
               _has_valid_8_points(st.session_state.minimap_pts))

    if not track_roi:
        st.info("[i] Keine track_minimap ROI -> Tab ROI Setup -> ROI anlegen.")

    col_a,col_b=st.columns(2,gap="medium")
    clrs=[(255,80,80),(255,160,0),(255,255,0),(80,255,80),
          (0,200,255),(100,100,255),(200,80,255),(255,80,200)]

    with col_a:
        st.markdown('<div class="section-card">',unsafe_allow_html=True)
        _mat_name = st.session_state.get("ref_track_mat_name", "") or ""
        _ref_title = f"Referenz-Track | {_mat_name}" if _mat_name else "Referenz-Track | Centerline [m]"
        st.markdown(f'<div class="section-title">{_ref_title}</div>', unsafe_allow_html=True)
        if has_ref:
            # P1-P8 already baked into the rendered image by render_centerline_image(); height-limited in this tab.
            st.markdown('<div class="ref-track-fit">', unsafe_allow_html=True)
            st.image(st.session_state.ref_track_img, width=520,
                     caption="P1–P8 fest aus Streckendatei")
            st.markdown('</div>', unsafe_allow_html=True)
            _sl_c1, _sl_c2 = st.columns(2)
            if _sl_c1.button("Neu laden", width="stretch", key="reload_mat_btn"):
                st.session_state.ref_track_img = None
                st.session_state.ref_track_pts = None
                st.session_state.centerline = None
                st.session_state.ref_track_mat_name = ""
                st.rerun()
            _sl_c2.caption("Slim-Datei wird automatisch gespeichert, falls sie in der Cloud fehlt.")
        else:
            st.markdown('<div style="text-align:center;color:#2e3545;padding:.5rem 0;">'
                        'Keine Streckenkarte geladen</div>', unsafe_allow_html=True)
            # Cloud: list .mat files from reference_track_siesmann/
            if st.session_state.r2_connected and st.session_state.r2_client is not None:
                pfx = st.session_state.r2_prefix.strip("/")
                ref_dir = (pfx + "/reference_track_siesmann").strip("/") if pfx else "reference_track_siesmann"
                ok_ls, ref_items = st.session_state.r2_client.list_files(ref_dir)
                if ok_ls and isinstance(ref_items, list):
                    mat_files = [f for f in ref_items if f.lower().endswith(".mat")]
                    mat_files = sorted(
                        mat_files,
                        key=lambda f: (0 if Path(f).stem.lower().endswith("_slim") else 1, Path(f).name.lower()),
                    )
                    if mat_files:
                        sel = st.selectbox(
                            "Streckendatei wählen",
                            mat_files,
                            key="ref_track_sel",
                            label_visibility="collapsed",
                        )
                        if st.button("Aus Cloud laden", width="stretch", key="ref_track_load_btn"):
                            _load_centerline_from_r2(f"{ref_dir}/{sel}", sel)
                            st.rerun()
                    else:
                        st.caption(f"Keine .mat-Dateien in Cloud-Ordner: {ref_dir}")
                else:
                    st.caption("Cloud-Ordner 'reference_track_siesmann' nicht gefunden.")
            else:
                st.caption("Cloud nicht verbunden.")
        st.markdown('</div>', unsafe_allow_html=True)

    with col_b:
        st.markdown('<div class="section-card">',unsafe_allow_html=True)
        st.markdown('<div class="section-title">Minimap | Kalibrierung + Farberkennung</div>',
                    unsafe_allow_html=True)
        if has_vid and track_roi:
            _track_t = st.slider(
                "Zeit fuer Farberkennung / Track [s]",
                float(st.session_state.t_start),
                float(st.session_state.t_end),
                float(min(max(st.session_state.t_current, st.session_state.t_start), st.session_state.t_end)),
                step=round(1 / max(float(st.session_state.vid_fps or 25.0), 1.0), 4),
                format="%d s",
                key="track_color_time_slider",
            )
            st.session_state.t_current = float(_track_t)
            frame=_get_media_frame(st.session_state.t_current)
            if frame is not None:
                crop=extract_minimap_crop(frame,track_roi,fw,fh)
                ch,cw=crop.shape[:2]
                mm_pts=list(st.session_state.minimap_pts or [])
                # Sync counter forward if points already loaded from config/MAT
                if st.session_state.minimap_next_pt_idx < len(mm_pts):
                    st.session_state.minimap_next_pt_idx = len(mm_pts)
                next_idx = st.session_state.minimap_next_pt_idx

                # ── Iterativer Track-Overlay ──────────────────────────────────────
                # Ab 4 Punkten: Homographie berechnen und Centerline auf Minimap projizieren.
                # Je mehr Punkte, desto besser die Überlagerung.
                _cl_px   = st.session_state.get("centerline_px")
                _ref_pts = st.session_state.ref_track_pts
                vis_c = crop.copy()
                if _cl_px and _ref_pts and len(mm_pts) >= 4:
                    try:
                        vis_overlay = vis_c.copy()
                        n_use = min(len(mm_pts), len(_ref_pts))
                        H_fwd, _ = cv2.findHomography(
                            np.array(mm_pts[:n_use], dtype=np.float32),
                            np.array(_ref_pts[:n_use], dtype=np.float32),
                            cv2.RANSAC, 5.0,
                        )
                        if H_fwd is not None:
                            H_inv = np.linalg.inv(H_fwd)
                            # Subsample centerline (every 15th pt) for speed
                            cl_sub = np.array(_cl_px[::15], dtype=np.float32).reshape(-1, 1, 2)
                            cl_mm  = cv2.perspectiveTransform(cl_sub, H_inv).reshape(-1, 2)
                            cl_int = np.round(cl_mm).astype(int)
                            for i in range(len(cl_int) - 1):
                                p1 = (int(cl_int[i, 0]),   int(cl_int[i, 1]))
                                p2 = (int(cl_int[i+1, 0]), int(cl_int[i+1, 1]))
                                if (0 <= p1[0] < cw and 0 <= p1[1] < ch and
                                        0 <= p2[0] < cw and 0 <= p2[1] < ch):
                                    cv2.line(vis_overlay, p1, p2, (0, 220, 100), 1)
                            vis_c = cv2.addWeighted(vis_c, 0.70, vis_overlay, 0.30, 0)
                            _overlay_pts = n_use
                        else:
                            _overlay_pts = 0
                    except Exception:
                        _overlay_pts = 0
                else:
                    _overlay_pts = 0

                # Draw set points on top of overlay (white ring + colored fill + shadow label)
                for pi, pt in enumerate(mm_pts):
                    if pt and len(pt) == 2:
                        px_i, py_i = int(pt[0]), int(pt[1])
                        cv2.circle(vis_c, (px_i, py_i), 9, (255,255,255), 2)
                        cv2.circle(vis_c, (px_i, py_i), 6, clrs[pi%8], -1)
                        cv2.putText(vis_c, f"P{pi+1}", (px_i+10, py_i+5),
                                    cv2.FONT_HERSHEY_SIMPLEX, .45, (0,0,0), 3)
                        cv2.putText(vis_c, f"P{pi+1}", (px_i+10, py_i+5),
                                    cv2.FONT_HERSHEY_SIMPLEX, .45, clrs[pi%8], 1)

                if streamlit_image_coordinates is None:
                    st.warning("Bitte installieren: pip install streamlit-image-coordinates")
                    st.image(vis_c, width="stretch", caption=f"Minimap ({cw}x{ch}px)")
                elif next_idx < 8:
                    # ── Kalibrierung: P1 … P8 anklicken ──
                    # Fixed widget key + dedup: avoids image disappearing when key changes.
                    # The widget returns the last click on every rerun; we only act on a NEW click.
                    _overlay_info = (f"  ·  Track-Overlay: {_overlay_pts} Punkte"
                                     if _overlay_pts >= 4 else "")
                    st.caption(
                        f"Klicke **P{next_idx+1}** auf der Minimap  ·  {next_idx}/8 gesetzt"
                        + _overlay_info
                    )
                    _last_cal = st.session_state.get("_mm_last_click")
                    click = streamlit_image_coordinates(vis_c, key="mm_calibrate")
                    if click and isinstance(click, dict) and click != _last_cal:
                        st.session_state["_mm_last_click"] = click
                        x = int(round(float(click.get("x", 0))))
                        y = int(round(float(click.get("y", 0))))
                        while len(mm_pts) <= next_idx:
                            mm_pts.append([0, 0])
                        mm_pts[next_idx] = [x, y]
                        st.session_state.minimap_pts = mm_pts
                        st.session_state.minimap_next_pt_idx = next_idx + 1
                        st.rerun()
                else:
                    # ── 8 Punkte fertig → Farberkennung per Klick ──
                    # Live detection with current color range (runs every render)
                    cr = st.session_state.moving_pt_color_range
                    mp_live = detect_moving_point(crop, cr)

                    # Overlay: detected blob (yellow) + last click position (cyan cross)
                    vis_detect = vis_c.copy()
                    if mp_live:
                        dx, dy = int(mp_live["x"]), int(mp_live["y"])
                        cv2.circle(vis_detect, (dx, dy), 12, (255, 255, 0), 2)
                        cv2.circle(vis_detect, (dx, dy),  3, (255, 255, 0), -1)
                    _clk_pos = st.session_state.get("_mm_color_click_px")
                    if _clk_pos and 0 <= _clk_pos[0] < cw and 0 <= _clk_pos[1] < ch:
                        cv2.drawMarker(
                            vis_detect, (int(_clk_pos[0]), int(_clk_pos[1])),
                            (0, 255, 255), cv2.MARKER_CROSS, 14, 2,
                        )

                    # Color swatch for current target color
                    h_m=(cr["h_lo"]+cr["h_hi"])//2
                    s_m=(cr["s_lo"]+cr["s_hi"])//2
                    v_m=(cr["v_lo"]+cr["v_hi"])//2
                    swatch=np.zeros((20,40,3),dtype=np.uint8)
                    swatch[:]=cv2.cvtColor(
                        np.array([[[h_m,s_m,v_m]]],dtype=np.uint8),
                        cv2.COLOR_HSV2RGB)[0,0]

                    detected_lbl = "✓ Erkannt" if mp_live else "✗ Nicht erkannt"
                    sc1, sc2 = st.columns([3,1])
                    sc2.image(swatch, caption="Zielfarbe", width="stretch")

                    _last_col = st.session_state.get("_mm_last_color_click")
                    color_click = streamlit_image_coordinates(vis_detect, key="mm_color_pick")
                    if color_click and isinstance(color_click, dict) and color_click != _last_col:
                        st.session_state["_mm_last_color_click"] = color_click
                        cx = max(0, min(cw-1, int(round(float(color_click.get("x",0))))))
                        cy = max(0, min(ch-1, int(round(float(color_click.get("y",0))))))
                        st.session_state["_mm_color_click_px"] = (cx, cy)
                        pixel_rgb = crop[cy, cx]
                        hsv_px = cv2.cvtColor(
                            np.array([[pixel_rgb]],dtype=np.uint8),
                            cv2.COLOR_RGB2HSV)[0,0]
                        h,s,v = int(hsv_px[0]),int(hsv_px[1]),int(hsv_px[2])
                        st.session_state.moving_pt_color_range = dict(
                            h_lo=max(0,h-15),   h_hi=min(179,h+15),
                            s_lo=max(0,s-60),   s_hi=min(255,s+60),
                            v_lo=max(0,v-60),   v_hi=min(255,v+60),
                        )
                        set_status(f"Farbe gesetzt: HSV({h},{s},{v})","ok")
                        st.rerun()

                # Reset / Undo buttons
                rb1, rb2 = st.columns(2)
                if rb1.button("Zurücksetzen", width="stretch", key="mm_pts_reset"):
                    st.session_state.minimap_pts = []
                    st.session_state.minimap_next_pt_idx = 0
                    st.session_state["_mm_last_click"] = None
                    st.session_state["_mm_last_color_click"] = None
                    st.rerun()
                if rb2.button("Letzten entfernen", width="stretch", key="mm_pts_undo"):
                    if next_idx > 0:
                        st.session_state.minimap_pts = mm_pts[:next_idx-1]
                        st.session_state.minimap_next_pt_idx = next_idx-1
                        st.rerun()
                if st.button("Vergleich 5 Zeiten", type="primary", width="stretch", key="cmp_5_times_btn", disabled=not can_cmp):
                    st.session_state["_run_compare_5_times"] = True
                if not can_cmp:
                    st.caption("Benötigt: Referenztrack, track_minimap ROI, Video und je 8 Punkte.")
        else:
            st.markdown('<div style="text-align:center;color:#2e3545;padding:2rem;">'
                        'Video + track_minimap ROI benötigt</div>',unsafe_allow_html=True)
        st.markdown('</div>',unsafe_allow_html=True)

    def _centerline_progress_percent(ref_pt, centerline_px) -> float | None:
        if ref_pt is None or not centerline_px:
            return None
        try:
            p = np.array(ref_pt, dtype=float).reshape(2)
            cl = np.asarray(centerline_px, dtype=float).reshape(-1, 2)
            if cl.shape[0] < 2:
                return None
            seg = cl[1:] - cl[:-1]
            seg_len = np.linalg.norm(seg, axis=1)
            total = float(np.sum(seg_len))
            if total <= 0:
                return None
            best_s = 0.0
            best_d2 = float("inf")
            cum = np.concatenate([[0.0], np.cumsum(seg_len)])
            for i, v in enumerate(seg):
                l2 = float(np.dot(v, v))
                if l2 <= 0:
                    continue
                u = float(np.clip(np.dot(p - cl[i], v) / l2, 0.0, 1.0))
                q = cl[i] + u * v
                d2 = float(np.sum((p - q) ** 2))
                if d2 < best_d2:
                    best_d2 = d2
                    best_s = float(cum[i] + u * seg_len[i])
            return float(np.clip(100.0 * best_s / total, 0.0, 100.0))
        except Exception:
            return None

    def _comparison_overlay_low_opacity(crop, cmp):
        overlay = draw_comparison_overlay(
            crop, st.session_state.ref_track_img,
            st.session_state.minimap_pts, st.session_state.ref_track_pts,
            cmp, st.session_state.moving_pt_color_range,
        )
        try:
            if overlay is not None and overlay.shape == crop.shape:
                return cv2.addWeighted(crop, 0.15, overlay, 0.85, 0)
        except Exception:
            pass
        return overlay

    if st.session_state.pop("_run_compare_5_times", False) and can_cmp:
        with st.spinner("Vergleich 5 Zeiten läuft ..."):
            rng = np.random.default_rng(12345)
            lo, hi = float(st.session_state.t_start), float(st.session_state.t_end)
            times = sorted(rng.uniform(lo, hi, size=5).tolist()) if hi > lo else [lo] * 5
            results = []
            for _t in times:
                _frame = _get_media_frame(float(_t))
                if _frame is None:
                    continue
                _crop = extract_minimap_crop(_frame, track_roi, fw, fh)
                _cmp = compare_minimap_to_reference(
                    _crop, st.session_state.ref_track_img,
                    st.session_state.minimap_pts, st.session_state.ref_track_pts,
                )
                if _cmp.get("error"):
                    continue
                _mp = detect_moving_point(_crop, st.session_state.moving_pt_color_range)
                _ref_pt = None
                _progress = None
                if _mp:
                    _ref_pt = project_point_with_homography((_mp["x"], _mp["y"]), _cmp.get("H"))
                    _progress = _centerline_progress_percent(_ref_pt, st.session_state.get("centerline_px"))
                _overlay = _comparison_overlay_low_opacity(_crop, _cmp)
                results.append({
                    "t": float(_t), "cmp": _cmp, "mp": _mp,
                    "ref_pt": _ref_pt, "progress_pct": _progress,
                    "overlay": _overlay,
                })
            st.session_state.track_comparison_samples = results
            set_status(f"Vergleich fuer {len(results)} Zeiten durchgefuehrt.", "ok")
        st.rerun()

    st.markdown('<div class="section-card">',unsafe_allow_html=True)
    st.markdown('<div class="section-title">Vergleich | Ueberlagerung | Bewegende Punkte</div>',
                unsafe_allow_html=True)
    _samples = st.session_state.get("track_comparison_samples") or []
    if _samples:
        st.markdown("**Test: 5 zufaellige Zeiten zwischen Start und Ende**")
        _cols = st.columns(5)
        for _col, _res in zip(_cols, _samples[:5]):
            _col.image(_res["overlay"], width="stretch", caption=f"t={_res['t']:.2f}s")
            _c = _res.get("cmp", {})
            _mp = _res.get("mp")
            _progress = _res.get("progress_pct")
            _pos_html = f"<div class='track-progress-big'>{_progress:.1f}%</div>" if _progress is not None else "<div class='track-progress-big'>n/a</div>"
            _col.markdown(_pos_html, unsafe_allow_html=True)
            _col.caption(
                f"ø={_c.get('mean_dist_px', 0.0):.1f}px · max={_c.get('max_dist_px', 0.0):.1f}px · "
                f"pt={'ja' if _mp else 'nein'}"
            )
    else:
        st.caption("Noch kein Vergleich durchgeführt. Button steht unter Zurücksetzen / Letzten entfernen.")
    st.markdown('</div>',unsafe_allow_html=True)



    st.markdown('<div class="roi-theme-card"><div class="theme-title">Speicherung</div><div class="theme-text">Speichert die aktuelle ROI-, Zeit- und Track-Konfiguration gemeinsam als JSON und MAT.</div></div>', unsafe_allow_html=True)
    if st.button("Speichern", type="primary", width="stretch", key="roi_save_json_mat_btn_bottom"):
        ok_save, msg_save, payload_save = _save_result_json_and_mat()
        st.session_state["_last_save_payload"] = payload_save
        set_status(msg_save, "ok" if ok_save else "warn")
        st.rerun()

    if st.button("Nächste Datei laden", width="stretch", key="roi_load_next_missing_btn_bottom"):
        ok_next, msg_next = _load_next_roi_setup_file()
        set_status(msg_next if isinstance(msg_next, str) else str(msg_next), "ok" if ok_next else "warn")
        st.rerun()

    _last_save = st.session_state.get("_last_save_payload") or {}
    if _last_save:
        targets = " · ".join(str(x) for x in (_last_save.get("targets") or []))
        st.markdown(f'<div class="save-status-card">Zuletzt gespeichert: <b>{_last_save.get("json_name", "results.json")}</b> + <b>{_last_save.get("mat_name", "results.mat")}</b><br>{targets}</div>', unsafe_allow_html=True)
        st.download_button("JSON herunterladen", _last_save.get("json_bytes", b""), _last_save.get("json_name", "results.json"), "application/json", width="stretch", key="json_download_bottom")
        st.download_button("MAT herunterladen", _last_save.get("mat_bytes", b""), _last_save.get("mat_name", "results.mat"), "application/octet-stream", width="stretch", key="mat_download_bottom")

    if False and st.session_state.moving_pt_history:
        st.markdown('<div class="section-card">',unsafe_allow_html=True)
        st.markdown('<div class="section-title">Verlauf bewegender Punkt</div>',
                    unsafe_allow_html=True)
        import pandas as pd
        c1,c2=st.columns([1,4])
        c1.metric("Positionen",len(st.session_state.moving_pt_history))
        if c1.button("Leeren",key="hist_clear"):
            st.session_state.moving_pt_history=[]; st.rerun()
        df=pd.DataFrame(st.session_state.moving_pt_history[-100:])
        c2.dataframe(df, width="stretch", height=180)
        st.markdown('</div>',unsafe_allow_html=True)
